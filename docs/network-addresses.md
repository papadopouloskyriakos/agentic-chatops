# Network Addresses Reference

Auto-generated by `scripts/refresh-network-addresses.py` on 2026-04-23 02:05 UTC from live IaC configs.
Source of truth: the firewall configs at
`infrastructure/{nl,gr}/production/network/configs/Firewall/`.

## FRR Route Reflectors

### NL site
- **RR1**: `10.0.X.X` — primary reflector on NL. Cluster-id `1.1.1.1`.
- **RR2**: `10.0.X.X` — secondary reflector on NL.

### GR site
- **RR1**: `10.0.X.X` — primary reflector on GR. Cluster-id `2.2.2.2`.
- **RR2**: `10.0.X.X` — secondary reflector on GR.

Every ASA and VPS peers to BOTH RR1 and RR2 for redundancy.

## ASA BGP peers (extracted 2026-04-23 02:05 UTC)

### nl-fw01 — 8 iBGP peers

| Peer IP | Remote AS | Description |
|---|---|---|
| `10.255.200.X` | 65000 | GR-ASA-Freedom-VTI |
| `10.0.X.X` | 65000 | nlrtr01-budget-edge |
| `10.0.X.X` | 65000 | nlk8s-frr01-DMZ |
| `10.0.X.X` | 65000 | nlk8s-frr02-DMZ |
| `10.0.X.X` | 65001 | (no description) |
| `10.0.X.X` | 65001 | (no description) |
| `10.0.X.X` | 65001 | (no description) |
| `10.0.X.X` | 65001 | (no description) |

### gr-fw01 — 7 iBGP peers

| Peer IP | Remote AS | Description |
|---|---|---|
| `10.255.200.X` | 65000 | NL-ASA-XS4ALL-VTI |
| `10.255.200.X` | 65000 | NL-ASA-Freedom-VTI |
| `10.0.X.X` | 65000 | grk8s-frr01-DMZ |
| `10.0.X.X` | 65000 | grk8s-frr02-DMZ |
| `10.0.58.X` | 65001 | (no description) |
| `10.0.58.X` | 65001 | (no description) |
| `10.0.58.X` | 65001 | (no description) |

## VPS loopback addresses

- `chzrh01vps01` loopback: `10.255.X.X` (router-id, monitoring)
- `notrf01vps01` loopback: `10.255.X.X` (router-id, monitoring)

VPS loopbacks are advertised via FRR `network 10.255.X.X/24` / `network 10.255.X.X/24` but NOT used as `update-source` for NL RR peers (must use VTI /31).

## VPS VTI update-source (2026-04-10 fix)

| VPS | RR1 update-source | RR2 update-source |
|---|---|---|
| chzrh01vps01 | 10.255.200.X (Freedom) | 10.255.200.X (xs4all) |
| notrf01vps01 | 10.255.200.X (Freedom) | 10.255.200.X (xs4all) |

GR RR peers and inter-VPS peer still use loopback update-source.

## BGP communities (deployed 2026-04-23 for IFRGRSKG01PRD-200)

Origin tagging at VPS edges + receiver-side LP policy at ASAs. Standard 32-bit `AS:value` format.

| Community | Semantic | Set by | Matched by |
|-----------|----------|--------|------------|
| `65000:100` | Origin = any VPS edge (policy trigger) | both VPS FRRs on their local /24 | GR ASA `CL-VPS-ORIGIN` → LP 50 via `BUDGET_IN seq 5`, `FREEDOM_IN seq 5`. rtr01 `XCLUSTER_RR_IN seq 5` (from GR-FRR peers). |
| `65000:101` | Origin = chzrh01vps01 | CH VPS only | informational |
| `65000:102` | Origin = notrf01vps01 | NO VPS only | informational |

Reserved for future symmetric work (not yet deployed): `65000:200` (NL-origin), `65000:300` (GR-origin).

**ASA 9.16 syntax gotchas:** `community-list` takes NO `ip` prefix (IOS-only); `clear bgp ipv4 unicast soft in` rejected — must be per-neighbor `clear bgp <peer-ip> soft in`; `send-community both` cosmetically hidden by `show run` even when active.

## Cross-cluster RR-client peerings

| Peer pair | Established | Role |
|-----------|-------------|------|
| `nlrtr01 ↔ grk8s-frr01` (via Port-channel4.2 ↔ 10.0.X.X) | 2026-04-23 | rtr01 as RR-client of GR cluster (cluster 2.2.2.2) |
| `nlrtr01 ↔ grk8s-frr02` | 2026-04-23 | same, GR-FRR02 |
| VPSs ↔ both NL-FRRs + both GR-FRRs | existing | each VPS is a 4-RR-cluster client |

**NOT established** (ASA 9.16 hardware limitation): `gr-fw01 ↔ nlk8s-frr01/02` and `nl-fw01 ↔ grk8s-frr01/02`. Reason: ASA 9.16 lacks `neighbor update-source` and its to-the-box policy drops cross-interface TCP targeting ASA interface IPs. The FRR sidecar design compensates — each ASA peers only with local FRRs; cross-cluster propagation rides the existing FRR-to-FRR mesh. See `memory/asa_9_16_bgp_limitations.md`.

## Key constant facts (one-liner form)

- **NL FRR RR1 is 10.0.X.X, RR2 is 10.0.X.X.**
- **NL ASA has 8 iBGP peers.**
- **GR ASA has 7 iBGP peers.**
- **NL FRR cluster-id 1.1.1.1; GR FRR cluster-id 2.2.2.2; NL rtr01 cluster-id 3.3.3.3.**
- **VPS loopbacks: chzrh01=10.255.X.X, notrf01=10.255.X.X.**
- **FRR failure-domain separation verified 2026-04-23:** NL-FRR01→pve01, NL-FRR02→pve03; GR-FRR01→pve01, GR-FRR02→pve02.

## Scanners

- `nlsec01` — `10.0.181.X` (scans GR + VPS targets)
- `grsec01` — `10.0.X.X` (scans NL + VPS targets)

## LibreNMS

- NL: `nl-nms01` — `10.0.181.X`
- GR: `gr-nms01` — dedicated GR instance

## Dedicated service endpoints

- Ollama: `nl-gpu01:11434`
- Anthropic shim: `nl-gpu01:11435`
- Rerank service (bge-reranker-v2-m3): `nl-gpu01:11436`

Last regenerated: 2026-04-23 02:05 UTC
