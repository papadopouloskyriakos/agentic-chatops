# Teacher-agent lockfile directory (IFRNLLEI01PRD-654).
# Holds /var/lib/claude-gateway/teacher-{morning_nudge,class_digest}.last —
# mtime bumped by teacher-agent.py._touch_last_run() on each successful cron
# invocation; read by scripts/write-learning-metrics.sh and keyed off by
# TeacherAgent{MorningNudge,ClassDigest}Stale Prometheus alerts.
#
# Type  Path                     Mode UID           GID           Age Argument
d       /var/lib/claude-gateway  0755 app-user app-user -   -
