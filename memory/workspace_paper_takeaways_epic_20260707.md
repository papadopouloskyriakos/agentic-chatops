# Workspace-paper takeaways — epic IFRNLLEI01PRD-1663 (Global Workspace guardrails, 2026-07-07)

2026-07-07: audited Anthropic's "Verbalizable Representations Form a Global Workspace
in Language Models" (transformer-circuits.pub/2026/workspace) and applied the ~8%
directly-actionable-to-a-black-box-orchestrator takeaways to the gateway.

**Epic IFRNLLEI01PRD-1663** (4 subtasks) → **MR !152** (branch `feature/workspace-paper-takeaways`, commit `d5b8c5b`, 4 files). NOT merged; nothing live-active except item 4.

**Deploy state — the non-obvious part (each is reversible, off by default):**
- **-1664 reasoning_transparency** — `config/prompt-patches.json` entry, `active:false` (STAGED). Go-live = set `active:true`.
- **-1665 silent-cognition guard** — `classify-session-risk.py::_assign_bands`: under sentinel `~/gateway.silent_cognition_guard`, an AUTO band whose draft reply has NO fenced post-state evidence demotes AUTO→AUTO_NOTICE (auto-resolves + SMS). Nested under AUTONOMY_FORWARD + own sentinel → flag-off byte-identical. Go-live = `touch ~/gateway.silent_cognition_guard`. + QA suite `scripts/qa/suites/test-1665-silent-cognition-guard.sh` (5/5).
- **-1666 reflection_checkpoint** — `config/prompt-patches.json` entry, `active:false` (STAGED). CRT analog, prompt-level. Go-live = set `active:true`.
- **-1667 eval-awareness canary caveat** — `synthetic-incident-canary.sh` emits `synthetic_incident_canary_eval_awareness_caveat 1` + header note. **LIVE** (pure annotation).

I deliberately did NOT flip 1664/1665/1666 — they change every dispatched prompt / the autonomy gate globally, which is an operator go-live decision (matches the `autonomy_forward` sentinel discipline). Offered to activate on request.

**Key architectural finding (reusable):** `scripts/build-prompt-layers.py` is **dead code** — its docstring claims "called by the n8n Runner Build Prompt node via SSH" but it is referenced NOWHERE in the live Runner (`workflows/claude-gateway-runner.json`) — only in `config/interaction-graph.json`. The REAL live prompt-injection surface is `config/prompt-patches.json` → emitted as `PROMPT_PATCHES:<json>` (by `prompt-improver.py` / Query Knowledge node) → the Build Prompt Code node injects only entries where `active && (!expires_at || expires_at>now)`. So standing prompt directives go in `prompt-patches.json` (no n8n Code-node edit needed — avoids the 14h-outage validator gate). The prompt-patcher (`prompt_patch_trial.py`) APPENDS winners to this file, so manual entries survive. Don't edit `build-prompt-layers.py` expecting it to affect live sessions. [[feedback_verify_agent_generated_doc_claims]]

**Tests:** risk-integration 10/10 + OOD 4/4 (no regression), guard unit 5/5, prompt-trials test-645 16/0, canary e2e (gauge emits, stages 3/3, leak 0), band-invariant audit GREEN.

**Orchestrator ownership wired (commit 198481f, in MR !152):** the 4 guardrails are now owned components, not dark-component risks. `scripts/write-workspace-guardrail-metrics.py` (benign read-only health writer) → `prom:workspace_guardrail`, piggybacked on `write-governance-metrics.py` (*/15, isolated subprocess — no new Cronicle job). Registered in `component-registry.json` (owner=epic, non-critical while staged → flip critical=true on go-live). Metrics: `workspace_guardrail_feature_active{feature}`, `_prompt_patch_injectable{dimension}` (activated-but-dropped drift), `_silent_cognition_demotions_total{window}` (guard ON + flat demotions = silently dead), `_last_run_timestamp`. Verified LIVE (registry-check 361 comps / 0 CRITICAL-dark, prom:workspace_guardrail LIVE age 0s). Follow-up: `WorkspaceGuardrailActiveButSilent` Prom alert (IaC repo, on go-live).

**Confidence: high** on correctness/no-regression (each change unit+integration tested, guard inert by default proven). Full illustrated report of the paper itself: artifact https://claude.ai/code/artifact/c679a02a-d2e0-4ce9-9155-a64c3cbd64d2 (+ /tmp/workspace-audit-report.zip, 94 captured figures).

**Open:** operator to review MR !152 and decide go-live flips for 1664/1665/1666.

---
**GO-LIVE UPDATE 2026-07-07 (session cont'd):** reviewed/merged/went-live/e2e-tested per operator directive.
- MR !152 (polluted — cut from WIP branch docs/agt-benchmark, dragged 81 unrelated files) → **closed**, replaced by clean MR **!153** (9 files off main, +REQ-008 spec + lockstep) → CI green → **merged** (main 03ea672).
- **E2E caught 2 that didn't meet "much better" bar** (verify-before-claiming): item2 silent-cognition guard was a **production NO-OP** — the Phase-4 classifier plan (build-investigation-plan.sh) carries no draft_reply, so the guard never fired; and the protection already existed via the Prepare Result **J4 evidence-missing check** (CONFIDENCE≥0.8 → strip [AUTO-RESOLVE]). Items1&3 (prompt patches) were real but unproven-global-changes. Presented to operator → chose "reposition item2" + "A/B trial items1&3".
- **Item2 repositioned to Phase 5 (Prepare Result), LIVE** — MR **!154** (main 8f1de2e). classify emits `silent_cognition_guard` flag (sentinel-gated, byte-identical off) threaded Phase4→5; Prepare Result J4 now suppresses an [AUTO-RESOLVE] with no fenced evidence at ANY confidence (was ≥0.8). Live n8n PUT versionId 9963504e via validator gate (VALIDATION PASSED + test-fire 4/4). Sentinel `~/gateway.silent_cognition_guard` ACTIVE.
- **Items1&3 in live A/B trials** (prompt_patch_trial ids 6=evidence_based, 7=safety_compliance) — directive vs control ~50/50, judge-scored, finalize-prompt-trials.py Welch-t-test promotes only if beats baseline (ev 4.66 / safety 4.795). Patches stay active:false unless promoted.
- **n8n API key** = `jq -r '.mcpServers["n8n-mcp"].env.N8N_API_KEY' ~/.claude.json` (MCP is SSRF-blocked for private IPs; use curl). Runner is `executeWorkflowTrigger` (not webhook) → Code-node change applies next execution, no toggle.
- **Deploy model confirmed:** repo checkout IS the deployment (n8n SSH nodes call the repo path). Live checkout is on `feature/workspace-paper-takeaways` (= operator WIP + my code); NOT switched to main (would revert operator's live config WIP). classify+workflow synced to live checkout + committed (52be1e1).
- **Reusable lesson:** a unit test passing ≠ the feature fires in the real data flow — item2's test-1665 passed by hand-passing a draft the production path never provides. [[feedback_verify_agent_generated_doc_claims]]

---
**TRIAL-DATA-PIPELINE BUG FOUND + FIXED 2026-07-07 (MR !155):** operator asked "are we sure we're gathering the eval data?" — we were NOT. The prompt-patch A/B trials (and every trial "since April") gathered **~0 joinable samples**: `session_trial_assignment` had 5 valid rows / 1 session in 2.5 months. Root cause: Runner **Query Knowledge** node set `ISSUE_ID="{{ $json.issueId || '' }}"` — `$json` there is the upstream **Send Pre Stats** SSH node's output (`{stdout,stderr,exitCode}`, no issueId) → EMPTY → `prompt-trial-assign.py --issue ""` wrote `issue_id=""` rows that can never join `session_judgment.issue_id` → finalizer Welch-t-test saw 0 samples. (Also broke the L7 session-cost lookup.) Fix: use `$('Called by Receiver').first().json.issueId` (the trigger ref every OTHER node + the same node's KB lookup already use). Live n8n PUT HTTP 200; end-to-end join PROVEN (collect_arm_scores joins a valid-issue_id pair). Volume ~24 sessions/day + judge healthy (209/209 scored evidence_based+safety_compliance/7d) → trials 6(evidence_based)/7(safety_compliance) will now accumulate real samples, judgeable in ~1-2 weeks. **Reusable lesson:** in n8n, `$json.X` = the *immediately upstream* node's output, NOT the trigger — reference the trigger node explicitly (`$('<trigger>').first().json.X`) for fields that must survive intermediate SSH/Code nodes. This is the 3rd "looked done but wasn't" catch this session (item2 no-op, item1/3 unproven, now trial-data empty) — all surfaced by verify-in-the-real-data-flow. [[feedback_verify_agent_generated_doc_claims]]

---
**EPIC DEBT AUDIT + 2 MORE FIXES 2026-07-07 (6-agent verified audit):** operator asked for all pending items/gaps/debt. Found 12 real items + caught 2 auditor errors.
- **FIXED (MR !156 + live checkout):** (1) The MR!155 issue_id fix turned empty→QUOTED (`"IFR-123"`) which STILL didn't join session_judgment — trials would've gathered 0 data AGAIN. Fixed defensively in `scripts/prompt-trial-assign.py`: `args.issue.strip().strip('"').strip("'")` + bail-on-empty (skip persisting junk). (2) `test-1665` on the live checkout was the pre-reposition version (tested removed 6-arg `_assign_bands`, 5/5 FAIL polluting live QA) — synced to main's flag-emission version AND made hermetic (`guard_off` forces `SILENT_COGNITION_GUARD=0` because the live sentinel is now active; main's version had the same latent bug, hidden because CI's container has no sentinel). Cleared trial 6/7 pollution rows → clean slate.
- **AUDITOR ERRORS (verified false):** REQ-008 DOES exist (main requirements.md:10) — auditor said it didn't; "item-2 only on feature branch" — false, it's on origin/main.
- **REMAINING DEBT (verified open):** ★ **#1 the trials are STATISTICALLY UNDERPOWERED** — evidence_based/safety_compliance are ceiling-pinned (84%/75% score 5.0); at 15 samples/arm + min_lift 0.05 + p<0.1, a real 0.05 lift → p≈0.45 (undetectable). So the ~1-week verdict will most likely be "inconclusive/no promotion" regardless of merit — the whole trial design can't show improvement on a saturated metric. #2 live guard fires with NO counter (Prepare Result JS, no DB write) → activated-but-dead invisible, blocks the ActiveButSilent alert. #3 no starved-trial dead-man (the blind spot that hid the 2.5-month dark pipeline). #4 Prepare Result suppression JS has zero test coverage (rewire → silent break, CI green). #5 **live host on the closed-MR fork feature/workspace-paper-takeaways, diverged from origin/main**, at-risk uncommitted work (test-636 + ~25 renovate files) — needs reconciliation. #6 **committed workflows/claude-gateway-runner.json is STALE — re-importing would STRIP the claude-provider-env.sh provider switch + the MemoryMax=12G/MemoryHigh=11G cap on Launch Claude nodes** (live has them, repo doesn't) — NEVER re-import until re-exported. #7 docs claim "5 active trials/patches" (CLAUDE.md:353/README:252/benchmark-catalog:116) = false. #8 prom:workspace_guardrail critical:false + no alert. #9 no integration test catches the issue_id round-trip (test-645 hand-seeds matching ids). #10 REQ-008 has no covering BDD scenario.
- **Reusable:** (a) always verify sub-agent audit claims — 2/13 were wrong here. (b) n8n `="{{ expr }}"` in an SSH node can wrap the value in literal quotes; normalize at the consumer. (c) tests that read `_envflag`/sentinels must force the env value (hermetic) or they break once the sentinel goes live. [[feedback_verify_agent_generated_doc_claims]]

---
**DEBT LEDGER "FIX EM ALL" 2026-07-07 (waves 1-3, MRs !157/!158 + earlier !156):** 8 of 10 fixed + 3 bonus bugs.
- **#1 trial power (live):** aborted ceiling-saturated trials 6/7, re-registered as **8** (reasoning_transparency→investigation_quality) + **9** (reflection_checkpoint→overall_score) — HEADROOM dims (38%/39%@5.0 vs 90%/86%), min_lift 0.2, min_samples 50. Now a result is possible.
- **#3 trial dead-man metrics** (write-trial-metrics.sh: arm_samples/assignments/malformed_issue_ids/newest_age) + **bonus: fixed its stale default DB path** (cubeos→gateway-state).
- **#2 item-2 firing counter** (write-workspace-guardrail-metrics.py: workspace_guardrail_suppressions_total + _autoresolve_replies_total from session_transcripts — no n8n change; base rate 1914).
- **#4** committed Prepare Result suppression JS regression test (extracts live JS→node harness, 4/4). **#9** issue_id round-trip test (3/3). **bonus: test-1665 made hermetic** (forces env=0 vs live sentinel; main's copy had the latent bug).
- **#6** re-synced repo Runner export Launch nodes (restored claude-provider-env.sh + MemoryMax=12G — a re-import would've stripped them). **#7** corrected false "5 active trials" docs. **#8** prom:workspace_guardrail critical:true.
- **DEFERRED/scoped (not risky-forced):** #5 git reconcile — local main fast-forwarded (was 66 behind, safe); moving the live checkout off feature/workspace-paper-takeaways NOT done (102 uncommitted paths = operator's ACTIVE renovate-mission WIP; theirs to time). Alerts for #3/#8 = IaC follow-up (agentic-health-alerts.tf infra repo + promtool, not locally validatable; metrics/data are in place). #10 REQ-008 BDD scenario = minor (behavior already covered by test-1665 + test-1665-prepare-result), needs new step defs.

---
**"WHY STILL OPEN" → CLOSED 2026-07-07 (the 3 residuals):** #5 git-reconcile — non-destructively backed up the FULL live checkout (tracked+untracked, incl. operator renovate WIP) to `origin/backup/live-checkout-wip-20260707` (working tree UNTOUCHED) → the "unrecoverable work" risk is gone; only the deployment branch-move remains (operator's 1-command, now zero-risk). **#10** REQ-008 executable scenario (MR !159, BDD 18/18). **#3/#8 alerts** PromptTrialMalformedIssueIds + WorkspaceGuardrailWriterStale — in-repo YAML (MR !159, agentic-health group) + **infra Atlantis MR !374** (agentic-health-alerts.tf). GOTCHA: agentic-health.yml has 2 groups (agentic-health, scheduled-reboot) — a naive append lands in the wrong group; also the infra repo was on the operator's `agora-dashboard` WIP branch (I branched off it, repeated the MR!152 mistake) → restored their checkout, deleted the polluted branch, redid via a worktree off origin/main. promtool .test.yml fixtures deferred (need in-cluster promtool). Whole epic debt ledger now closed except the operator-owned deployment branch-move.

---
**FINAL DEPLOY CLOSURE 2026-07-07 (Atlantis !374 + branch-reconcile truth):**
- **Infra MR !374 APPLIED + MERGED + VERIFIED LIVE.** Posted `atlantis apply` (plan was clean: "0 to add, 1 to change, 0 to destroy", both my alerts present, no destroys), background poller confirmed "Ran Apply" → merged via API (state=merged). Verified end-to-end, not assumed: PrometheusRule CRD carries both alert names (grep count 2) AND they're loaded in the live Prometheus `/api/v1/rules` (`PromptTrialMalformedIssueIds` + `WorkspaceGuardrailWriterStale` both present). So #3/#8 alerting is genuinely deployed + watching, not just merged.
- **Branch-reconcile — the FUNDAMENTAL truth (corrected my own "would wipe" overstatement to operator):** (a) `git checkout main` does NOT delete untracked files, so the renovate WIP (mostly untracked) would survive a switch anyway — my "wipe" framing was wrong. (b) The real finding, measured: the live working tree differs from origin/main by **205 files** (~2263+/3752-), and **105 of the 106 uncommitted paths overlap files main changed** (renovate work is already committed+merged on main; the checkout is 57 commits behind so it shows them "uncommitted" vs its stale HEAD). (c) **The deep truth: you cannot have the checkout on pristine `main` AND keep the renovate WIP running simultaneously — because the running WIP IS the divergence from main.** Any move that makes the tree == main reverts those files (breaks the running renovate mission); any move that keeps them running leaves the checkout diverged. No git trick resolves this. A `git merge origin/main` would conflict on all 105 overlapping files. **Resolution: the checkout converges to main NATURALLY when the operator's renovate MRs land** (then WIP==main, divergence vanishes) — don't fight the branch. Zero-loss is already guaranteed by the backup `origin/backup/live-checkout-wip-20260707`. The only safe intermediate I offered: commit the working tree to `feature/renovate-mr-autonomy` (files stay byte-identical on disk, mission keeps running, "at-risk uncommitted" concern gone) — awaiting operator's yes/no. **Reusable:** when asked to "move the live checkout to main," first measure working-tree-vs-main overlap; if the divergence IS the operator's active WIP, the reconcile is a mission-completion event, not a git command. [[feedback_dont_disturb_foreign_repo_working_tree]] [[renovate_autonomous_mission_20260707]]

---
**"OTHER PENDING?" RE-ASK → TRIAL 9 WAS DEAD ON ARRIVAL (2026-07-07, commit 555231e):** operator asked AGAIN for remaining epic debt; direct live-DB tracing (not the agent audit) caught a critical bug the whole prior ledger missed. **Trial 9 (reflection_checkpoint → dimension `overall_score`, the item-1666 headroom trial re-registered this session) received ZERO assignments and would have finalized "inconclusive" on 2026-07-21** — i.e. half the eval data the operator explicitly cares about was silently not being gathered. **Root cause: TWO dimension whitelists that must match but drifted.** The finalizer's `_JUDGMENT_DIM_COLS` (scripts/lib/prompt_patch_trial.py:346) DOES include `overall_score`, but the assign-side `DIMENSIONS` tuple (scripts/prompt-trial-assign.py:46) did NOT — so `active_trial_for('build-prompt','overall_score')` was never called → trial 9 never bucketed a session. A dim in the finalizer-whitelist-but-not-the-assign-list = dead trial (0 samples); a dim in assign-but-not-finalizer = ValueError at finalize. **Fix:** add `overall_score` to DIMENSIONS. **Verified** against a temp DB COPY (cp gateway.db → mktemp; ran prompt-trial-assign.py --issue SYNTH → trial 9 now buckets; removed temp — zero live pollution). **Guarded:** new `scripts/qa/suites/test-1666-trial-dimension-sync.sh` (4/4) asserts the two sets are IDENTICAL (both drift directions fatal). Headroom confirmed overall_score is the BEST A/B target: only **6.9%** of 432 judgments at ceiling (vs investigation_quality 41.7%, evidence_based 72.5%, safety_compliance 66.2%, completeness 56.9%, actionability 5.1%). **Also VERIFIED (not bugs):** (a) `~/gateway-state/gateway.db` is a SYMLINK (created Jun 16) → `~/gitlab/products/cubeos/claude-context/gateway.db` — SAME inode 138018; the memory note calling cubeos "stale snapshot only" is OUTDATED, the trial pipeline (assign/judge/finalize all default to either path) is coherent. (b) Trial intake healthy: assign fires at session START (Query Knowledge node) so assignments LEAD session_log (written at END) — the "0 sessions but 1 assignment since 14:51" is that lag, not a gap; ~14-38 dispatched sessions/day → 50/arm/trial well before 2026-07-21. **Also found (LOW):** the live checkout's `prometheus/alert-rules/agentic-health.yml` is 34 lines behind origin/main + missing the 2 new alerts (branch feature/workspace-paper-takeaways is 57 behind main) — but that file is the TEST/DOC copy; deployed truth (infra agentic-health-alerts.tf) is live+correct, so low-sev, a symptom of the branch-staleness. **Reusable lesson:** any feature with two parallel allow-lists (producer + consumer, assign + score, emit + alert) needs a set-equality guard test — drift is invisible and one-directional-fatal. This is the SAME failure shape as the 2.5-month-dark trial pipeline (a silent 0-sample condition). A read-only 5-surface adversarial sweep (workflow) was launched for the remaining epic surfaces (other-guardrail wiring / test-integrity / doc-accuracy / alert-coverage / YT+spec hygiene); consolidation + main-MR pending its result. [[feedback_verify_agent_generated_doc_claims]] [[feedback_count_incidents_not_events]]

**SWEEP OUTCOME + FINAL LEDGER (2026-07-07, MR !162 merged 172ce5f):** the 5-agent workflow FAILED usefully — all 5 code-explorer agents investigated (87 tool-uses/391k tokens) but NONE emitted StructuredOutput (schema-forcing is unreliable with agentType:'code-explorer'; journal had only 'started' lines, transcripts had only opening narration — no recoverable findings). **Lesson: don't pair a `schema:` with agentType:'code-explorer' — it silently yields nothing; either omit schema (take prose) or use the default workflow subagent.** Fell back to direct live verification (faster + reliable all session). **MR !162 (off clean main worktree, pipeline green, merged): trial-9 fix (555231e cherry-picked) + README.extensive.md:179 doc fix** ("5 prompt patches"→"8 prompt-patch entries + Welch-gated A/B trials"; was stale on main too — the prior "5 active" correction missed this one line; CLAUDE.md/README.md/benchmark-catalog WERE corrected on main, just branch-stale here). **VERIFIED-HEALTHY (no action):** all 4 guardrails wired end-to-end (item1/3 inject via live Build Prompt PROMPT_PATCHES filter active+expires; build-prompt-layers.py DEAD/only self-refs; item4 canary caveat live unconditional =1; item2 Prepare Result has SUPPRESSED-rewrite + consumes silent_cognition_guard flag + reads Classify Risk stdout); epic suites glob-discovered by run-qa-suite.sh (line 100 `for f in suites/*.sh`) so all run in CI; 1664(3/3)/1665(4/4)/1666(4/4)/prepare-result(4/4)/645(16/16) PASS; **spec lockstep PASSES on origin/main** (the working-tree FAIL on classify-session-risk.py+write-governance-metrics.py is a stale-manifest branch artifact — main ran --update-manifest via !159). **BRANCH-STALENESS class (all resolve when operator reconciles feature/workspace-paper-takeaways→main; deployed/main truth is correct):** agentic-health.yml alerts, CLAUDE.md/README.md/benchmark-catalog "5 active", spec-lockstep manifest. **OPERATOR-JUDGMENT (flagged, NOT done autonomously):** YT epic 1663 + all 4 children (1664/1665/1666/1667) are OPEN — defensible (item1/1664+item3/1666 staged active:false awaiting go-live flip) but **item4/1667 (canary caveat, fully live, ungated) is a genuine closure candidate**, and none carry a deployment-status comment; deployment branch-move (operator's 1-cmd); promtool .test.yml fixtures (in-cluster). **MINOR/NOTED:** config/prompt-patches.json lists `actionability` TWICE (possible dedup, maybe intentional — dual instructions); patch entries have no `id` field (injector filters on dimension/active/expires, doesn't need it). **Reusable: any feature with two parallel allow-lists needs a set-equality guard.** [[feedback_verify_agent_generated_doc_claims]]

**PATCH GC + FINAL GIT CLOSURE (2026-07-07, MR !164 merged 8b4423b):** operator "wtf with the actionability dedup" → investigated: NOT a bug. config/prompt-patches.json had 8 entries, only 1 injects (`actionability` #5 active+unexpired); the "duplicate" #2 was byte-identical but active:False + expired 2026-05-11. Root: `apply-prompt-promotion.py` line 74 sets old patch `active=False` on re-promotion but NEVER deletes → 5 dead 2026-04 patches accumulated. Pruned `active:False AND expired` → 8→3 (kept actionability-live + reasoning_transparency/reflection_checkpoint staged); a prune_patches.py ASSERTS the injectable-set is unchanged (`[actionability]`) so it's provably behavior-neutral; verified the guardrail writer still parses + reports both epic patches staged=0. **No prune cron exists — dead patches WILL re-accumulate (optional future GC job).** MR !164 (behavior-neutral) merged; live checkout mirror committed bf4553d. **FINAL git state (operator asked "everything merged/committed/pushed?"): YES.** All canonical epic work on origin/main (trial-9 acd7230/!162, doc f52b028/!162, prune d2deff5/!164, plus earlier REQ-008/alerts !159 + infra alerts !374). Live checkout `feature/workspace-paper-takeaways` had 8 unpushed local commits — ALL mine/epic (content already on main = zero loss risk) — PUSHED to origin/feature/workspace-paper-takeaways (now 0-ahead). Working tree still dirty with the operator's 107-path renovate WIP (uncommitted, theirs, untouched; push carried only committed epic commits). **Remaining epic pending = operator-only:** go-live flips (item1/1664 reasoning_transparency + item3/1666 reflection_checkpoint active:false; item2/1665 sentinel ~/gateway.silent_cognition_guard) · close YT-1667 (fully live, ungated) + epic status comment · deployment branch-move · promtool .test.yml fixtures (in-cluster) · trial verdict ~2026-07-21 (cloud check scheduled). Confidence HIGH — every claim verified against live state (git/DB/n8n export), not asserted.
