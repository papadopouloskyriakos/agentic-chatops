---
name: orchestrator_control_plane_20260626
description: ACTIVE build of the agentic orchestrator/control-plane epic IFRNLLEI01PRD-1421 (3 bricks). Operator overrode the A-on-all gate ("begin the orchestrator, don't stop until I'm back"). Brick 1 (component registry) DONE+deploying; Bricks 2/3 next.
metadata:
  node_type: memory
  type: project
  originSessionId: 446fe240-f009-4fd5-a87c-b8ecb446a101
---

**ACTIVE: building the orchestrator control-plane (IFRNLLEI01PRD-1421).** Operator OVERRODE the A-on-all gate 2026-06-26 ("begin the autonomous work for the orchestrator and do NOT stop until I am back from afk") — the 2 held benchmark items (#3 guardrails, #4 HITL) stay held; the orchestrator proceeds in parallel.

**Research-backed plan** (docs/orchestration-governance-research-2026-06-25.md): **compose Healthchecks.io + Langfuse, BUILD the 3 thin bricks on the existing Prometheus+SQLite substrate; do NOT adopt a platform** (skip LangGraph/CrewAI/Temporal/Airflow/Dagster/Backstage — all rewrites). The 3 bricks:
- **Brick 1 — Component Registry** (ensure proper usage): declared manifest {name,type,trigger,cadence,liveness,writes,kill_switch} + mechanical liveness check. The dark-fix alerts (HolisticHealthStale etc.) were the first retrofit.
- **Brick 2 — Interaction Graph** (detect overlap/gaps): SQLite introspection — declared-vs-observed reads/writes/locks/cron-slots/metrics/hooks → conflicts (2 writers of a table), redundancy (2 readers), GAPS (consumer with no producer = the Session-End→reconcile hole). Copy Dagster's asset-graph model; layer Langfuse for the LLM/agent edges.
- **Brick 3 — Orchestration-level benchmark + chaos**: replay a stream of incidents → did the right components fire in the right order, no conflict/double-work/gap, correct end-state?

**BRICK 1 — DONE (gateway side committed) + DEPLOYING.** Files (branch **feat/orchestrator-control-plane**, gateway repo, pushed):
- `scripts/registry-seed.py` — auto-discovers 233 components (97 cron, 56 n8n, 53 prom-writer, 27 db-table) from crontab + textfile-collector + n8n API + DB tables → `config/component-registry.json`. Hand metadata preserved on re-seed (`liveness` always refreshes; tune via `liveness_override`).
- `scripts/registry-curate.py` — re-runnable baseline classification: **9 critical** (prom:holistic_health/self_audit/handoff_depth/registry_check/gateway_watchdog/session_metrics/agent_metrics + n8n Runner+Poller — must be fresh, all VERIFIED fresh = 0 false-fire), **35 known_dark** (inactive n8n + retired tables a2a_task_log/credential_usage_log/execution_log/features/work_units + chaos event-driven).
- `scripts/registry-check.py` — liveness verifier; emits registry_{components,dark,critical_dark}_total + registry_check_last_run_timestamp_seconds (who-watches-the-watcher). Exit 1 on critical-dark. Prom default 90000s (25h) catches the dark-component CLASS without daily false-positives.
- `prometheus/alert-rules/agentic-health.yml` — RegistryCriticalDark (tier1) + RegistryCheckStale (absent()-guarded). Also SYNCED 6 prior territory/dark-fix alerts that had drifted off origin/main's doc copy.
- Crons: registry-check `*/30`, registry-seed+curate daily `20 6`.
- **Deploy: infra MR !347** (infrastructure/nl/production, branch feat/orchestrator-registry-alerts) — `tofu fmt` passed pre-commit; **atlantis plan = 0 add / 1 change / 0 destroy**; atlantis apply POSTED. AFTER apply: merge + verify the PrometheusRule in-cluster (kubectl get prometheusrule), then start Brick 2.
- Live state: 233 components, 9 critical all fresh, 2 genuinely-stale informational (chaos_exercise event-driven, kb_rag_eval 3.9d).

**BRICK 1 = COMPLETE + LIVE-VERIFIED 2026-06-26.** MR !347 apply = "Apply complete! 0 added, 1 changed, 0 destroyed", MERGED, and verified in-cluster: `prometheusrule/agentic-health-alert-rules` contains both RegistryCriticalDark + RegistryCheckStale. Gateway branch feat/orchestrator-control-plane committed+pushed. The dark-component failure class is now mechanically detected across all 233 components (was a manual quarterly audit). Brick-1 follow-ups (incremental): adopt self-hosted Healthchecks.io for ping-based "never ran" detection (the one absent() can still miss); enrich the manifest with per-component reads/writes (feeds Brick 2).

**BRICK 2 = DONE 2026-06-26 (committed feat/orchestrator-control-plane).** `scripts/interaction-graph.py` static-analyzes all 238 repo scripts (follows sub-script writes + dynamic `INSERT INTO {TRACE_TABLE}` bindings) → `config/interaction-graph.json` + metrics, cron daily 06:30. Surfaces: **22 CONFLICTs** (tables written by ≥2 scripts — mostly intended fan-in like llm_usage's 12 writers; a genuine race would now be visible), **0 GAPs** (clean — InteractionGraphGap alert fires if an orphan-consumer appears = the reconcile-hole class), **6 CRON-CLASHes** (0 4 has 5 jobs). Copies Dagster's asset-graph model in ~250 lines (no daemon).

**BRICK 3 = DONE 2026-06-26 (committed).** `scripts/orchestration-benchmark.py` replays a 10-incident synthetic stream through the isolated spine (classify→predict, throwaway DB+HOME) scoring 4 ORCHESTRATION INVARIANTS: **I1 safety-composition** (irreversible NEVER band=AUTO — all 5 of mkfs/zpool-destroy/dropdb/rm-rf/tf-destroy correctly POLL_PAUSE), **I2 determinism**, **I3 completeness** (valid band + prediction artifact), **I4 structural-integrity** (interaction-graph gaps==0). **First run: score 1.0, invariants 4/4.** Hard-gates I1+I4. config/orchestration-scorecard.json + metrics, cron weekly Mon 06:45. Alerts OrchestrationSafetyFailure (tier1) + OrchestrationBenchmarkStale.

**ALL 3 BRICKS BUILT + FULLY DEPLOYED + LIVE-VERIFIED 2026-06-26.** Brick 1 alerts = infra MR !347 MERGED. Brick 2+3 alerts = infra MR !348 MERGED (apply: 0 add/1 change/0 destroy). **VERIFIED: all 5 alerts LIVE in the in-cluster prometheusrule/agentic-health-alert-rules** (RegistryCriticalDark, RegistryCheckStale, InteractionGraphGap, OrchestrationSafetyFailure, OrchestrationBenchmarkStale). Gateway MR **!57** (feat/orchestrator-control-plane, 3 brick commits) created, ready to merge. The orchestrator epic's CORE is COMPLETE — the federation now has a governing layer (inventory + liveness + interaction map + orchestration invariants), all cronned + alerting.

**POST-BRICK HARDENING (all done 2026-06-26, committed to feat/orchestrator-control-plane / gateway MR !57):**
- **Self-monitoring**: registry-curate now marks prom:interaction_graph + prom:orchestration_benchmark critical (8d liveness_override for the weekly benchmark) — the control-plane watches its OWN 3 bricks (11 critical, 0 dark). Who-watches-the-watcher fully closed.
- **First governance report**: docs/orchestration-findings-2026-06-26.md — 233 components, 0 gaps, 6 cron-clashes proven BENIGN (cross-ref cron-slots × write-map = disjoint tables, no concurrent-write race), 22 conflicts classified (7 append-only fan-in safe / 15 update-in-place coordination-review, low actual risk), orchestration 1.0. + operator backlog.
- **QA regression guard**: scripts/qa/suites/test-1421-orchestrator.sh (6 tests, all pass) — the load-bearing one asserts the orchestration safety invariant (irreversible NEVER auto-resolved).
- Big QA suite running as final session hygiene gate (background watcher bzrkimuwe).

**BRICK-GROWTH DONE 2026-06-26 (operator-approved "deploy both"):** Healthchecks.io + Langfuse v2 both deployed (Docker on nlopenobserve01, LXC resized 10→30G) + INTEGRATED + PROVEN — see [[healthchecks_langfuse_access_20260626]]. Healthchecks: registry-check */30 cron pings it (status `up`; catches never-ran). Langfuse: scripts/lib/langfuse_export.py wired into reconcile → every session traces (proven with a real $2.17 session). Committed (langfuse lib) to feat/orchestrator-control-plane.

**MERGED + E2E-PROVEN 2026-06-26.** Gateway MRs **!57 (orchestrator) + !58 (benchmark) MERGED to main** (8114b11 + 0bbfb5a; both mergeable/no-conflict/CI-success). **Full e2e VERIFIED with evidence:** (1) bricks emit real metrics (registry_components_total 239, gaps 0, orchestration_benchmark_score 1); (2) **crons fire on schedule** — registry_check.prom @09:30 (*/30 run), interaction_graph.prom @06:30 (daily run), not just manual; (3) node_exporter→**Prometheus scrapes** (orchestration_benchmark_score=1 confirmed IN Prometheus, pod prometheus-monitoring-kube-prometheus-prometheus-0); (4) **all 5 alert rules evaluate state=inactive health=ok**; (5) **GOLD-STANDARD fault-injection drill PROVED detection FIRES** — injected interaction_graph_gaps_total=1 into the .prom → Prometheus scraped it → InteractionGraphGap flipped to state=pending → auto-reverted to 0 (never fired: 1h for-clause + condition cleared). QA test-1421-orchestrator passes; HC check `up`; a real session traced into Langfuse. **The orchestrator e2e is DONE.** Drill method reusable: write a synthetic value to a .prom, poll /api/v1/rules for the alert state via `kubectl exec -n monitoring <prom-pod> -c prometheus -- wget -qO- http://localhost:9090/api/v1/...`, always auto-revert.

**REMAINING (incremental):** merge gateway MRs !57 (orchestrator) + !58 (benchmark); roll out HC pings to more critical crons (holistic-health, gateway-watchdog) + grow the LF trace coverage; grow the Brick 3 scenario catalog + a full-pipeline replay tier; the 22-conflict coordination-review backlog (docs/orchestration-findings). **Benchmark FINALIZED 12/14 A — the 2 B's (#3 guardrails KEEP-OFF, #4 HITL keep-passive) are operator-decided, NOT open.** n8n key in scripts/holistic-agentic-health.sh; GITLAB_TOKEN in .env; tofu at ~/.local/bin/tofu; infra MR via the GitLab API (project infrastructure%2Fnl%2Fproduction). [[autonomous_benchmark_mission_20260625]] [[dark_component_audit_20260625]]