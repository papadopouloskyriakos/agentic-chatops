---
name: autonomous_benchmark_mission_20260625
description: ACTIVE 24h autonomous mission (2026-06-25, operator AFK ~24h) — benchmark the gateway against the Anthropic + OpenAI ebooks (separate attributed scorecards), improve every dimension to ≥A, then build the orchestrator epic. FULL-AUTONOMY mandate granted.
metadata:
  node_type: memory
  type: project
  originSessionId: 446fe240-f009-4fd5-a87c-b8ecb446a101
---

**ACTIVE AUTONOMOUS MISSION — operator AFK ~24h from 2026-06-25 ~23:00. Self-paced loop.**

**MISSION (in order):** (1) Benchmark the claude-gateway agentic system against **IFRNLLEI01PRD-1422** (Anthropic 'Building Effective AI Agents', 10 dims) as its OWN fully-attributed scorecard; (2) then **IFRNLLEI01PRD-1423** (OpenAI 'A Practical Guide to Building Agents') as its OWN attributed scorecard; (3) a PROVENANCE-TAGGED synthesis (each backlog item tagged `[Anthropic·dimX]` / `[OpenAI·Y]` / `[BOTH]` — NEVER mix sources: operator explicitly rejected a combined pass because it loses traceability); (4) **improve the system until EVERY dimension scores ≥A**; (5) once **100% confident A-on-all**, move to the orchestrator epic **IFRNLLEI01PRD-1421**.

**OPERATOR'S 3 ANSWERS (AskUserQuestion 2026-06-25) = FULL AUTONOMY:** (Q1 Deploy posture) **Full autonomy, deploy all** — incl. the live autonomy/safety hot path (Runner Build-Prompt/Code nodes, classifier band/floor logic, infragraph gate) + model changes, Atlantis-applying as I go. (Q2 Orchestrator overlap) **Build the minimal bricks now** — if a dim needs orchestrator bricks (interaction-graph observability, PII/output-validation guardrails, multi-agent coordination tracing) to hit A, build the minimal version now. (Q3 Judgment calls) **All calls autonomous** — incl. model/behavior + safety-bar changes; operator reviews on return.

**OPERATING DISCIPLINE (full autonomy ≠ careless — it's a LIVE prod system handling real infra alerts):** every change reversible behind a kill-switch sentinel where feasible (dark-by-default → enable after verify); run QA suite (`scripts/qa/run-qa-suite.sh`, baseline 759/0/2) + band-aware audit (`audit-risk-decisions.sh`) after each change; n8n code-node-safety sequence (`docs/runbooks/n8n-code-node-safety.md` + `validate-n8n-code-nodes.sh`) for ANY Code-node edit; Atlantis 0-destroy plans ONLY; deploy off FRESH origin/main worktrees (stale branches revert main); commit to a clean branch; document every change in memory + scorecards (docs/) + YT comments. Tripwires: the new HolisticHealthStale + RiskAuditFloorViolation alerts (live in-cluster MR !346). DON'T genuinely endanger the live system to chase an A — keep it safe + recoverable.

**METHODOLOGY (per the operator's correction):** per-book scorecards SEPARATE + ATTRIBUTED (every finding cited to a specific ebook principle/page + live evidence with file refs); adversarial (each score gets a skeptic challenge); A-F grade per dim; THEN provenance-tagged synthesis. Independent agreement across both books = higher-confidence gap; divergence = source-specific lens (OpenAI=tools/guardrails/HITL heavy; Anthropic=context-mgmt/multi-agent heavy). The 10 Anthropic dims + 10 OpenAI dims are in the -1422 / -1423 epic descriptions. Both ebook PDFs at /tmp.

**KNOWN GAPS already surfaced (to confirm/score):** model-selection (252/252 Opus, modelHint computed in Build Prompt runner.json:~2232 but SESSION_MODEL never set → cost lever, dim-2 both books); missing OpenAI guardrail types (PII filter / output validation / per-tool risk ratings); the 36KB monolithic Build Prompt (modular-design dim-3); the infragraph fold-precision ceiling (eval rigor); the orchestrator-absence (observability/coordination dims = the -1421 bricks).

**PROGRESS LOG:**
- 2026-06-25 ~23:xx: First -1422 benchmark workflow (wf_581d117f, 10 concurrent scorers) FAILED — ALL agents hit transient server-side rate-limit ("Server is temporarily limiting requests · NOT your usage limit"). LESSON: workflow fan-out of 10+ concurrent agents trips Anthropic's transient throttle. **WORKAROUND: batch the agents (3 concurrent max), score+verify sequential per item.** Re-launched batched as wf_c542e44b (task wmfvk0ohv). If batched STILL rate-limits → back off via ScheduleWakeup (transient, clears in mins) before retry. **For ALL benchmark/improve workflows this run: keep concurrency LOW (batches of 2-3), not the 16 cap.** Batched script at workflows/scripts/benchmark-1422-anthropic-wf_581d117f-ce7.js (reusable via scriptPath).

- 2026-06-26: **-1422 ANTHROPIC SCORECARD DONE** (batched wf_c542e44b worked, 21 agents, adversarially-verified). Saved docs/scorecard-anthropic-2026-06-26.md (23.5KB) + posted to -1422. **SCORES: 1 A (safety/guardrails) / 7 B (pattern-fit, modular-design, observability, context-mgmt, eval-rigor, resource-economics, future-readiness) / 1 C (multi-agent-coordination) / 1 D (model-selection).** 9 dims to lift to A. **#1 fix VERIFIED FIRSTHAND (model-selection D, low-effort/high-value):** Build Prompt bug `var priorIncidents = (kbRaw.match(/\|/g)||[]).length` counts every PIPE in the markdown-table KB output → `priorIncidents<=2` sonnet-gate NEVER fires → session_log 0 sonnet/0 haiku across 738 Opus orchestrator sessions. SESSION_MODEL wiring EXISTS (4 refs in runner.json, modelHint→SESSION_MODEL→--model) but PUT-live-uncommitted + `Launch Claude Fresh` (resume) never sets it. FIX (ready to execute post-synthesis): count incident ROWS not pipes, widen predicate (availability/cert/maintenance + conf>0.8 + no MIXED/HIGH band → sonnet), wire BOTH launch paths, never-downgrade-risky floor, commit. Other concrete gaps: handoff-depth gate dark (0/75), no token ceiling ($361/day spike 14× over $25 gate), Runner Opus MemoryMax=infinity, eval-flywheel dead cron, RAGAS -1 sentinels, otel OTLP near-dead (15/39K), team_charter 0 events. Many flagged orchestrator_dependent=True but most are CONCRETE fixes NOT needing -1421 (treat that flag skeptically). The Build Prompt fix = LIVE n8n Code-node edit (code-node-safety sequence) + re-export.

- 2026-06-26: **BOTH SCORECARDS DONE + SAVED + POSTED.** -1423 OpenAI: 5A/3B/1C/1D (A: when-to-build, tool-design, instruction-quality, orchestration-pattern, iterative-deployment; B: single-vs-multi, guardrails-layered, human-intervention; C: optimistic-execution+tripwires; D: model-selection). Both books D on model-selection. Saved docs/scorecard-openai-2026-06-26.md + docs/scorecard-anthropic-2026-06-26.md + **docs/benchmark-synthesis-2026-06-26.md (the provenance-tagged ranked to-A backlog, 12 items)**. Posted to -1422/-1423. Created clean branch **feat/benchmark-to-A-improvements** off origin/main (committed: 3 scorecard docs + the 2 QA test fixes; pushed, MR-ready).
- 2026-06-26: **IMPROVEMENT #1 (model-routing, the BOTH-books D) DONE + LIVE + VERIFIED.** Fixed the Build Prompt (runner workflow qadF2WcaBsIR7SWG) via the code-node-safety runbook: (a) `priorIncidents` now counts incident ROWS not pipes [was `match(/\|/g)` = every markdown-table pipe → gate never fired]; (b) added never-downgrade-risky FLOOR (riskLevel=high || band POLL_PAUSE/AUTO_NOTICE → force opus). Snapshot saved ~/cron-backups/runner-buildprompt-*.jsCode. node --check OK, validate-n8n-code-nodes VALIDATION PASSED, 6/6 unit tests (sonnet fires simple+low-risk, opus on risky/complex), PUT live, re-validated live, active=True. Next simple/low-risk alert → Sonnet. **RESUME PATH still always-opus** (claude -r uses the session's model so likely moot — verify when re-scoring). Lifts model-selection D→ (re-score to confirm A) + helps Anthropic dim1/9/10.

**REMAINING BACKLOG (docs/benchmark-synthesis-2026-06-26.md, items #2-12):** #2 sub-agent dispatch wiring (delegation block to 0/331 sessions) [A·dim6 C/O·dim5 B]; #3 guardrail layering on --dangerously-skip-permissions path + PII/output-validation/relevance + activate semantic rail [O·dim7 B]; #4 human-intervention page/pause on failure-threshold [O·dim8 B]; #5 optimistic async tripwire (watch+abort) [O·dim9 C]; #6 context-mgmt+token-ceiling, wire handoff-depth gate (dark 0/75) [A·dim5/9]; #7 Runner MemoryMax cap + cost-gate enforce [A·dim9]; #8 OTLP export flush + snapshot wiring [A·dim4]; #9 eval-rigor dead crons (eval-flywheel, RAGAS -1 sentinels, jailbreak relative-path) [A·dim7]; #10 Skill-drift single-source [A·dim3]; #11 agents-as-tools descriptions [O·dim3]; #12 un-pin dated haiku model + cron provenance-guard [A·dim10]. After all → RE-BENCHMARK to confirm A-on-all → orchestrator -1421. Each: kill-switch where feasible, QA+audit after, commit to feat/benchmark-to-A-improvements (cp clean files into the worktree at $WT). n8n-key is in scripts/holistic-agentic-health.sh (NOT gateway-watchdog.sh).

- 2026-06-26: **IMPROVEMENT #9 (eval-rigor) + #8 (observability OTLP) DONE.** #9: jailbreak weekly cron had a relative-path bug (`bash scripts/qa/...` from $HOME → No such file ×3 in log) → fixed (added `cd <repo> &&`); eval-flywheel.sh VERIFIED WORKING (auditor's "missing" was wrong); RAGAS FRESH (138 rows, run-golden cron from dark-fix worked); jailbreak metrics repopulated. #8: **OTLP trace export to OpenObserve was DEAD** (15/39K shipped). Root cause = a STALE hardcoded `OTLP_AUTH` in .env shadowing OPENOBSERVE_USER/TOKEN → HTTP 401 since ~March → spans aged past OpenObserve's ~5h ingest window (ZO_INGEST_ALLOWED_UPTO) → 206. FIXED: commented stale OTLP_AUTH, .env now uses operator-provided admin@example.com creds; added retention pre-filter to export-otel-traces.py (spans >4h → state 2 expired, not retried forever). **PROVEN e2e:** synthetic current-ts span exports state 0→1, lands in OpenObserve HTTP 200, pending=0. Committed to feat/benchmark-to-A-improvements (export-otel-traces.py). OpenObserve access saved → [[openobserve_access_20260626]]. (Operator interjected the admin creds + caught my "no collector" rationalization — [[feedback_verify_belief_not_rationalize_observation]].) **LESSON: several auditor findings overstated gaps — trace-metrics cron EXISTS, otel local spans queryable, snapshot wired-awaiting-traffic, eval-flywheel works. VERIFY each before fixing.**

- 2026-06-26: **investigate-backlog workflow (wf_5934969c) verified all 9 remaining items** (saved details in task output). Confirmed: #10 Skill-drift is a FALSE gap (inline copy IS in parity — "691B drift" was wrong); **#3 guardrails must HOLD for operator** (re-wiring unified-guard.sh to the dispatched PreToolUse would reverse the operator's DELIBERATE disable in commit 1c88ba6 on this branch — surface, don't override). Others real.
- 2026-06-26: **DONE #2A, #11, #12, #10 (low-risk, committed feat/benchmark-to-A-improvements).** #2A: symlinked `~/.claude/agents → gateway/.claude/agents` (host-level) → all 11 Haiku sub-agents now DISCOVERABLE by every dispatched session (root cause of 0/331: dispatched sessions cd into cubeos/infra slot repos which have no .claude/agents, and ~/.claude/agents didn't exist). #11: agent_as_tool.py frontmatter parser was flat → `description: >` block scalars collapsed to bare agent name; now block-scalar-aware → full descriptions. #12: scripts/lib/models.py registry + scripts/check-model-registry.py drift-guard (CAUGHT+FIXED a real straggler: llm-judge.sh:152 opus-4-6→opus-4-8). #10: scripts/check-skill-parity.py (parity confirmed 11640 chars). 3 weekly parity-guard crons added (skill-parity, model-registry, the never-cronned check-model-provenance-drift).

- 2026-06-26: **DONE #6.1 (handoff telemetry) + #7 (MemoryMax).** #6.1: PreToolUse:Task hook scripts/hooks/handoff-bump.sh records each delegation via lib.handoff_depth (test-fired depth=1); wired in config/dispatched-session-settings.json; FOUND+FIXED a 2nd dark-path bug — write-handoff-metrics.sh defaulted OUT_DIR to non-existent /var/lib/prometheus/node-exporter (canonical = /var/lib/node_exporter/textfile_collector) so handoff_depth.prom never produced; fixed + cronned (*/5, with cd). #7: added `-p MemoryMax=12G -p MemoryHigh=11G` to BOTH Launch Claude + Launch Claude Fresh systemd-run --user --scope commands (Runner qadF2WcaBsIR7SWG) — VERIFIED MemoryMax enforceable here (scope got 3221225472; user.slice has memory delegated) + command well-formed + PUT + active. A runaway claude -p is now cgroup-OOM-killed at 12G (vs the 26G that wedged pve04). NOTE: confirmed BOTH Launch nodes already set SESSION_MODEL (#1 resume-path concern was moot). Snapshot ~/cron-backups/runner-launch-*.json.

- 2026-06-26: **DONE #5 (optimistic tripwire).** scripts/session-tripwire.sh (called from Progress Poller uRRkYbRfWuPXrv3b Poll Progress node) parses cumulative tokens/tool-calls/cost from the live JSONL + ABORTS (kill -TERM) a runaway on breach of generous bounds (3M tok / 250 tool-calls / $20). Kill-switch `~/gateway.tripwire_off` = observe-only. Tested 3 cases, integrated+PUT+active. Closes OpenAI dim9 + the token/cost ceiling for dim5/dim9.

- 2026-06-26: **RE-BENCHMARK (wf_21aa9fb0, wiring-aware, 14 dims) = A=9 / B=5 / below-B=0.** 9 confirmed A. The 5 B: #3 guardrails + #4 HITL (correctly HELD), + 3 FIXABLE which I then closed: (a) **model-selection B→ durability** — the n8n workflow fixes (model-routing/MemoryMax/tripwire) were LIVE-ONLY, never exported → repo→n8n deploy would REVERT them. FIXED: exported live Runner+Poller to repo + committed (model-routing fix/MemoryMax/tripwire now version-controlled on the MR branch). (b) **eval-rigor B→ RAGAS bug** — local Ollama judge (format:json) returns array wrapped in an object → 3/4 metrics silently -1 every row. FIXED: _unwrap_list() at 4 parse sites (6/6 unit test). (c) **multi-agent B→ comment** — handoff-bump.sh claimed handoff_log but records sessions.handoff_depth+event_log; corrected the comment (telemetry real). **NET: 12 of 14 at A; 2 HELD at B (guardrails #3, HITL #4) = operator decisions.** Branch feat/benchmark-to-A-improvements = 9 commits ahead of main, ready to merge (the durability export makes the n8n fixes durable on merge+deploy).

**STATUS:** **10 DONE** (#1 model-routing, #9 eval-rigor, #8 observability-OTLP, #2A sub-agent-discovery, #11 agent-desc, #12 model-registry, #10 skill-parity, #6.1 handoff-telemetry, #7 MemoryMax, #5 tripwire) + 2 bonus (llm-judge straggler, write-handoff-metrics path). **3 HELD for operator** (disciplined — judgment/risk, not auto-changeable unattended): **#3 guardrails** (re-wire unified-guard reverses operator's deliberate disable 1c88ba6; PII redactor already exists post-hoc), **#4 HITL** (failure-threshold SMS-page+hard-pause needs a downstream HTTP/node-graph change; passive Matrix warning ALREADY exists at Parse Response L96/99 so it's not silent), **#2B team_charter telemetry** (incremental — core dim6 fixed via #2A+#6.1; the Build-Prompt execFileSync is n8n-sandbox-blocked dead-code; emit via session_events.TeamCharterEvent from Launch node). **GATE: NOT at A-on-all (3 held) → do NOT proceed to orchestrator -1421; present held items + await operator.** Re-benchmark in flight (wiring-aware: many fixes show DATA only on the next dispatched session — score the MECHANISM). Worktree $WT has 6 commits pushed (feat/benchmark-to-A-improvements). REMAINING (all LIVE-WORKFLOW-NODE edits, careful — snapshot→validate→PUT→test-fire):
- **#2B** team_formation: the Build Prompt's `execFileSync('python3',['-m','lib.team_formation'])` is n8n-sandbox-blocked (child_process) → teamCharterSection always '' → 0 team_charter rows. FIX: move the call to the Launch SSH node + have lib.team_formation.py write the event_log row. [coordination, completes #2]
- **#7** Runner mem/cost: dispatched `claude -p` runs via `systemd-run --user --scope` with NO MemoryMax. FIX: add `-p MemoryMax=<N>G` to Launch Claude + Launch Claude Fresh SSH commands (check LXC mem first; generous value so legit sessions aren't OOM-killed). Per-session cost ceiling in poller. [resource-econ dim9]
- **#6** context/token: handoff_depth.py DARK (0/75, lib exists, never called). FIX: PreToolUse:Task hook → `python3 -m lib.handoff_depth <issue> --bump-to <agent>` + write-handoff-metrics.sh cron; token ceiling via generous `--max-turns` on Launch (NOT 15 — first auto-resolve was 26 turns; use ~80). [dim5]
- **#5** tripwire: Progress Poller "Poll Progress" SSH node tails JSONL + reads $PID but has no kill authority. FIX: parse cumulative tokens/tool_use, on breach `kill -TERM $PID` + [TRIPWIRE-ABORT] marker + m.notice. (Poller workflow, NOT Runner — lower risk.) [OpenAI dim9 C]
- **#4** HITL: Parse Response Code node computes costExceeded/toolCallLimitExceeded but only annotates. FIX: POST /alert-session (page) + pause; extend bridge `_session_reason_is_critical()` to accept reason.startswith('failure-threshold'). [OpenAI dim8] [hot-path Code node — code-node-safety]
- **#3 HOLD** guardrails — unified-guard.sh exists but unwired to dispatched path; re-wiring reverses operator's deliberate disable (1c88ba6). Surface for operator, don't auto-enable. (PII redactor already exists post-hoc in Prepare Result; intermediate rail observe-only.)
Then RE-BENCHMARK to confirm A-on-all → orchestrator -1421. n8n key in scripts/holistic-agentic-health.sh. Runner=qadF2WcaBsIR7SWG. Worktree=$WT (gw-bench-wt). Next: -1423 scorecard→docs+YT → provenance-tagged synthesis → improvements (start with the verified model-selection fix). Loop: workflow re-invokes me on completion. Prior context: the dark-component fix batch is COMPLETE+DEPLOYED (see [[dark_component_audit_20260625]]); 3 epics created; research says compose Healthchecks.io+Langfuse+build-3-thin-bricks for -1421 ([[docs/orchestration-governance-research]]). Loop continuation = workflow-completion re-invocations + ScheduleWakeup fallback; never stall — always have a pending action until A-on-all then -1421.

**OPERATOR DECISIONS 2026-06-26 (benchmark FINALIZED at 12/14 A):** #3 guardrails = KEEP OFF (operator's deliberate unified-guard disable stands; dimension stays B BY CHOICE, not a gap). #4 HITL = KEEP passive Matrix warning (no escalation node-graph change; dimension stays B by choice). So the 2 remaining B's are operator-decided, NOT open work. Benchmark = DONE. Gateway MR !58 (benchmark) + !57 (orchestrator) open for review. Orchestrator follow-ups: Healthchecks.io + Langfuse = APPROVED, deploying.