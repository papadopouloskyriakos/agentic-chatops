# *

**Site:** NL (Leiden)

## Knowledge Base References

**nl:CLAUDE.md**
- - **GitLab instance**: `https://gitlab.example.net/` (NL site — primary GitLab instance)
- - **Repo path**: `infrastructure/nl/production`
- - **Note**: The GR site has its own GitLab instance at `https://gr-gitlab.example.net/` — these are separate instances, not mirrors.
- - **Commit directly to `main`** for docker/, network/, pve/ changes — pipelines auto-deploy on push.
- - **Create MRs** for k8s/ changes — Atlantis handles `plan`/`apply` via MR comments.

**nl:k8s/CLAUDE.md**
- - **Cluster**: `nlcl01k8s` (ID: 1), K8s v1.34.2, API at `api-k8s.example.net:6443`
- - **Nodes**: 3 control-plane (4 CPU, 8GB — ctrl02 4GB on pve02, ctrl01+ctrl03 upgraded 4→8GB on 2026-03-15) + 4 workers (8 CPU, 8GB), all Ubuntu 24.04, IPs 10.0.X.X-12 (CP), .20-23 (workers)
- - **CNI**: Cilium v1.18.4, eBPF, kubeProxyReplacement, VXLAN tunneling, MTU 1350
- - **Pod CIDR**: 10.0.0.0/16 (NL), 10.1.0.0/16 (GR) — must not overlap for ClusterMesh
- - **ClusterMesh**: Connected to GR cluster `grcl01k8s` at 10.0.58.X:2379, mTLS via ExternalSecret from OpenBao

**nl:network/CLAUDE.md**
- │   ├── *.py                # Drift detection, syntax validation, post-validation scripts
- Config files have **no file extension** — they are raw Cisco running-config output.
- 1. **`detect_drift.py`** — Scheduled job. Connects to all devices, compares running-config vs git. If drift found, `auto_sync_drift.py` syncs device→git and commits directly to main (device is source of truth).
- 2. **`pre_deploy_drift_gate.py`** — Blocks deployment if device has unreported manual changes. Compares device vs HEAD vs HEAD^ to distinguish TRUE DRIFT (manual SSH additions never in git) from PENDING DELETIONS (lines removed in current commit). Exit 0=clean, exit 2=drift+MR created, exit 1=error.
- 3. **`validate_syntax.py`** — Checks: hostname exists, dangerous commands flagged as warnings (`no ip routing`, global `shutdown`, `default interface`, `write erase`, `reload`), typos flagged as errors (`interace`, `descripion`), tabs and long lines (>500 chars) as warnings.

**nl:ci/CLAUDE.md**
- | Docker | `docker/**/*` changes | Validation only | No | `GitLab CI Auto-Sync` |
- | Cisco | `network/configs/**/*` changes | Never deploys | Drift detection only | `NL Oxidized Bot`, `GitLab CI Auto-Sync` |
- | K8s | `k8s/**/*.tf` changes | Validation only | No | No |
- | Argo CD | `k8s/argocd-apps/**/*` changes | Validation only | No | No |
- | LXC | `pve/**/lxc/*.conf` changes | No | No | `GitLab CI Auto-Sync` |

**nl:edge/CLAUDE.md**
- **AS Number:** AS64512
- **IPv6 prefix:** `2a0c:9a40:8e20::/48` (announced from all three VPS nodes — anycast). Per-domain anycast `/128`s carved out of the prefix: `::1` (papadopoulos.tech, matrix, mattermost), `::2`/`::3` (reserved for future papadopoulos.tech multi-homing), `::4` (mulecube), `::5` (cubeos.app), `::6` (withelli), `::7` (meshsat.net), `::8` (meshsat.org), `::9` (omoikane.coach). Next free: `::a`. When allocating a new slot, add it to all three VPS netplans (reboot to apply — never `netplan apply` on a VPS once eBGP is established; the 241k+ kernel routes from iFog blow the netlink-enumeration timeout — see txhou01vps01 onboarding 2026-05-06), add matching `bind [::N]:80` and `bind [::N]:443` lines in `frontend http_redirect` and `frontend tls_in` on all VPS's haproxy.cfg, then add the Cloudflare AAAA record for the service.
- **Internal BGP AS:** 65000 (iBGP full mesh via route reflectors)
- ├─ nl-dmz01: portfolio, cubeos-*, mulecube-*, umami (NL only), withelli, withelli-beta, meshsat-hub
- ├─ gr-dmz01: portfolio, cubeos-*, mulecube-*, withelli, withelli-beta, meshsat-hub (GR mirror)

**nl:edge/openvpn/CLAUDE.md**
- Read-only snapshots for the 6 OpenVPN-AS instances. Same model as the rest of `edge/`: **live host is the source of truth**, the git copy exists for human audit/diff and **disaster-recovery rebuild**. There is no deploy pipeline.
- The snapshot is sized for a **bare-metal rebuild**. Given a fresh OAS install at the right version on a fresh LXC (re-created from `pve/<host>/lxc/<vmid>.conf`), dropping the snapshot back into place restores the box without operator effort to re-enroll users, re-issue client certs, or reset passwords.
- The trade-off: the SQLite DBs and web-ssl PEMs contain server private keys (OAS internal CA, web UI cert), all client certs, all user password hashes, and all TOTP seeds. These are committed verbatim. **The repo's private-repo posture is the security boundary.** github-sync sanitizes for the public mirror.
- **Not snapshotted** (intentionally): `db/log.db` (ephemeral logs), `etc/.ldat/` (license metadata cache), `etc/sock/`, `etc/tmp/`, `etc/upgrade-flags/`. The `licenses/` dir is empty on all 6 hosts — `sacli SubscriptionStatus` returns `NOT_CONFIGURED` (free 2-connection mode), so nothing to back up.
- 1. **Recreate the LXC** from `pve/<pve-host>/lxc/<vmid>.conf` (committed in this repo). The PVE pipeline handles SCP + `pct create` + start.

**nl:images/CLAUDE.md**
- Build contexts for CI runner images used by GitLab pipelines. These are **build-only** — they get pushed to the private registry, not deployed as running services. Never place these under `docker/` (that directory triggers the deployment pipeline).
- 1. Triggers on changes to `images/<name>/**/*` on main
- 2. Requires **manual click** to build (never auto-builds)
- - Clean up package caches in the same `RUN` layer (`rm -rf /var/lib/apt/lists/*` or `rm -rf /var/cache/apk/*`).

**nl:native/CLAUDE.md**
- Services installed directly on VMs (not containerized with Docker). This directory tracks their configuration files as **read-only snapshots** for reference, drift detection, and disaster recovery.
- Unlike `docker/` (which has CI/CD pipelines for deployment), native configs are **reference copies only**. There is no automated deploy pipeline for native services.
- **To update:** SSH to the VM, make changes, copy updated config back, commit with `chore(native): sync <service> config from <host>`.
- **To restore:** Copy configs from this repo to the VM, restart the relevant service.
- **Note:** `sshpass` does not work with nl-nas02 (DSM 7.1 keyboard-interactive quirk). Use paramiko for password-based automation.

**nl:native/fcksns/CLAUDE.md**
- All 7 speakers are **RPi Zero 2 WH** running **piCorePlayer** (Tiny Core Linux, RAM-based) with **Squeezelite** as the player client. Integrated with Home Assistant (HAHA cluster) for TTS, presence-based playback, and automation.
- 1. **`boot_health_check.sh`** — system health checks (disk, CPU temp, WiFi, internet), blinks green LED on success, starts SNMPD
- 2. **`sbpd-script.sh`** — starts pigpiod, loads uinput, maps physical buttons to Squeezelite commands
- 3. **`led_monitor_squeezelite.sh`** — continuous 5s loop: white=OK, amber=squeezelite down, red=server unreachable; includes WiFi self-healing
- **fcksns07 (Play:5)** only runs `boot_health_check.sh` which combines health check + WiFi self-healing (no GPIO hardware).

**nl:native/synology/CLAUDE.md**
- | **Model** | DS1621+ (6-bay + 2 NVMe) | DS1513+ (5-bay) |
- | **CPU** | AMD Ryzen Embedded V1500B (4c/8t) | Intel Atom D2701 @ 2.13GHz (4c) |
- | **RAM** | 32 GB (3.6 GB used, 27 GB cache, 911 MB swap) | 4 GB (367 MB used, 2.8 GB cache, 296 MB swap) |
- | **DSM Version** | 7.2.2-72806 Update 5 (2025/11/10) | 7.1.1-42962 Update 9 (2025/07/29) |
- | **Kernel** | Linux 4.4.302+ x86_64 | Linux 3.10.108 x86_64 |

**nl:native/pve/CLAUDE.md**
- **pve01 extras:** Common systemd reference set (check_mk, chronyd, iscsi, pve-manager, zed, smartd, sshd, syslog), `pbc-host-backup.sh` script.
- **pve03 extras:** GPU passthrough modprobe (VFIO, IOMMU, nouveau/nvidia blacklist, KVM).
- **gr-pve02 extras:** Dell OMSA systemd units (7 services), `iscsi-portal-fixer`, `rsyncd`, `perccli-log`, Dell APT repo.
- There are **two** PVE-related trees in this repo — they are not the same thing:
- | `../../pve/<host>/{lxc,qemu}/<vmid>.conf` | **Guest** configs (LXC/VM definitions) | Git (after initial SCP) | `ci/lxc.yml` / `ci/qemu.yml` — push-to-deploy, stops guest, replaces config, restarts |

**nl:native/servarr/CLAUDE.md**
- **Status:** Planned — Docker Compose on servarr01 is current production.
- **Motivation:** Operational consistency — one deployment model (K8s/OpenTofu/Argo CD) instead of three. Not for resource savings (VM uses 2.5 GB actual, K8s adds per-pod overhead).
- **Target:** Single `servarr` namespace, Argo CD app-of-apps (`k8s/argocd-apps/servarr/`). NFS PVs for media (nl-nas01+nl-nas02, RWX). iSCSI PVCs for config. Secrets via ExternalSecrets → OpenBao. Drop Watchtower (Argo CD manages versions) and Selfheal (K8s liveness probes).
- **Migration phases:**
- 1. **Foundation** (OpenTofu MR): namespace, NFS PV+PVC, OpenBao secrets

**nl:native/syncthing/CLAUDE.md**
- Continuous file synchronisation across the operator's lab + workstation + service hosts. Each node runs a Syncthing daemon (`~/.config/syncthing/config.xml`); the mesh uses **two private discovery/relay servers** (no public discovery, no NAT-traversal).
- The human-readable inventory (paths, roles, share scope for `03_Lab`) lives in **`Q/03_Lab/CLAUDE.md`** and is itself synced via Syncthing — every `03_lab` member has a copy. Per-node authoritative state is `~/.config/syncthing/config.xml` on each running daemon.
- **Do NOT commit:** `key.pem`, `https-key.pem`, or any `index-v0.14.0.db/` content — those are private keys and local indexes.

**nl:native/ncha/CLAUDE.md**
- **HAProxy backends:**
- - `redis_servers` — redis03(.125) PRIMARY, redis01(.123)+redis02(.124) BACKUP. **Note:** HAProxy uses TCP PING, can't detect Redis master. Actual master is redis02.
- **Key config (config.php):**
- **NFS mounts (both nlnc01 and nlnc02, VLAN 88):**
- **DNS:** `proxysql.example.net` → RR 10.0.181.X + .154 (Nextcloud connects here directly, NOT via HAProxy)

**nl:native/smtp/CLAUDE.md**
- **NL (primary):**
- **GR (independent as of 2026-04-26):**
- | `nlsmtp-gpg01/` | nlsmtp-gpg01 | 10.0.181.X | VMID_REDACTED / nl-pve01 | GPG encryption (zeyple) | **Live** — Debian 12 LXC, 2C / 1GB RAM / 10G disk, VLAN 10 |
- | `nlsmtp-dkim01/` | nlsmtp-dkim01 | 10.0.181.X | VMID_REDACTED / nl-pve01 | DKIM signing (14 domains) + final outbound relay | **Live** — Debian 12 LXC, 2C / 1GB RAM / 10G disk, VLAN 10 |
- | `grsmtp-gpg01/` | grsmtp-gpg01 | 10.0.X.X | 201020204 / gr-pve01 | GPG encryption (zeyple) — GR site | **Live** — Debian 12 LXC, 2C / 1GB RAM / 10G disk, VLAN 2. In policy lock-step with NL as of 2026-04-26 |

**nl:native/haha/CLAUDE.md**
- **Current active node** (use `crm status` for real-time): live state ping-pongs between iot01 and iot02 with each AWX `IOT Weekly Update` run. As of 2026-05-01: iot02.
- **Floating VIP:** 10.0.181.X/24 (Pacemaker `p_vip_iot`, on whichever node is active)
- **VIP DNS:** `nlcl01iot00.example.net` → 10.0.181.X
- **Firewall object group:** `nlcl01iotXX` — iot00(.177/VIP), iot01(.175), iot02(.176)
- **Tags:** `iot;cluster-lab-nl`

**nl:docker/CLAUDE.md**
- - **Path convention**: `docker/<hostname>/<service>/docker-compose.yml` → deploys to `/srv/<service>/` on `<hostname>` via rsync + SSH
- - **Pipeline**: Push to main triggers `ci/docker.yml` — rsync files, then `docker compose pull && docker compose up -d --remove-orphans`
- - **Dockerfile changes**: Trigger `docker compose build --no-cache` instead of pull
- - **Verification**: Pipeline waits 30s then checks `docker compose ps` for unhealthy containers
- - **Notifications**: Matrix webhook on deploy start/success/failure

**nl:docker/nl-gpu01/ollama/CLAUDE.md**
- | `OLLAMA_*` env var | Value | Why |
- **1. Strips cache-churn fields** that Claude Code rotates every turn:
- **2. Patches `anyOf`/`oneOf`/`allOf` JSON schemas** that lack a top-level `type` field, by injecting a synthetic `type` copied from the first option that declares one.
- Ollama 0.20.x's chat template for gpt-oss does `{{ index $prop.Type 0 }}` unconditionally on every tool-definition property. If the property uses a union type (`anyOf: [{type: "string", enum: [...]}, {type: "string", const: "deleted"}]`) with no top-level `type` field, `.Type` is an empty slice and the template panics with *"reflect: slice index out of range"*. Claude Code's `TaskUpdate` tool's `status` property hits this exactly. The patch makes gpt-oss + Claude Code interactive mode work at all.
- All non-`/v1/messages` traffic (native `/api/*`, OpenAI-compat `/v1/chat/completions`, health checks) passes through unmodified.

**nl:docker/nlfrigate01/frigate/CLAUDE.md**
- - **Web UI / API**: `http://10.0.181.X:5000` (and `:8971` for the new authenticated UI in 0.17)
- - **RTSP restream (go2rtc)**: `rtsp://127.0.0.1:8554/<stream-name>` (host-internal only)
- - **go2rtc API**: `http://127.0.0.1:1984/api`
- - **WebRTC port**: UDP/TCP 8555
- - **MQTT broker**: `10.0.181.X:1883` (user `frigate`)

**nl:docker/nlservarr01/servarr/pinchflat/CLAUDE.md**
- **On session start**: Read `PROJECT_STATE.md` to understand current system status, recent changes, and known issues before doing any work.
- **Before session end / on user request**: Offer to update `PROJECT_STATE.md` with:
- This project manages **Pinchflat**, a self-hosted YouTube video archiver, running as part of the consolidated **servarr media stack** on `nlservarr01` with VPN protection. The core component is `pinchflat-selfheal.py`, a Python script that automatically detects and fixes common issues with Pinchflat's operation.
- **Primary Goal**: Keep Pinchflat downloading YouTube videos reliably 24/7 without manual intervention.
- **Migration History**: Pinchflat was originally on a dedicated LXC (`nlpinchflat01`, VMID VMID_REDACTED on nl-pve01). Migrated 2026-03-27 to the consolidated servarr VM as part of IFRNLLEI01PRD-202. All 21 servarr LXCs were consolidated into a single Docker Compose stack.

**nl:docker/nl-matrix01/matrix/CLAUDE.md**
- **At the start of every session**, read `PROJECT_STATE.md` to understand current state.
- **At the end of every session** (or when significant changes are made), update:
- Self-hosted **Matrix homeserver** stack for `matrix.example.net`, running on host `nl-matrix01` at `/srv/matrix/`. This git repo holds the config-as-code mirror. All containers use **host networking** and log to **syslog** (`udp://127.0.0.1:514`).
- ├── /.well-known/*               → static JSON (includes rtc_foci for Element X)
- ├── /_matrix|/_synapse/*         → synapse:8008

**nl:docker/nlprotonmail-bridge01/protonmail-bridge/CLAUDE.md**
- **Native service:** Postfix runs on the host (loopback-only, no relay). Binds port 25, which blocks the bridge's socat SMTP proxy.
- - **Image source:** `images/protonmail-bridge/Dockerfile`
- - **Base:** `debian:bookworm-slim`
- - **Bridge binary:** Installed from official `.deb` release (includes `libfido2` and all dependencies)
- - **Entrypoint:** `entrypoint.sh` — two modes: `init` (interactive login) and default (runtime with socat forwarders wrapped in retry loops, post-2026-04-29)

**nl:docker/nlsearxng01/searxng/CLAUDE.md**
- Primary consumers: **OpenWebUI** + **Perplexica** via JSON API.
- | `searxng` | `searxng/searxng:2026.5.10-df1f24fb7` | Main app — **Granian** WSGI server (NOT uWSGI) |
- `UWSGI_WORKERS` / `UWSGI_THREADS` env vars are **silently ignored**. The runtime is
- Verify workers actually running: `docker exec searxng ps -eo comm | grep -c "searxng worker"` — must equal `$GRANIAN_WORKERS`. Default without env override is **1 worker**.
- Verify after any change: `curl -s https://searxng.example.net/config | jq '.plugins | length'` — expect **10** plugins listed (the 8 above + `oa_doi_rewrite` + `infinite_scroll`/`tor_check` shown as `enabled:false`).

**nl:pve/CLAUDE.md**
- **Total: 116 LXC + 22 QEMU = 138 configs**
- Changes to `pve/**/lxc/*.conf` or `pve/**/qemu/*.conf` trigger `ci/lxc.yml` or `ci/qemu.yml` on push to main.
- **Deploy flow**: backup current config → stop container/VM → upload new config via staging dir → verify with `diff -q` → start → cleanup. Auto-rollback on failure.
- **This causes downtime.** Every config change stops and restarts the container/VM. No live migration or hot-apply. Be aware of which VMs are critical (K8s nodes, DNS, firewall-adjacent services).
- - **CPU**: 4 cores (62%), 2 cores (25%), 8 cores (11%)

**gr:CLAUDE.md**
- This is the **production infrastructure-as-code repository** for the Greece (GR) Kubernetes cluster site (`gr`), part of a **two-site active-active architecture** with the Netherlands (NL) site (`nl`). Everything is managed via **OpenTofu + Atlantis** (infrastructure) and **Argo CD** (applications), with secrets in **OpenBao** via External Secrets Operator.
- - **GitLab instance**: `https://gr-gitlab.example.net/` (GR site — separate instance from NL)
- - **NL GitLab instance**: `https://gitlab.example.net/` (primary GitLab, hosts the NL site's repo)
- - **Repo path**: `infrastructure/gr/production`
- - **Domain**: `example.net` (services use `gr-<service>.example.net`)

**gr:ci/CLAUDE.md**
- **Differences from NL:** GR has 3 includes (NL has 8). GR does not yet have: `cisco.yml`, `lxc.yml`, `qemu.yml`, `github-sync.yml`, `images.yml`.
- | Docker deploy | `docker/**/*` changes | No | No (excluded) |
- | Docker verify | `docker/**/*` changes | No | No (excluded) |
- | K8s (OpenTofu) | `k8s/**/*.tf` changes | Validation only | No |
- | K8s (Argo CD validate) | `k8s/argocd-apps/**/*` changes | Validation only | No (excluded) |

**gr:k8s/CLAUDE.md**
- - **Cluster**: `grcl01k8s` (ID: 2), API at `gr-api-k8s.example.net:6443`
- - **Nodes**: 3 workers, IPs 10.0.58.X, .58.21, .58.22
- - **CNI**: Cilium v1.18.4, eBPF, kubeProxyReplacement, VXLAN tunneling
- - **Pod CIDR**: 10.1.0.0/16 (GR), 10.0.0.0/16 (NL) — must not overlap for ClusterMesh
- - **ClusterMesh**: Connected to NL cluster `nlcl01k8s` at 10.0.X.X, mTLS via ExternalSecret from OpenBao

**gr:docker/CLAUDE.md**
- - **Path convention**: `docker/<hostname>/<service>/docker-compose.yml` → deploys to `/srv/<service>/` on `<hostname>` via rsync + SSH
- - **Pipeline**: Push to main triggers `ci/docker.yml` — rsync files, then `docker compose pull && docker compose up -d --remove-orphans`
- - **Dockerfile changes**: Trigger `docker compose build --no-cache` instead of pull
- - **Verification**: Pipeline waits 30s then checks `docker compose ps` for unhealthy containers
- - **Notifications**: Matrix webhook on deploy start/success/failure (requires `MATRIX_WEBHOOK_URL` CI variable)

**gr:pve/CLAUDE.md**
- **Compliance gap:** NL has full PVE CI automation (`ci/lxc.yml`, `ci/qemu.yml`) that deploys config changes on push to main with automatic backup and rollback. GR has none of this.
- | grk8s-node* (x3) | QEMU | TBD | K8s worker nodes (.58.20, .58.21, .58.22) |
- - **Primary VLAN**: 2 (management, 10.0.X.X/24)
- - **K8s VLAN**: 58 (10.0.58.X/24)
- - **NFS VLAN**: 188 (10.0.188.X/27)

**gr:network/CLAUDE.md**
- GR has a **drift-sync CI pipeline** (`*/30 * * * *` schedule, `auto_detect_and_sync_drift` in `ci/cisco.yml`) landed via IFRNLLEI01PRD-700 + remediation MRs !63/!64/!65/!67 (2026-04-22 → 2026-04-24). Captures device drift via netmiko direct-SSH and auto-commits to `main` as `GitLab CI Auto-Sync`. Live device = source of truth (see `feedback_data_trust_hierarchy`); git tracks reality, it does not drive deployment.
- **Not a deployment pipeline.** Unlike NL, GR does **not** have NAPALM/hier_config/v2 push-side automation. Config changes on GR devices are still made manually via SSH; the pipeline only captures the result. Do not edit `network/oxidized/**` files by hand — they are overwritten on the next drift-sync cycle (~30 min).
- | gr-sw01 | Switch | **Cisco CBS 350** (cbs_ros 3.5.x) | **cisco_s300** (override) | Core switch |
- | gr2sw01 | Switch | **Cisco CBS 350** (cbs_ros 3.5.x) | **cisco_s300** (override) | Remote rack switch |
- **Cisco Small Business (CBS/SG) note** — `gr-sw01` and `gr2sw01` run CBS firmware, not Catalyst IOS. Two device-side prereqs for drift-sync to succeed: (a) `ip ssh password-auth` must be enabled (default off; without it the SSH server advertises no password method and paramiko can't authenticate), (b) `write memory` to persist. Client side: netmiko `cisco_s300` driver handles the ANSI-escape-wrapped prompt + `terminal datadump` paging that CBS uses. Both switches have (a)+(b) set and are listed in the `device_driver_overrides` map in `network/scripts/{detect,auto_sync}_drift.py`. When onboarding a new CBS switch, do the device-side fix first, then add to the override map. See `feedback_cisco_small_business_cbs_ssh` memory.

**gr:edge/CLAUDE.md**
- **Important:** This directory contains **read-only config snapshots** — not deployed by CI/CD. There is no automated deploy pipeline for GR edge services.
- **To update:** SSH to the host, make changes, copy updated config back, commit with `chore(edge): sync <service> config from <host>`.
- **IPsec tunnel groups:**
- - `203.0.113.X` — NL ASA Freedom (primary inter-site; route-based **VTI** `vti-nl-f` — both ASAs are fully VTI, **0 crypto maps** since the 2026-04-09 migration, verified live 2026-06-19)
- - `203.0.113.X` — **NL Budget via nlrtr01** (backup inter-site, `vti-nl`; ex-XS4ALL — the xDSL ISP is now **Budget Internet**, terminated on rtr01's Dialer1)

**gateway:CLAUDE.md**
- - **ChatOps** — infrastructure alerts (LibreNMS, Prometheus) → `#infra-nl-prod`, `#infra-gr-prod`
- - **ChatSecOps** — security alerts (CrowdSec, vulnerability scanners) → same infra rooms
- - **ChatDevOps** — development tasks (CI/CD, features) → `#cubeos`, `#meshsat`
- - **n8n instance:** https://n8n.example.net
- - **GitLab:** https://gitlab.example.net/n8n/claude-gateway (project ID: 30)

**other:/app/cubeos/CLAUDE.md**
- This file is **timeless** — rules, protocols, and patterns only. All project state lives in YouTrack.
- **NEVER commit any Claude Code files to git.** This includes:
- Every repo has `.gitignore` entries for these. If `git status` ever shows `CLAUDE.md` or `.claude/` as untracked, **stop and verify the .gitignore** before proceeding.
- **Local-only (no git, never commit):**
- **Per-repo `CLAUDE.md` files** exist inside each repo directory. They are gitignored and local-only.

**other:/app/n8n/social-media-autoposter/CLAUDE.md**
- - **Edit**: Open the workflow in the n8n UI at https://n8n.example.net
- - **Export**: Export workflow JSON from the n8n UI or API, save to `workflows/`
- - **Programmatic access**: Use `mcp__n8n-mcp__*` tools to read, update, validate, and test workflows. Workflow ID: `dCCyqbu2lrWTxAxh`
- - **Structure**: `workflows/` contains n8n workflow JSON exports
- 1. **Schedule Trigger** — runs every 10 minutes

**other:/app/n8n/doorbell/CLAUDE.md**
- - **n8n instance:** https://n8n.example.net
- - **GitLab:** https://gitlab.example.net/n8n/doorbell (project ID: TBD)
- - **Workflow ID:** `kd43QTEhMTtyVWs2`
- - **Workflow name:** Webhook Bridge (UniFi Protect Alerts → All Destinations)
- - **Status:** Active, 6 nodes

## Incident History

| Date | Alert | Root Cause | Resolution | Confidence |
|------|-------|------------|------------|------------|
| 2026-06-19 | InfragraphPrecisionDrop | Infragraph cascade-predictor precision is below the alert th | Expected noise, NOT a flap. The InfragraphPrecisionDrop thre | 0.9 |
| 2026-06-17 | TargetDown | Prometheus scrape target briefly unreachable (pod restart, r | Self-resolves; transient flap. Auto-resolve at Tier 1 unless | 0.8 |
| 2026-06-17 | HighPodRestartRate | Short-lived pod restart churn (rollout, OOM-and-recover, ima | Self-resolves; transient flap. Auto-resolve at Tier 1 unless | 0.8 |
| 2026-06-17 | ContainerOOMKilled | Container hit its memory limit and was restarted; recovers a | Self-resolves; transient flap. Auto-resolve at Tier 1 unless | 0.8 |
| 2026-06-17 | KubeClientErrors | Transient kube-apiserver client errors during apiserver/etcd | Self-resolves; transient flap. Auto-resolve at Tier 1 unless | 0.8 |

## Related Memory Entries

- **03_Lab Reference Library Integration** (project): 03_Lab (~10GB, ~5200 files) integrated into ChatOps/ChatSecOps triage as supplementary reference. lab-lookup skill, SOUL.md, CLAUDE.md, infra-triage Step 2d, k8s-triage Step 2e, Runner Build Prompt labRefStep.
- **agentic-agriops project — gradual deploy on defra01agri01** (project): Full project state for the agentic-agriops gradual deploy on defra01agri01 (Frankfurt, ifog.ch). Architecture, decisions, what's done, what's blocked, where to resume.
- **agentic-agriops 3-lane vision + phased build plan** (project): Architecture brainstorm 2026-04-27 for what agentic-agriops actually DOES — three lanes (dev flow, support flow, agronomy-advisor) and the phased Phase-A→F build plan to get there. Read after agentic-agriops-project.md (which captures the deploy state) before starting any new lane work.
- **agentic-agri Matrix room IDs and YouTrack project mapping** (project): 3 Matrix rooms + 2 YT projects for the agri lanes — the routing table that n8n workflows + agents key off. Created 2026-04-27.
- **agentic-agri service tokens (operator-authorised storage)** (reference): Three long-lived service tokens for YouTrack/n8n/Chatwoot on defra01agri01. Used by Claude Code MCP servers configured for app-user@defra. Stored at operator's explicit instruction 2026-04-27 (token storage policy = memory).
- **Agentic-platform sweep — 2026-04-24** (project): 13 open agentic items triaged; 10 closed by code shipped; 3 deferred as operator decisions. One-shot reference for the batch.
- **Agentic-platform sweep — 2026-04-25** (project): Post-04-24-batch regression diagnosed + fixed (chaos cmd_start self-flock); node_exporter restored on openclaw01; 3 YT alerts moved to Done.
- **agentic-chatops-page-audit-1048-20260619** (project): 2026-06-19 Playwright audit of live agentic-chatops page verifying the IFRNLLEI01PRD-1048 closed-loop fix renders; reusable live-audit method + uncommitted-fix deploy risk.
- **agentic_patterns_21_21** (project): 21/21 agentic design patterns — tri-source audited 11/11 dimensions A+ (100%). 16 YT issues implemented 2026-04-07.
- **agentic_state_orange_verified_20260628** (project): "2026-06-28 verified the post-benchmark 'orange' issue tier against live sources — ALL items dissolved under verification (3 agent fabrications + false_auto_resolve/demotion gap audited to 0 misfires); no code fixes warranted. MRs !123/!124 shipped + reusable Cronicle-API pattern."
- **agentic-stats outcomes block (auto-resolve % + closed-loop median) — MRs !7 / !9** (project): 2026-05-11 — Outcomes tile added to portfolio agentic-stats widget. Two coupled MRs (claude-gateway script + kyriakos UI). Live numbers + key schema decision. SUPERSEDED 2026-05-12 by per-incident metric semantic (see `[[auto_resolve_regression_diagnosis_20260512]]`).
- **kyriakos agentic-stats Token Usage widget audit + fix — 13 findings closed, 17/17 Playwright PASS** (project): 2026-05-11 read-only audit → same-day full fix shipped in kyriakos commit 729f0bb. All 13 audit items verified on live page. Spec retained as regression check.
- **agentic_top5_fixes_implemented_20260627** (project): "Implemented + merged the 5 do-now agentic improvements (synth oracle, NFKC, OOD gate, KV reorder, egress guard) from the benchmark/downsizing audits via a worktree-isolated workflow + self-review. 2 activation steps are operator-side (n8n SSRF-blocked)."
- **agents-cli audit + adoption plan (2026-04-23)** (project): Deep audit of claude-gateway vs google/agents-cli. 16-dimension scorecard, 10 patterns to steal, 6 to skip, 4-phase adoption plan in /home/app-user/.claude/plans/drifting-napping-donut.md. Plan file approved by ExitPlanMode, but 3 open questions still need user steer before any implementation begins.
- **agt_benchmark_postponed_20260701** (project): "We benchmarked claude-gateway vs Microsoft AGT (2026-07-01); gap recorded, implementation POSTPONED pending operator orders."
- **alert_pipeline_v2_2026_03_18** (project): Major alert pipeline upgrade (2026-03-18): flap detection, issue dedup, confidence scoring, error propagation, CI/CD review, retry loops, few-shot prompts, context summarization
- **alerting_dispositions_silences_20260624** (project): "2026-06-24 alerting policy + the 'silence forever the not-actionable' silences created (GR etcd cascade, NL AS64512CountLow/InfragraphPrecisionDrop), killers kept live. Plus the NL-etcd-unmonitored gap."
- **Tier 2 allowlist audit 2026-03-28** (project): Allowlist trimmed 236→50, golden test monthly→biweekly, 7 external proposals audited (2 implemented, 5 skipped)
- **ans-agent-name-service-eval-20260623** (project): 2026-06-23 evaluated Linux Foundation Agent Name Service (ANS) for the gateway — decision NOT to adopt now; revisit triggers + add to benchmark catalog
- **apiserver-ctrl01-balloon-chronic-restart-fixed-20260515** (project): "RESOLVED 2026-05-15. nlk8s-ctrl01's kube-apiserver had restartCount=1665 (~27 days of crash-looping, ~24-min cycle). Root cause was the balloon device on the underlying VM (VMID_REDACTED on nlpve04) inflating during host pressure events, leaving the VM with only 3.7 GiB instead of 8 GiB. etcd's WAL/DB page cache got evicted → fsyncs disk-bound → apiserver timeouts → liveness probe HTTP 500 → kubelet kill → restart. Fix: `qm set --balloon 0` + VM reboot to apply [PENDING] (config change cannot live-remove a balloon device)."
- **approval-vote-ledger-live-20260617** (project): "2026-06-17 IFRNLLEI01PRD-1100 LIVE on main 86335dd. Approval-poll votes now persist to event_log as mcp_approval_response, emitted from the bridge's Release Lock + Pause SSH nodes (NOT the Extract Messages Code node)."
- **ASA 9.16 BGP limitations — why FRR sidecars exist + why ASAs don't peer with remote RRs** (feedback): Two hard ASA 9.16 constraints (no neighbor update-source, to-the-box cross-interface drop) make direct ASA↔remote-RR iBGP peerings impossible. FRR sidecars are the CCIE-correct workaround. Adding more ASA iBGP sessions is architectural regression.
- **ASA Weekly Reboot — DISABLED** (project): EEM watchdog auto-reboot REMOVED from both ASAs on 2026-04-10. Reboot watcher cron disabled.
- **Atlantis switched to ATLANTIS_TF_DISTRIBUTION=opentofu on nlatlantis01 (2026-04-20)** (project): Server-side env fix unblocking plans after HashiCorp PGP key expired for Terraform 1.14.9. Host-only change (start-atlantis.sh is not IaC-tracked); should be moved into docker-compose.yml env block on next pass.
- **CLI and n8n Audit 2026-03-12** (reference): Comprehensive audit of Claude Code CLI flags, n8n node patterns, and MCP tool usage against official documentation. Includes HIGH-risk bugs, correct/incorrect patterns, and action items.
- **Audit remediation 2026-04-09** (project): Full platform audit remediation. Phase 1 (IFRNLLEI01PRD-411 to 423) + Phase 2 (IFRNLLEI01PRD-424 to 437) — ALL 27 TICKETS DONE as of 2026-04-19 (verified against YT State + code).
- **9-Source Audit → Repo File Mapping** (reference): When grading the platform against the 9 rubric sources, which file in claude-gateway is canonical for each source. Used by /loop audits, mirror re-grading, and bundle assembly.
- **auto-resolve-regression-diagnosis-6-76-headline-2026-05-12** (project): RESOLVED 2026-05-12 — Auto-resolve headline 6.76% (event-based) → 28.57% (per-incident, honest) on identical 7d data. Commits 1434bb5 + 69bd6f7 on fix/agentic-stats-tier-classify. Page rendering verified via Playwright. Two blast-radius control issues (IFRNLLEI01PRD-894 + IFRGRSKG01PRD-241) created for future cascade folding. Prior "76%" was inflated by 51 cutover synthetics; real baseline ≈ 46%. 7d window naturally clears the 2026-05-11 cascade by 2026-05-19 → expected steady-state 55-65%.
- **autonomous_benchmark_mission_20260625** (project): ACTIVE 24h autonomous mission (2026-06-25, operator AFK ~24h) — benchmark the gateway against the Anthropic + OpenAI ebooks (separate attributed scorecards), improve every dimension to ≥A, then build the orchestrator epic. FULL-AUTONOMY mandate granted.
- **autonomous_conservative_remediation_20260625** (project): IFRNLLEI01PRD-1408 — autonomous conservative-remediation auto-execute (operator out of the loop). Built + partially deployed live 2026-06-25; carve dark behind a sentinel. Mid-implementation.
- **autonomy-forward-gate-live-20260616** (project): "Autonomy-forward risk gate (3-band, human as circuit-breaker) is LIVE + ENABLED on nl-claude01 and merged to main; how to operate/kill it."
- **autoposter-silent-halt-search-widget-20260527** (project): "2026-05-27 RCA — RSS2Postiz workflow (n8n ID dCCyqbu2lrWTxAxh) stopped posting after 2026-05-17 because withelli.com added a client-side search widget whose JS-template `<img src=\"'+e.thumb+'\">` now appears before the article image; the brittle image-extractor regex returns an empty src → imageUrl=null → silently filter-zero at Filter Latest Item; no node errors so all executions still report status=success."
- **awx-default-group-zero-capacity-20260620** (project): "2026-06-20 NL AWX default instance-group capacity=0 → all kyriakos portfolio deploys stuck pending → CI timeout. Infra outage, not a code bug."
- **AWX project_update 504 pattern on infrastructure/common/production.git** (project): Recurring transient HTTP 504 on AWX git-fetch from this specific repo, ~once per 2-3 weeks since 2026-01. 5 confirmed instances. Self-heals in 15-20 min.
- **BGP community scheme for inter-site path selection (YT-200 fix)** (project): Community origin-tagging at VPS edges + receiver-side LP policy on ASAs, deployed 2026-04-23 to fix GR Prometheus scrape asymmetry. Scales to any future site/edge.
- **ASA iBGP ECMP -> 1 fix (NL + GR) 2026-04-17** (project): NL + GR ASA maximum-paths ibgp 4->1. Fixed AWX-to-VPS-loopback recurring timeouts caused by asymmetric ECMP paths dropped as tcp-not-syn.
- **blast-radius-control-issues-for-phase-1b-operator-playbook** (project): 2 always-on openclaw_memory blast-radius rules gated by YT control issues. Reopen the issue when the cascade pattern is observed; close when stable. Phase 1b only folds while the issue is open. Created 2026-05-12.
- **Budget migration 2026-04-21 — completion state + outstanding items** (project): xs4all->budget rename + rtr01 WAN-edge isolation migration; completed Steps 1-8; etherchannel attempt aborted after my channel-group 1 mistake caused mgmt outage; rtr01 reload-safety recovered it. Open items for follow-up.
- **cc-cc migration done — OpenClaw retired (2026-04-29)** (project): 2026-04-29 — OpenClaw LXC stopped + onboot=0. All 9 alert receivers rewired to call triage scripts via direct SSH to nl-claude01 instead of @openclaw Matrix mention. Webhook→SSH→YT issue chain verified ~6s end to end.
- **cert-sync playbook — visible unreachable-host banner 2026-04-17** (project): Added observability banner to sync_certs_to_edge.yml so ignore_unreachable skips are no longer silent. Commit 328c6f7.
- **chaos_audit_history** (project): Consolidated chaos-engineering audit history 2026-04-11 → 2026-04-14. Four audit passes, 55 findings total, all fixed. Security posture 9.52/10. CMM Level 2+ (path to L3 documented). Supersedes chaos_audit_20260411, chaos_deep_audit_20260411, chaos_comprehensive_audit_20260414, chaos_audit_remediation_20260414.
- **chaos_baseline_history** (project): Chaos baseline plan (2026-04-12) → implementation (2026-04-13) narrative. 13 YT issues (468-480), 10/13 Done, 18 experiments run (15 PASS, 2 DEGRADED, 1 FAIL). iBGP full mesh fix landed as part of Phase 2. Supersedes chaos_baseline_plan + chaos_baseline_impl.
- **chaos_baseline_metrics_fix_20260416** (project): Chaos baseline comparison fixed (2026-04-16). Was comparing convergence (~37s) vs wall-clock (~136s) = always SLOW. Now uses recovery_seconds. IP tracking added. Live log race fixed.
- **chaos_cron_collision_20260423** (project): 2026-04-23 two chaos drills collided on ~/chaos-state/chaos-active.json at 04:00 UTC producing a spurious "Experiment None FAIL" Matrix message. Failover actually worked. Safeguard gaps identified.
- **Autonomous chaos-port-shutdown primitive + Freedom-ONT drill** (project): scripts/chaos-port-shutdown.py runs the full Freedom-ONT drill unattended — shut nl-sw01 port, observe, PoE-recycle restore, write chaos_experiments row. First unattended run 2026-04-23 08:00 CEST. Replaces the manual operator-nudge flow.
- **chaos_weekly_cron_20260414** (project): Weekly chaos cron deployed (CMM Level 3). Daily 10:00 UTC cron, self-selecting exercise type. chaos_exercises table, exercise-summary CLI, preflight gate, Matrix notifications.
- **CLAUDE.md refactor 2026-05-06 — 52.6 KB → 24.9 KB** (project): Distributed dated content out of CLAUDE.md to existing referenced files (platform-features.md, infrastructure.md, llm-usage-tracking.md, memory/*) to clear the 40 KB performance threshold. No information loss; 35/35 cross-file pointers resolve. Allocation table added at CLAUDE.md bottom to prevent regrowth.
- **CLI-session RAG capture pipeline** (project): 3-tier capture (IFRNLLEI01PRD-646/-647/-648) of interactive Claude Code CLI JSONLs into session_transcripts + tool_call_log + incident_knowledge. Closes the gap where CLI sessions produced knowledge but only cost/tokens were recorded.
- **cloudflare_dns_api_access_20260624** (reference): Standing Cloudflare DNS API access for the example.net zone — token in .env; manage public A/CNAME records via the API; never ask the operator for the token again
- **Cloudflare API token for meshsat.org zone (operator-authorised storage)** (reference): Cloudflare User Token with DNS edit access on the meshsat.org zone. Stored at operator's explicit instruction 2026-04-27 ("save this don't ask again"). Used for all DNS record management on meshsat.org.
- **CodeGraphContext (CGC) Setup** (project): Code graph database for CubeOS/MeshSat — Neo4j backend, scheduled reindex (no live watcher), MCP server, 43K nodes across 5 repos
- **composed_eval_verdict_20260627** (project): Built compose-eval-verdict.py (Ch8 det+LLM blended verdict) — JOINs session_trajectory + session_judgment hard-first-then-judge. Surfaced that the LLM judge is being FOOLED (4.6-4.8/5 on 25-50%-complete sessions). MR !104 merged + live.
- **cronicle_migration_20260626** (project): ALL 172 schedulable cron jobs (gateway/ChatOps 100 + agora/quant 72) migrated off the raw crontab to Cronicle, installed NATIVELY on nl-claude01 (NOT Docker). Cutover done + verified 2026-06-26. Rollback = restore the crontab snapshot.
- **dark_component_audit_20260625** (project): 2026-06-25 systematic sweep for "MemPalace-class" silently-dark components (built, looks live, but unused/broken for weeks-months). Scout done; classification workflow wf_5cda57e3 in flight.
- **definitive_guide_benchmark_20260627** (project): "Benchmarked our agentic system against O'Reilly \"AI Agents: The Definitive Guide\" (12 ch). Overall B-/C+ (2.42 GPA) — strictest lens yet; strong control-half, weak systems-engineering-half. Router bug independently re-confirmed."
- **defra01agri01 — agentic system mirror target** (project): Designated mirror target for gradual deploy of the NL agentic system (n8n + Claude Code + RAG + chaos). Access + baseline specs.
- **DLI RAG Course Slides** (reference): NVIDIA DLI "Building RAG Agents with LLMs" 188-slide deck stored at docs/DLI-RAG-Slides.pptx — reference for RAG concepts mirrored in this project
- **dmz_chaos_engineering** (project): DMZ cluster monitoring + web service chaos engineering implementation (2026-04-10). Graph redesign, safety calculator, 7 scenarios.
- **dmz-container-count-zero-baked-20260513** (project): "Status page shows DMZ host 0/0 containers in user's normal browser but 30/30 in incognito — stale browser cache of an HTML that baked containers_total=0 when ssh failed in vpn-mesh-stats.py"
- **dns-chain-servfail-recovery-20260516** (project): "2026-05-16. Two-part DNS incident on NL/GR DNS chain. (a) NL FreeIPA bind held stale NS delegation for extreme-agri.gr (zone moved server40.net → Cloudflare); cleared via rndc flushname (operator-flagged as wrong shape — see feedback-freeipa-named-no-manual-ops). (b) GR FreeIPA named had been crashed since May 10 (lib/isc/mem.c:2549 REQUIRE mctx magic during logrotate-triggered rndc reload); recovered by-the-book via `ipactl start`."
- **docs_audit_20260628** (project): Audit of CLAUDE.md/README.md/README.extensive.md vs the live system — verified ground-truth count table + the stale-claim clusters + in-flight edit plan (workflow wf_676d9878-b38)
- **dual_source_audit_2026_04_03** (project): Comprehensive dual-source audit (Anthropic docs + Gulli book) — hooks, sub-agents, skills, guardrails, sanitization, RAG, VPN fix. 8+1 YT issues.
- **Dual-WAN VPN full parity (Freedom + xs4all)** (project): Both NL WANs have full S2S tunnel coverage. Freedom PPPoE outage auto-handled via xs4all failover, QoS cron, SMS alerting, and trained triage scripts.
- **edge-vps-bgp-audit-20260517** (project): "2026-05-17 audit of AS64512 external BGP across CH/NO/TX VPS — RESOLVED same day. Infrastructure: MR infrastructure/nl/production!305 (merge commit e378876), pipeline #31849 ✅ 75s. Status-diagram: kyriakos:4e6f279 (cache-buster v=44), pipeline #31843 ✅ 179s. D1+D2 BGP-drift fixed live on all 4 FRR RRs; CLAUDE.md FRR versions corrected; IaC snapshots re-pulled; status-page diagram now wires all 3 VPS to correct upstreams."
- **feedback-adapter-knobs-must-be-env-driven** (feedback): "Every adapter tunable (timeout, page-cap, item-limit, retry, inter-request delay, kill-switch) MUST live in .env / sops not in source. Tuning a value should never require a new MR + image rebuild + auto-deploy cycle."
- **Try admin API operations before claiming "no admin access"** (feedback): Operator caught me listing "no admin access" as a residual concern when I had `is_admin=True` token + sudo + admin_mode scopes the whole time. Don't conflate "no SSH/Rails-console" with "no admin REST." Try the API first.
- **ASA after-auto source dynamic PAT has rpf-check side-effect on inbound traffic** (feedback): `nat (<src_zone>, <outside_X>) after-auto source dynamic any interface` silently rpf-drops inbound traffic on outside_X destined to src_zone when source doesn't match the interface IP. Fix is Section-1 identity NAT for the specific subnet pair.
- **HAProxy allowlist is the safety net — deploy compose + frontend in one shot** (feedback): When a new service will live behind an HAProxy allowlist, do the full deploy (compose up + HAProxy backend + frontend ACL + reload) in a single step. Don't split into "internal-only first, expose later."
- **feedback-always-code-in-foreground** (feedback): "Operator preference: always code in the foreground. Do NOT delegate Rust/SQL/Maud/Python implementation work to general-purpose subagents via Agent + run_in_background. Operator wants to see each edit + commit + push in real time, and wants me to own the implementation decisions personally rather than briefing a subagent and waiting on its report. Subagents are still fine for read-only research (Explore agent) and orthogonal verification (Playwright/visual subagent) — the constraint is specifically on CODE that's part of the deliverable."
- **feedback-always-netmiko-for-cisco** (feedback): "ALWAYS use netmiko (device_type cisco_asa / cisco_ios) for ANY Cisco ASA/IOS access — never sshpass, expect, or raw ssh/paramiko command-channel."
- **feedback_always_screenshot_visual** (feedback): NEVER claim visual/graphic/color changes are done from code alone — ALWAYS verify with Playwright screenshots
- **Anchor literal markers in LLM output to start-of-line** (feedback): When parsing markers (e.g. `[POLL]`, `[AUTO-RESOLVE]`, `CONFIDENCE:`) out of free-form LLM output, anchor the regex to start-of-line AND prefer the last match — never the first. Otherwise quoted/echoed prompt instructions hijack the parse.
- **feedback-anti-slop-means-empty-chip-is-valid** (feedback): "On the vacancy-match chip strip, `—` (data_present=false) is a correct anti-slop signal when source data is genuinely absent. Don't fabricate a number to fill the gap."
- **When operator asks for "API", confirm API surface first — don't dive into source/DB until gap is proven** (feedback): When the operator says "use the API", the FIRST move is to GET the API root listing and grep for the relevant route. If the documented route exists, use it. If it doesn't, surface the gap to the operator and offer alternatives. Don't preemptively dive into source code, DB schema, or CLI tools until you've confirmed the REST surface can't do it.
- **ASA 9.16 PPPoE/VPDN show commands — which exist, which don't** (feedback): Skip the dead command list when diagnosing PPPoE on nl-fw01 — show vpdn session/tunnel and show pppoe* do not exist; use show interface + show ip address + show track + tracked routes + ping via the other WAN
- **ASA floating-conn for route changes** (feedback): Enable timeout floating-conn on ASA to auto-teardown stale connections when BGP/routing changes. Use netmiko for ASA automation, not expect.
- **ASA crypto-map delete+recreate pattern** (feedback): When modifying ASA crypto-map peer lists, always DELETE then RE-CREATE the entry. In-place changes leave stale IKEv2 TS matching tables.
- **GR ASA access — direct via grclaude01 netmiko (not OOB public IP)** (feedback): To diagnose/query gr-fw01, SSH direct to grclaude01 over VPN and drive netmiko from `/tmp/netmiko-venv/`. Do NOT go via OOB 203.0.113.X:2222 unless VPN is down.
- **ASA show dhcpd CLI gotchas** (feedback): ASA `show dhcpd binding` / `show dhcpd state` reject interface filter; client-id has 01 hwtype prefix that must be stripped to recover MAC.
- **feedback_asa_shun_vti** (feedback): ASA threat-detection scanning-threat shun recurring issue — ANY new trusted infrastructure subnet must be added to whitelist_shun_nlgr_all_subnets on nl-fw01 (+ GR ASA), else it WILL be shunned during high-traffic events. Missing entries caused incidents 2026-04-12 (VTI mesh) and 2026-04-22 (rtr01 transit /30).
- **ASA syslog timestamps are in the ASA local clock (CEST), not UTC** (feedback): Avoid 2h labelling errors when copying ASA log lines into customer/vendor emails — ASA timestamp = clock timezone setting, not UTC
- **Always ask before making changes** (feedback): NEVER modify config files without explicit user approval — present findings, propose fixes, wait for go-ahead
- **Atlantis apply command format and architecture** (feedback): Atlantis apply requires project flag, and NL/GR are independent Atlantis instances — can run in parallel
- **Atlantis — do NOT spam plan/apply commands** (feedback): Post Atlantis commands ONCE and WAIT. Never poll in a loop or re-trigger. kube-prometheus-stack plans take 1-3 minutes.
- **feedback_audit_before_mass_delete** (feedback): NEVER mass-delete config lines by grep pattern without auditing each one. The NAT cleanup removed 161 lines blindly and missed that xs4all had no outbound PAT.
- **Audit-before-sync pattern — prefer this over ignore_unreachable** (feedback): When a sync playbook needs tolerance for unreachable/broken hosts, add a separate scheduled audit playbook that fails LOUD *before* the sync runs. Do not retrofit ignore_unreachable onto the sync itself.
- **Audit dependency source before integrating, not after** (feedback): When wrapping a foreign program/library, read its key source files up front rather than paying for omissions in redeploys
- **feedback-autonomous-claude-p-loop-dispatch** (feedback): "How to dispatch autonomous extraction/processing loops via `claude -p --dangerously-skip-permissions` with stream-json + an optional driver wrapper. Opus 4.7 self-iterates in-response for well-scoped bounded loops, so the driver is a safety net rather than a critical-path tool."
- **AWX EE Image Persistence Problem** (feedback): Custom EE images imported via ctr are lost on K8s node reboot. Need persistent registry or image in PVC.
- **AWX project_update HTTP 500 ≠ 504 — check gitlab host disk first** (feedback): AWX project_update failures with HTTP 500 from gitlab indicate something durable (disk full → redis read-only) and require intervention; the recurring 15-20min self-heal applies only to HTTP 504 (gitaly timeout / sidekiq queue saturation)
- **Add per-peer LP override when introducing a new ISP edge (rtr01-style), not just blanket FRR_TRANSIT_IN** (feedback): When a new edge router (e.g. rtr01) is added and directly peers iBGP with remote endpoints (VPS, other sites), the FRR RRs may reflect the same prefix via two NHs — one via the new edge (Budget), one via the old Freedom path. Both arrive at the core ASA under the same `FRR_TRANSIT_IN` (LP 100) route-map — tie-break picks the new edge. But the REPLY from the remote endpoint comes back on the original Freedom-path interface → asymmetric routing → stateful drop on ASA (rpf-violated, nat-rpf-failed). Add a per-peer route-map (e.g. `FREEDOM_FRR_IN` applied only to the FRR that reflects the Freedom-NH version) matching a VPS_LOOPBACKS-style prefix-list and setting LP 200. ASA 9.16 does NOT support `match ip next-hop` in BGP inbound route-maps — must use prefix-list match only, scoped per neighbor.
- **feedback-cache-only-request-path-pattern** (feedback): "When a request handler does serial HTTP fan-out to enrich a view, refactor to cache-only + background tokio::spawn. The user-facing path should never block on live HTTP."
- **Canary cron during dispatch-chain cutover, retire once real traffic confirms steady state** (feedback): A canary cron is a temporary cutover instrument, not a permanent monitor. Run it during a dispatch-chain rewire to catch silent regressions, then retire it once real alert volume has continuously exercised the chain for >24h.
- **Canary verification mechanisms must clean their own artifacts** (feedback): Synthetic-traffic monitors that produce real production records (YT issues, tickets, pages) need cleanup designed in at day 1, not "operator filters by name"
- **Canonical-label helpers must use site-priority fallback, not blind reverse** (feedback): When a helper normalizes a bidirectional label (e.g. "NL ↔ GR") via a lookup table, the fallback for misses must be a deterministic ordering — not blind reverse. Blind reverse silently breaks the moment a new entity is added that isn't in the table yet.
- **Capture state on exception raise, not in the except handler** (feedback): When a `with lock:` block raises, the lock has already been released by the time `except` runs — re-reading the locked state there is racy. Carry the data on the exception instead.
- **Cloudflare edge serves stale HTML during Hugo deploy verification — always bust the cache** (feedback): After pushing a kyriakos.papadopoulos.tech deploy, the first plain `curl https://...status/` will likely return the PREVIOUS build because Cloudflare's edge cache hasn't seen its TTL expire. Use `Cache-Control: no-cache, no-store` + `Pragma: no-cache` + a unique nanosecond query param to defeat the edge. Otherwise you'll chase ghost regressions in code that's already correct.
- **chaos catalog drift per topology change** (feedback): experiments/catalog.yaml drifts silently every time a new edge router/VPS is added — pre-existing rtr01-since-2026-04-21 gap was caught only when a 2026-05-06 TX onboarding referenced rtr01 as a target and the validator failed. Check + update the catalog (targets + experiments) on every topology change.
- **chaos test density can drift kernel state** (feedback): When running chaos tests on shared production clusters, multiple back-to-back disruptive operations within minutes can plant non-deterministic kernel-state issues that surface 30-60 minutes later as a real outage. Space tests apart and verify ground-truth between batches.
- **Chatwoot FORCE_SSL=true breaks internal HTTP — set X-Forwarded-Proto: https** (feedback): Chatwoot rails has FORCE_SSL=true by default which 301-redirects all plain HTTP to HTTPS. Internal n8n→rails:3000 calls break on the redirect (port 3000 has no SSL). Fix is the standard "behind a reverse proxy" pattern — send X-Forwarded-Proto:https header.
- **Chatwoot sidekiq is on a separate docker network from rails — webhooks need explicit network attach** (feedback): Chatwoot's default compose puts rails on both the public/agentic network AND chatwoot-internal, but sidekiq on chatwoot-internal ONLY. Outbound webhook delivery runs in sidekiq, so without an explicit network attach sidekiq can't resolve sibling services and all webhooks timeout.
- **check other sessions before claiming account exhaustion** (feedback): If you're about to diagnose "account/quota exhausted," first check whether OTHER sessions on the same account are still working. If they are, your theory is wrong. (2026-04-29 OpenClaw incident — three rounds of bad theory before admitting it.)
- **Check upstream capabilities before designing a shim** (feedback): Before proposing a translation shim/proxy/adapter for a third-party service, search its source + CHANGELOG for native support of the target. Saved ~150 LOC of FastAPI work on the OpenClaw Sonnet migration when OpenClaw 2026.4.11 already shipped --auth-choice claude-cli.
- **feedback-check-working-case-before-writing-refresh-code** (feedback): "When fixing a \"stuck SVG / stale UI\" bug, look at the case that already works correctly in the same code path before writing a new refresh block — the existing pattern usually IS what you should extend."
- **When K8s pod TCP probes hang at 10s — check CiliumNetworkPolicy egress first** (feedback): A K8s pod that hangs trying to reach an external port is most likely blocked by a CiliumNetworkPolicy egress rule, not a routing issue. Check `kubectl get cnp -n <ns>` before blaming the network. Verify with a labeled curl pod in the same namespace.
- **Adding IPsec peer to a Cisco edge — also add to inbound ACL on the WAN interface** (feedback): nlrtr01's Dialer1 has an explicit inbound ACL (OUTSIDE_BUDGET_IN) that whitelists ESP / udp 500 / udp 4500 only from known peers, then deny ip any any log. New IPsec peers need 6 ACL entries (ESP + isakmp + non500-isakmp from each new src). Symptom of missing rule = silent IKE retry, no log on the strongSwan side.
- **Cisco IaC — device is the source of truth, don't file-edit ahead** (feedback): Cisco configs under infrastructure/nl/production/network/configs/ (NL) and .../gr/production/network/oxidized/ (GR) are captured by a GitLab CI drift-sync job (`auto_detect_and_sync_drift`, schedule `*/30`) that netmiko-SSHes each device, normalises the output, and pushes to main as "GitLab CI Auto-Sync". Apply changes to the live device first, then let the job sync them to git. Don't open human MRs that edit the file before the live change exists. (Oxidized was decoupled from gitops 2025-11-23 — still runs on both oxidized01/02 hosts as an independent local-filesystem backup tier, but no longer pushes to GitLab. See docs/runbooks/oxidized-role.md per IFRNLLEI01PRD-701.)
- **lib/devices.py expects CISCO_PASSWORD; nl-claude01 has CISCO_ASA_PASSWORD** (feedback): For ad-hoc netmiko queries against NL Cisco gear, alias CISCO_ASA_PASSWORD into CISCO_PASSWORD before calling lib.devices helpers
- **feedback_cisco_small_business_cbs_ssh** (feedback): Cisco CBS/SG/SF ("Small Business") switches: default SSH password-auth is DISABLED + CLI has ANSI escape codes + different paging. Need device-side `ip ssh password-auth` + netmiko `cisco_s300` driver.
- **Classify CI failure clusters by JOB, not by pipeline status** (feedback): When investigating a cluster of failed pipelines, the failing JOB inside each pipeline tells the root cause; pipeline status alone conflates unrelated job failures.
- **Classify pipeline failures by step before assuming common cause** (feedback): Same job name can fail in multiple distinct ways across runs. Pull full traces and group by which step failed before proposing a single fix.
- **clippy-local-must-match-ci-workspace-all-targets** (feedback): "Always run `cargo clippy --workspace --all-targets --locked -- -D warnings` locally before push — `--bin --tests` is a narrower gate that misses casts CI catches"
- **feedback-clippy-strict-flags-for-omoikane-daemon** (feedback): "Pre-push clippy on omoikane.coach/daemon MUST run `bash scripts/ci-clippy-gate.sh` (NOT a hand-rolled flag list). The CI gate's deny set lives in `app/Cargo.toml [lints.clippy]`, not in the CLI — running with `-D warnings` alone OR with a partial hand-picked flag list will pass locally then fail CI on `cast_precision_loss`, `cast_possible_truncation`, `cast_sign_loss`, `cast_possible_wrap`, `explicit_iter_loop`, `unwrap_used`, etc. Same mistake repeated 4+ times: 2026-05-29 OMOIKANE-755, 2026-06-02 OMOIKANE-1117 MR-8b."
- **feedback-company-enrich-search-chain-diagnostic** (feedback): "When `company_enrich_llm::enrich()` silently returns Ok(None) and llm_enrichment_fetched_at stays NULL, do NOT assume \"Brave rate limit\". Read the actual log labels — the failure is far more likely all_engines_blocked on SearXNG with Brave skipped (not configured), because the default flipped 2026-05-24."
- **feedback_converge_diverged_live_branch_in_worktree** (feedback): How to safely converge a diverged LIVE working-tree branch with main without losing anything (3-way merge in an isolated worktree).
- **corosync CMAP version-mismatch = clone-of-member rejection** (feedback): corosync `[CMAP] Received config version (X) is different than my config version (Y)! Exiting` is the diagnostic signature for a node booting with a stale `corosync.conf` — typically a disk clone of an existing cluster member. SSH host-key change between reconnects is the parallel signature for a remote OS reinstall.
- **count-incidents-not-events-when-measuring-auto-resolve** (feedback): "For auto-resolution / MTTR / noise-rate metrics, the unit of analysis must be the incident (unique issue_id), not the alert event. Event-based counting inflates denominators 4-5× during sustained outages and is flagged as an immaturity signal by mainstream SRE references."
- **Cron PATH excludes /usr/local/bin — always export PATH in cron scripts** (feedback): Default cron PATH on Debian/Ubuntu is /usr/bin:/bin. Scripts that call tools from /usr/local/bin (or any path outside cron PATH) silently fail with bash exit 127 if stderr is redirected to /dev/null.
- **Data trust hierarchy** (feedback): Live device config > LibreNMS > NetBox > supplementary (03_Lab, IaC, backups). Running config is the ONLY 100% truth.
- **dataclass(frozen=True) + importlib.util fails on Python 3.11 — use NamedTuple** (feedback): Python 3.11's `@dataclass(frozen=True)` evaluates type annotations at class-creation time via `_is_type` which looks up `cls.__module__` in `sys.modules`. When the module was loaded via `importlib.util.spec_from_file_location` and not registered in `sys.modules`, this returns `None` and the dataclass decorator raises AttributeError. Use `typing.NamedTuple` instead — same immutable+typed shape, no `_is_type` path.
- **defra01agri01 SSH pattern — operator + one_key + sudo -i ONLY** (feedback): Hard rule for every SSH session to defra01agri01. User operator with ~/.ssh/one_key only. No passwords ever. Privilege escalation via passwordless sudo -i.
- **feedback_deployed_copy_not_repo_for_some_crons** (feedback): "Some gateway crons run from /home/app-user/scripts/ (un-versioned deployed copies that have DIVERGED from the repo), not the repo working tree. Editing the repo script does NOT take live effect for those — and a committed MR can look done while the live behavior is unchanged."
- **feedback-diagnose-deploy-hang-on-host-process-tree-first** (feedback): "When a deploy/Ansible task hangs on a specific host, read the host's process tree FIRST (ps wchan) instead of theorizing about the network."
- **feedback_direct_push_repos** (feedback): Repos that push directly to main (no feature branches, no MRs). Table + per-repo notes. Supersedes 4 separate feedback memories.
- **feedback-docker-compose-env-file-force-recreate** (feedback): "docker compose `restart` does NOT reload changed env_file contents. Only `up -d --force-recreate` re-reads them. Trap caught 2026-05-29 on omoikane.coach daemon."
- **feedback-docker-compose-restart-policy** (feedback): "Every docker-compose service on a PVE LXC must have explicit restart: unless-stopped — otherwise containers vanish on the next host reboot and stay dead for months until someone notices."
- **docker-reclaimable-misleading** (feedback): "Don't alert on `docker system df` Reclaimable column. It drifts upward on healthy hosts. Alert on dangling images instead."
- **dockerfile is the right place for runtime writes to root-owned paths** (feedback): If your container runs as non-root (e.g. node uid 1000) and you need to install/write to root-owned dirs like /usr/local/lib/node_modules at start, do it at build time via Dockerfile RUN. Don't do it in entrypoint scripts.
- **feedback_dont_disturb_foreign_repo_working_tree** (feedback): Never blind git stash/checkout/branch a SHARED repo's live working tree (esp. the infra repo) — it may be on an active WIP branch with the operator's uncommitted work. Use a worktree off origin/main. Also: agentic-health.yml is 6-space + doc-only; the .tf is the deploy.
- **don't infer account quota state from error messages** (feedback): Anthropic 402 + "out of extra usage" wording does NOT mean the account is at 100%. claude.ai/settings/usage is the authoritative source. Verify before claiming exhaustion.
- **feedback_dual_wan_nat_parity** (feedback): Dual-WAN requires PAT rules on BOTH outside interfaces for ALL inside zones. Freedom-only PAT = no internet when Freedom is down.
- **feedback_etcd_per_node_skew_can_be_counting_artifact** (feedback): "A lopsided etcd alert count across control-plane nodes (e.g. 212 vs 27/15) is NOT proof one node's disk is uniquely slow — check bucket-immune average fsync/commit per member before localizing"
- **Explicit stop conditions when sweeping list-shaped LLM output** (feedback): When extracting a list (poll options, fixes, plans) from free-form LLM output, never sweep "until end-of-message" — define explicit terminator regex or trailing prose gets absorbed as fake list items.
- **feedback-freeipa-named-no-manual-ops** (feedback): "In FreeIPA, never operate named directly — no rndc, no /etc/named/*.conf edits, no systemctl restart. Only via ipa* / ipactl commands."
- **FRR route reflectors are independent LXCs, NOT K8s pods or CRDs** (feedback): All 8 FRR instances (2 NL RR + 2 GR RR + 2 VPSs + 2 ASAs-as-clients) run as independent OS-level FRR daemons, not Kubernetes resources. Config changes go directly via SSH + /etc/frr/frr.conf, not via Atlantis MR.
- **Always use full hostnames [P0]** (feedback): P0 rule — never strip site/cluster prefixes. Use nl-pve02 not pve02, gr-dmz01 not dmz01, never "the ASA"/"the router"
- **GIT_STRATEGY=empty + manual clone bypasses refs/pipelines fragility** (feedback): When a CI job's get_sources fails with "couldn't find remote ref refs/pipelines/<id>" and the job doesn't actually need git history, set GIT_STRATEGY=empty and do your own `git clone` in before_script.
- **feedback_gitops_autonomous** (feedback): "Execute gitops (branch/commit/push/MR) autonomously — do NOT pause to ask \"should I commit?\" each time. Still surgical + convention-following; routine gitops is not a gate."
- **feedback-goal-rationale-prom-histogram-empty** (feedback): "RESOLVED 2026-05-25. `omoikane_goal_rationale_duration_seconds` was empty post MR !2425 because `metrics-exporter-prometheus` doesn't emit HELP/TYPE/buckets until first sample. Fixed by 0.0s boot-emit in MR !2449 (CCS-02 follow-up)."
- **OpenAI model instruction pattern for OpenClaw (HISTORICAL — Tier 1 migrated to Sonnet 2026-04-28)** (feedback): OpenAI models (GPT-4o, GPT-5.1) don't follow auto-trigger patterns in system prompt. Use direct explicit instructions via n8n instead. Historical context — Tier 1 moved to Sonnet via OAuth on 2026-04-28 (IFRNLLEI01PRD-746); Sonnet handles auto-trigger patterns reliably so this rule no longer affects production. Re-applies if OpenAI is ever swapped back in.
- **gpu01-target-ram-32g** (feedback): "Operator's chosen RAM allocation for nl-gpu01 is 32 GiB (32768 MB), not the historical 28 GiB. Don't argue this down to 28 G citing nl-pve03 host pressure — operator owns the trade-off."
- **GR ASA SSH requires stepstone via gr-pve01** (feedback): SSH to gr-fw01 only works via gr-pve01 as a jump host — direct SSH from NL is rejected (connection reset).
- **feedback_gr_dmz_direct_ssh** (feedback): GR DMZ (gr-dmz01) — use direct SSH via VPN, NOT OOB stepstone
- **feedback_greek_banned_phrase_sou_epanerxomai** (feedback): Operator-banned Greek phrase "Σου επανέρχομαι σύντομα" / "σου επανέρχομαι σύντομα". Correct form is "Επανέρχομαι σύντομα" (no "Σου"). Pin in every Greek-language LLM prompt + every static template.
- **feedback_greek_banned_word_evretiriasmena** (feedback): Operator-banned Greek word "ευρετηριασμένα" (and the verb ευρετηριάζω). Use plainer alternatives καταχωρημένα / καταγεγραμμένα / διαθέσιμα in any Greek-language LLM output.
- **Grep for parser-pattern duplication BEFORE declaring a fix complete** (feedback): When you fix a parser bug, search the whole repo for the buggy regex/pattern. Parsers get copy-pasted across workflow Code nodes and scripts; fixing only one leaves the other broken in production.
- **Grep for hardcoded host paths after migrating between deploy targets** (feedback): When a script moves from one host environment to another (openclaw container → nl-claude01, dev → prod, container A → container B), grep the FULL repo for the old environment's hardcoded paths — patching site-config.sh alone misses scripts that have their own SSH_KEY / token / DB-path constants. Caught 2026-04-29 on security-triage.sh.
- **HAProxy has a 64-words-per-line limit — use multi-line ACL pattern for host lists** (feedback): When growing a host-match chain in HAProxy, don't pile `|| { hdr(host) -i X }` onto one line. Use multi-line `acl name hdr(host) -i X` blocks (combine with OR for the same ACL name). 64-word limit is a hard wall.
- **HAProxy backend ssl/verify required when target serves nginx-with-TLS** (feedback): Hugo containers on the new dmz pair serve nginx-with-TLS (8443/TLS via 8456/8457 mapped ports). Plain-HTTP `server` lines from HAProxy got HTTP 400 from nginx → client saw ERR_HTTP2_SERVER_REFUSED_STREAM (000 in curl). Add `ssl verify required ca-file /etc/ssl/certs/ca-certificates.crt` per the cubeos/mulecube/withelli/meshsat backend pattern already in the file.
- **HAProxy named defaults — split http_defaults + tcp_defaults when mixing modes** (feedback): When a single haproxy.cfg has both HTTP and TCP backends (e.g. HTTP frontend + CrowdSec SPOE TCP backend), the HTTP-only options in `defaults` (forwardfor, http-buffer-request) generate ignored-with-warning noise on every TCP backend. The `no option forwardfor` workaround does NOT work — HAProxy 2.x rejects negation for that option. Fix is named defaults (HAProxy 2.4+).
- **Stuck Helm release — use OpenTofu -replace via Atlantis** (feedback): When helm_release is stuck in pending-upgrade/failed, use `atlantis plan -p k8s -- -replace=module.X.helm_release.Y` to destroy+recreate cleanly via GitOps
- **Hook output format — silent allow, plain text deny** (feedback): Claude Code PreToolUse hooks must output NOTHING on exit 0 (allow). JSON outputs like {"decision":"allow"} cause "Hook JSON output validation failed" errors.
- **feedback-html-scraper-first-match-fragile** (feedback): "Any n8n / shell scraper that does `/<tag …attr=…>/i` on raw HTML and uses the FIRST match must whitelist the expected value-shape (path prefix, host suffix, etc), or downstream filters silently zero-out as soon as the source site adds another tag of the same kind earlier in the page. Symptom is invisible: workflow keeps reporting success because no node errored."
- **Hugo {{ $data | jsonify | safeJS }} doesn't escape </script> and raw m.field concat parses as HTML** (feedback): Two coupled XSS-shaped traps in any Hugo shortcode that bakes webhook JSON into an inline script and innerHTML-concats string fields. Caught in kyriakos:layouts/shortcodes/agentic-stats.html 2026-05-11.
- **feedback-instrument-before-the-third-perf-mr** (feedback): "After 2 perf MRs that don't fully close a latency gap, the next MR MUST be observability — not another guess at a fix."
- **IPsec changes must be additive — never replace without asking** (feedback): When adding ISP redundancy to VPS tunnels, ADD new paths alongside existing ones. Never replace/migrate existing working tunnels to a new ISP without explicit approval.
- **IPsec tunnel naming — ISP-specific suffixes** (feedback): VPS strongSwan connections must be named with ISP suffix (e.g., nl-dmz-freedom, nl-dmz-xs4all) for clarity when multiple ISPs exist.
- **feedback_isolate_home_for_classifier_tests** (feedback): "Tests invoking classify-session-risk.py (or any gateway script reading ~/gateway.* sentinels) MUST isolate HOME — host has autonomy-forward ON, clean CI does not, so behavior diverges."
- **JUDGE_BACKEND defaults to local (gemma3:12b)** (feedback): Judge + synth run on local Ollama by default. Flip to Haiku only for KG/policy/meta/specific-incident benchmarking, calibration, or max-effort Opus escalations. Never compare pre/post 2026-04-19 hit rates without stamping the judge source.
- **K8s Strict GitOps Requirement** (feedback): ALL Kubernetes changes must go through OpenTofu + Atlantis MR flow — no kubectl apply, no helm install, no exceptions
- **feedback-let-deploy-pipeline-finish-before-manual-recreate** (feedback): "Don't manually `docker compose pull + force-recreate` on the omoikane.coach dmz hosts right after a post-merge pipeline succeeds. The pipeline auto-deploys; manual force-recreates are redundant work and signal impatience."
- **LibreNMS port-utilisation alarm false-fires on asymmetric DSL/VDSL2** (feedback): On asymmetric DSL lines, Cisco IOS-XE sets BW = min(DS_sync, US_sync); LibreNMS pulls that into ifSpeed; rule 6 then false-fires whenever real traffic exceeds 90% of the LOWER sync rate.
- **LibreNMS REST API exposes no ifSpeed override endpoint** (feedback): The documented LibreNMS REST API has no PATCH/POST route for overriding port ifSpeed. Only update_port_description and update_device_port_notes are writable on ports. To override ifSpeed programmatically, use the WebUI ajax_form.php form, lnms CLI on the LibreNMS host, or direct DB write — not /api/v0/...
- **LibreNMS check_cororings hard-codes expected cluster size in every service** (feedback): Every PVE node has its own check_cororings service with a hard-coded --rings N. Adding/removing a node requires PATCHing N services on both LibreNMS instances.
- **feedback_librenms_env_var_naming_variants** (feedback): "claude-gateway .env has 6 LibreNMS-related vars with overlapping names — use LIBRENMS_GR_API_KEY (GR) and LIBRENMS_API_KEY (NL). Stop guessing _TOKEN."
- **feedback_librenms_poller_stall_signature** (feedback): "Mass-flap pattern (broad ICMP wave + synchronized recovery + NO device-side eventlog) = LibreNMS poller-side stall, not real outage. Don't trigger device-side runbooks before checking the poller."
- **strongSwan needs libstrongswan-standard-plugins for socket support** (feedback): `apt install --no-install-recommends strongswan ...` strips libstrongswan-standard-plugins (a Recommends), and charon then has no socket plugin → silently fails to send/receive IKE. Always include the plugin pkg explicitly when using --no-install-recommends.
- **feedback-litellm-self-heal-on-boot** (feedback): "Any daemon that stores API keys at rest AND pushes them to a separate registration system (LiteLLM, gateway router, vault) MUST reconcile on boot. Operator save-then-restart sequence is racy without it."
- **Log10-of-tokens as flex-grow inside a stacked bar segment misrepresents proportions by orders of magnitude** (feedback): Anti-pattern caught in agentic-stats.html chart. A 1.0B vs 3.1M segment ratio (300x) renders as 1.4x visually. Use proportional grow, not log10.
- **pct exec hang with running status = host I/O starvation, not LXC crash** (feedback): When pct exec into an LXC times out but pct status returns "running" and the LXC's processes are visible in the PVE host's ps output, the cause is host-level memory/IO pressure starving the LXC's kernel scheduling — not LXC corruption. Don't reboot the LXC.
- **Create Matrix bot users via mas-cli inside the MAS container, NOT via Synapse admin endpoints** (feedback): When MAS handles auth, Synapse's `/_synapse/admin/v1/register` HMAC endpoint is correctly fenced off (nginx 404). The right path for bot creation is `mas-cli manage register-user` → `issue-compatibility-token` inside the agriops-matrix-mas container. Saved 2026-04-27.
- **Match defense-in-depth layers to the actual threat model — don't reflex-apply patterns from a different deployment shape** (feedback): When proposing security architecture, anchor on what the actual attack surface looks like, not on patterns memorised from larger/different systems. The operator's pushback on customer-DB tooling 2026-04-27 caught me applying a multi-tenant SaaS pattern to a one-DB-per-tenant system.
- **Max-sub 5-hour rolling rate-limit is account-level (not per-token)** (feedback): Anthropic Claude Max plans have a 5-hour rolling rate-limit window that is SHARED across ALL OAuth tokens for the same account, NOT per-token. Heavy Opus 4.7 1M-context use on one host can deplete the pool for another host's claude-CLI calls. Plan-level monthly quota (claude.ai/settings/usage) is unrelated to this window. Diagnose by querying last-5h llm_usage by model.
- **feedback-mesh-graph-cache-buster** (feedback): "When changing the behaviour of `static/js/mesh-graph.js` (kyriakos repo), you MUST also bump the cache-buster suffix in `layouts/shortcodes/mesh-health.html` (`?v=N` → `?v=N+1`). Browsers cache by URL — same URL with new content does not invalidate the cache, so users see the old behaviour even after Hugo redeploys."
- **feedback-mesh-graph-updatedata-key-shape** (feedback): "When adding code to `mesh-graph.js`'s `updateData(fullData)` that reads keys off fullData, ALWAYS accept BOTH camelCase (initial Hugo-baked __meshData) and snake_case (auto-refresh.js raw /api/mesh-stats payload). Hugo bakes `bgp/dmzNodes/latencyMatrix`; the API returns `public_bgp/dmz_nodes/latency_matrix`. updateData runs every 30s with the snake_case shape — get it wrong and the diagram corrupts state on the first auto-refresh tick."
- **MeshSat Android UX feedback** (feedback): First real-device test feedback — maps broken, SMS permissions too invasive, dark/light theme needed
- **MeshSat E2E verified** (project): First real-device E2E test successful — Pi5 AES-256-GCM encrypted SMS decrypted on Android v1.2.1
- **feedback_meshsat_hub_no_chaos** (feedback): NEVER chaos-kill hub.meshsat.net — fragile Galera MariaDB cluster would corrupt data
- **feedback-migration-filename-hhmmss** (feedback): "SQL migration filenames in omoikane.coach/daemon MUST use actual HHMMSS timestamp, never zero-suffixed placeholders like 000000 or 000100. The full pattern is YYYYMMDDHHMMSS_short_slug.sql."
- **feedback-migration-filename-must-use-real-hhmmss** (feedback): "Database migration filenames must use real wall-clock HHMMSS from date -u +%H%M%S, never round-zero placeholders like _160000 or _000000. Caught 2026-05-26."
- **feedback-minimum-mr-size-1500-loc** (feedback): Operator rule received 2026-05-29 via ccs-01 broadcast — minimum MR size is 1500 LOC. Never smaller unless there is genuinely nothing else to code on the topic.
- **feedback-minimum-scoped-css-diff** (feedback): "When scoping a CSS rule to fix one property's behaviour, scope ONLY that property — not the entire ruleset — or you'll break cascade for visual properties."
- **When mirroring a working production config, mirror it verbatim — don't reinterpret** (feedback): If an existing battle-tested config block exists for the same software (e.g., NL Matrix→defra Matrix), copy the exact structure even when "the docs say either works." The original choices encode lessons.
- **feedback-module-boundary-test-breaks-on-new-files** (feedback): "The REQ-40043 module-boundary drift-lock test in `app/src/outcome_sensor/module_boundary_tests.rs` (`signal_ledger_mentioned_only_in_outcome_sensor_module_or_listed_callers`) fails any time a NEW Rust file under `app/src/` contains the substring 'signal_ledger' — even in a comment. The test enforces an allowlist of file basenames; missing files trigger a deterministic failure. The 4MB CI log cap hides the FAIL line behind ~15000 PASS lines, making diagnosis non-trivial."
- **Always monitor pipelines after a push** (feedback): After every `git push` (any repo, any branch) tail the resulting CI/CD pipeline to completion. Don't treat a successful push as job-done.
- **feedback-mr-branch-naming-single-yt-id** (feedback): "MR branch names MUST be kp/OMOIKANE-XXX-ccs0X-<slug> — single YT id (not hyphenated pair); owner ∈ {kp, elli}; ccs0X is the dispatch session tag. Caught 2026-05-26 — my MR !2517 used kp/OMOIKANE-731-734-ccs02-* which is two YT ids and violates the pre-push hook."
- **feedback-mr-description-in-initial-post** (feedback): "Always include MR description in the initial POST body, never as a separate PUT — avoids the empty-description AIACT-gate race window."
- **feedback-mr-size-target-2000-loc-bundled** (feedback): "Operator's \"why so many MRs\" criticism means \"stop shipping tiny-change 100-300 LOC MRs; bundle multiple related improvements into ONE 1500-2000+ LOC MR\". NOT \"stop shipping\". Caught 2026-05-26."
- **feedback-n8n-expression-mode-and-buffer** (feedback): "Two recurring n8n SSH/command-node bugs that silently break the pipeline — lost '=' expression-mode prefix, and Buffer-not-in-the-expression-sandbox."
- **n8n task-runner sandbox blocks child_process — use SSH nodes** (feedback): n8n Code nodes run inside a sandboxed task-runner that disallows `require('child_process')`. Any subprocess work (sqlite3 lookup, Python invocation, shell pipeline) must move into an SSH node, not the Code node.
- **n8n upgrades can silently break workflow nodes** (feedback): n8n version upgrades may break existing node typeVersions — always audit Switch/If/Filter nodes after upgrade
- **feedback-n8n-workflow-export-format-per-file** (feedback): "When re-committing an n8n workflow export, match THAT file's existing json.dump params or surgically patch it in place — the repo's exports use inconsistent serialization per file, so a naive live-dump churns the whole file."
- **Narrate long-running work** (feedback): Post progress updates between steps when an operation takes more than ~30s or spans >2 tool calls; do not run in silence
- **netplan apply post-eBGP blows netlink timeout** (feedback): After eBGP/full-table is up on a Linux edge VPS, `netplan apply` hangs systemd-networkd ("Could not enumerate links: Connection timed out") because 241k+ kernel routes overflow the netlink-enumeration window. Reboot to apply netplan changes — never apply online once the BGP table is loaded.
- **feedback_never_abbreviate_hostnames** (feedback): "[P0] NEVER abbreviate or truncate a hostname — always the complete site-prefixed name (gr-pve01 not gr, nl-pve01 not pve01). Operator-anger rule, reinforced 2026-06-24."
- **feedback-never-block-request-on-external-llm** (feedback): Never .await an LLM-dispatch call on a synchronous HTTP request path. Always tokio::spawn fire-and-forget + render with partial data; the next render picks up the persisted result.
- **feedback_never_clear_bgp_vps** (feedback): NEVER run 'clear bgp *' or restart FRR on VPS nodes — drops real AS64512 upstream BGP sessions
- **Never install tools on the Proxmox hosts — use the site oversight agent** (feedback): PVE hosts (nl-pve01/02/03, gr-pve01/02) are hypervisors — keep them clean. For Python/netmiko/pexpect/etc., use the site oversight claude agent (nl-claude01 for NL, grclaude01 for GR), which is the intended tool-host with full tooling pre-installed.
- **NEVER modify OOB/lifeline systems without explicit approval** (feedback): Critical lesson — PiKVM was bricked by package upgrades. OOB systems must be treated as untouchable.
- **Never reuse an existing channel-group number when adding a new LACP bundle** (feedback): P0 rule — always `show etherchannel summary` first; reusing an existing Po number modifies the existing bundle's config AND absorbs new members into it, which can cut the production path that the existing bundle carries.
- **NEVER SSH to nl-sw01** (feedback): Do NOT attempt SSH to nl-sw01 (10.0.181.X) — login block-for will lock out ALL management IPs including the operator's workstation.
- **Never truncate or shorten hostnames anywhere** (feedback): STRICT P0 rule. Always full site-prefixed hostnames (notrf01dmz01, notrf01dmz02, nl-pve01, gr-fw01). NEVER dmz01/dmz02/the dmz host/pve01/the asa. No brace-expansion shortcuts in prose either.
- **Never use emojis anywhere** (feedback): STRICT operator rule — never use emojis in any output (text replies, code, docs, plan files, ASCII wireframes, commit messages, UI strings). Use Bootstrap Icons / Material icons / plain text instead.
- **NFSv4 stale-fh diagnostic ladder** (feedback): Order to investigate NFS4ERR_STALE on a Pacemaker Filesystem resource. Distinguishes server vs client vs path vs identity vs ARP fault. Avoids invasive fixes until cause is narrowed.
- **feedback-no-balloon-on-k8s-control-plane** (feedback): "Never run k8s control-plane VMs (kube-apiserver / etcd / scheduler / controller-manager) with an active Proxmox balloon device. etcd is fsync-sensitive; when host pressure causes balloon to reclaim guest memory, etcd's WAL/DB page cache gets evicted → fsyncs become disk-bound → apiserver timeouts → liveness probe HTTP 500 → kubelet kills → restart cycle. Caught 2026-05-15: nlk8s-ctrl01 had 1665 restarts (27 days of crash-looping) because of this."
- **Don't append dated content to CLAUDE.md — use the allocation table at its bottom** (feedback): CLAUDE.md is for stable orientation only. Dated incident summaries go in memory/<name>_<YYYYMMDD>.md. Dated systemic features go in .claude/rules/platform-features.md. Host-specific notes in infrastructure.md. The CLAUDE.md performance threshold is 40 KB — it hit 52.6 KB on 2026-05-06 because dated bullets accreted in Conventions for 6+ weeks.
- **don-t-pad-auto-resolve-numerator-with-dedup-only-writes** (feedback): "Plumbing extra \"dedup\" writes to triage.log at the receiver's isRepeat=true branch (so receiver-deduplicated alerts get counted as resolutions) is an anti-pattern. It drifts the auto-resolve metric from \"incidents resolved\" toward \"events deduplicated\", which are different things. Use incident-based counting instead."
- **feedback_no_double_flock_same_path** (feedback): Don't add a second flock on the same file path inside a function the outer caller already flocked — Linux per-fd flock semantics produce EAGAIN against the same process.
- **feedback_no_emojis** (feedback): NEVER use emojis in any output — code, UI, messages, comments, logs, Matrix, YT. No exceptions.
- **no-foreign-vs-local-in-company-schema** (feedback): "Never carve company-data schemas as \"local\" vs \"foreign\" — every jurisdiction is equally first-class. Use a pluggable RegistryAdapter pattern keyed on (scheme, value)."
- **feedback-no-fragment-prefer-bundled-mrs** (feedback): "Operator strongly prefers bundled MRs that close ≥1 YT child at once over fragmented \"substrate-first scaffold\" sub-phase splits. Caught 2026-05-26 after CCS-02 shipped 17 small MRs in one OMOIKANE-724 lane session."
- **Heredoc-with-pipe in Python is a silent no-op** (feedback): `echo "$VAR" | python3 << 'EOF' ... sys.stdin ... EOF` does NOT pipe $VAR to Python. The heredoc claims stdin, the pipe input is discarded, and sys.stdin is empty. Use `python3 -c '...'` for short code, or pass data via env var (export VAR; python3 << 'EOF' ... os.environ.get('VAR') ... EOF) for longer scripts.
- **feedback_no_human_anchor_for_absent_operator** (feedback): Do NOT build human-in-the-loop tools (labeling, voting, approval polls, manual review) as a PRIMARY mechanism for this system — the operator is deliberately absent for months (autonomy-forward). A "human-label ground-truth path" would just sit empty + fire a stale alert = a NEW dark component. Use no-human anchors instead.
- **feedback_no_internal_pipeline_jargon_in_user_replies** (feedback): Agents speaking to end users must NEVER mention internal pipeline phases, ingestion stages, or system architecture. The user does not care that "Phase F is incomplete" — they care whether the answer is available.
- **No ops/SRE jargon in user-facing text** (feedback): Operator flagged "P0" as opaque. Use plain language ("never-violate rule", "highest priority", "top-priority rule") in messages to the user. Jargon is fine inside memory files and code comments if it adds precision, not in conversation.
- **no-migration-off-pve03** (feedback): Do not propose migrating workloads off nl-pve03 to relieve memory/CPU pressure — solve in-place. Topology of the 5-node pve cluster is intentional.
- **feedback_no_multimb_bash_var_grep** (feedback): "Don't hold a multi-MB blob (e.g. the full Cronicle schedule JSON) in a bash variable and echo|grep it repeatedly — it churns memory and can SEGFAULT under node load. Cache to a file."
- **No public IPs for internal BGP services** (feedback): Never route internal BGP infrastructure traffic over public IPs -- keep everything within the mesh
- **Never embed Python in a heredoc inside `bash -c '...'`** (feedback): Python's `dict['key']` access has single quotes that terminate the outer bash-c single-quoted string. Always extract Python to its own file and invoke it as a regular argv-fed script.
- **feedback-no-shared-main-worktree** (feedback): "Any claude-code agent session that touches omoikane.coach/daemon MUST work in a per-session /tmp/daemon-<purpose> worktree. The canonical clone at /app/websites/omoikane.coach/daemon/ has a shared HEAD across concurrent agent sessions; one session's git checkout shifts HEAD out from under others."
- **Allowlist lookup miss must hard-error, never silently empty-pass** (feedback): When a backend builds a target list by iterating user input through an allowlist dict (CHAOS_TUNNELS.get(tk)), missing keys must produce an explicit error response — not silently truncate the list. Silent truncation produces "active" responses with empty payloads that the frontend's `if (.length)` checks misinterpret as "no kills happened, that's fine."
- **feedback_no_static_routes** (feedback): NEVER add static routes on ASA or anywhere — ALL inter-site routing is BGP
- **feedback-no-sudo-install-on-pve-hosts** (feedback): "Don't reach for `sudo` (install it OR sudo-prefix invocations) to bridge a permission gap on a PVE host. The Proxmox-staff position is \"sudo is not the right way to implement unprivileged services\" (Fabian Grünbichler). All 6 NL+GR PVE hosts in this estate happen to have sudo installed from legacy setup, but new fixes should use PVE-native patterns (cache-pattern cron for snmpd extenders, pveum ACLs for delegated admin, system systemd units for services)."
- **Don't estimate time/duration; group work by logical cohesion, not time-bound buckets** (feedback): I generate hour/day/week estimates that have no calibrated basis ("~3 hours", "9-12 weeks for full parity", "Day 8 closes here"). Operator corrected this 2026-04-27 after I'd been calling 1-3 hour focused sessions "Days". Stop generating duration estimates entirely; group by natural feature cohesion.
- **no-zramswap-on-pve-hosts** (feedback): Do not propose or apply zramswap (or any swap) on PVE hosts as a memory-pressure remediation — Proxmox does not officially recommend swap on hypervisors. Find another lever.
- **OCF docker start/stop timeout must match real container boot time** (feedback): ocf:heartbeat:docker default 90s start/stop timeouts cause Pacemaker fence escalation when slow-booting containers (Node-RED, HA, Z2M, ESPHome) miss the deadline. Always size start/stop timeouts to ≥ p99 boot time + margin.
- **Ollama runs in Docker on nl-gpu01** (feedback): Ollama CLI is not on $PATH on nl-gpu01 — it runs as a Docker container under /srv/ollama. Use `docker exec` for ollama CLI operations.
- **Ollama "model failed to load" — check host RAM + OOM-killer FIRST, not GPU VRAM** (feedback): When Ollama returns "model failed to load, this may be due to resource limitations", the cause is most often host RAM + OOM-killer killing the model-loader worker mid-load — NOT GPU VRAM. Check `dmesg`/`journalctl` for "Out of memory: Killed process … (ollama)" before nvidia-smi.
- **Always set per-request num_ctx when using Ollama on nl-gpu01** (feedback): OLLAMA_CONTEXT_LENGTH=65536 global on nl-gpu01 forces huge KV cache for every model, causing tiny models (1B-7B) to spill to CPU. Always pass num_ctx in per-request options.
- **feedback-omoikane-daemon-users-id-is-bytea** (feedback): "In omoikane.coach/daemon, `users.id` is BYTEA (raw UUID bytes), NOT UUID. Any new migration that adds `user_id` FK to `users(id)` MUST declare it `BYTEA NOT NULL`. Rust side keeps `uuid::Uuid` but binds via `.as_bytes().as_slice()` and reads via `Uuid::from_slice(&bytes)?`. Pattern lives in `app/src/fairness_audit_consent.rs` read_consent + write paths."
- **feedback-omoikane-p1-cargo-test-and-frontend-developer** (feedback): Two P1 rules for any claude-code session that touches omoikane.coach — cargo test --locked before push + invoke frontend-developer skill on any UI/UX surface.
- **feedback-omoikane-p1c-env-var-tunables** (feedback): Tunable operational parameters in omoikane.coach/daemon MUST be env-var-overridable with sensible defaults. Hardcoding tunables forces a 5-15 min CI+deploy cycle per adjustment vs ~30 sec docker restart for env vars.
- **OpenClaw deploy checklist** (feedback): When modifying OpenClaw skill scripts, ALWAYS SSH to nl-openclaw01 to verify and sync ALL related files — not just the one you changed.
- **OpenClaw SSH Access Pattern** (feedback): How to SSH to OpenClaw LXC for configuration changes — direct SSH, NOT pct exec
- **feedback-operator-does-not-watch-matrix-polls** (feedback): "The operator does not watch the Matrix approval polls; the gate is configured human-as-circuit-breaker (autonomy-forward), not gatekeeper."
- **feedback_operator_out_of_loop_complete_dont_defer** (feedback): The operator is OUT OF THE LOOP by design — never end a task by handing a completion step back to them ("the one remaining step is yours"). Complete it myself, including atlantis apply + k8s verification, and report back AFTER it's verified done.
- **NFS exports managed by Pacemaker have no /etc/exports** (feedback): On clusters where exports are provisioned by an `ocf:heartbeat:exportfs` resource, NEVER run `exportfs -r` to refresh — it sees an empty /etc/exports and silently unexports everything. Use `pcs resource restart exportfs` instead.
- **When fixing cron-env gaps, pair PATH + .env-sourcing** (feedback): A script run from cron has TWO common environment blind spots — missing PATH (no /usr/local/bin) and missing env vars (no .env source). Fixing one ≠ fixing the other. When patching either dimension, audit the other in the same commit.
- **Diagnose multi-replica services per-pod, not via the load-balanced service** (feedback): When a service has 2+ pods (filer, db, etc.) AND its symptom is intermittent (some reads 200, some 404), check each pod's state independently before drawing conclusions. The load-balanced service hides per-pod divergence.
- **feedback_pgrep_self_match_in_monitors** (feedback): "A wait-loop that polls `pgrep -f <script>` to detect a background job finishing will NEVER exit — its own command line contains `<script>`, so pgrep matches ITSELF. Caused me to misreport \"QA still running\" for ~6 turns."
- **feedback-playwright-force-refresh-before-push** (feedback): "Before pushing any JS change that lives inside an auto-refresh / poll callback, run Playwright with a forced manual invocation of that callback — natural polling intervals are too long to wait through, and a ReferenceError in the callback is invisible at page load."
- **Portfolio page must be updated alongside major default flips** (feedback): When landing a cross-cutting change (judge/synth backend, model defaults, dashboard counts, skill counts), update kyriakos.papadopoulos.tech/projects/agentic-chatops/ the same day. CLAUDE.md alone is not enough.
- **PPPoE Discovery failure is never an MTU/MSS issue** (feedback): When PADI/PADO fails, do not chase MTU 1500 vs 1492 vs MSS clamping 1448 — Discovery is L2 pre-IP and MSS only applies to TCP segments inside an up PPP session
- **feedback-preserve-row-layout-on-status-diagram** (feedback): "On the kyriakos status-page network diagram, the operator's strong preference is to preserve the single-horizontal-transit-row layout above the upstream bubbles. Do NOT invent fan / radial / per-upstream-arc layouts even when more transits need to fit. Adjust constants (row y, spacing, count cap) within the existing single-row layout instead."
- **feedback-prom-describe-needs-boot-sentinel** (feedback): "2026-06-03 OMOIKANE-1117. metrics-crate describe_counter! alone does NOT make HELP appear on /metrics — the Prometheus exporter omits any series that has never been incremented. EVERY new counter MUST also emit one zero-value sentinel sample with label=\"boot\". Pattern is documented as [[feedback-goal-rationale-prom-histogram-empty]] but I missed it on MR-8c/MR-8e/MR-10/MR-11 across the Amendment B chain; cost MR-13 to fix four modules in one go. Sentinel labels must NOT collide with any real-traffic label value, and operator PromQL MUST filter `{label!=\"boot\"}`. Canonical reference module: `app/src/metrics_outcome_sensor_consent.rs::register()`."
- **When asked for a prompt, scope it strictly to what was asked** (feedback): Operator asks for "a prompt to do X". Deliver one prompt that does X and nothing else. Do not bundle adjacent work, parallel findings, or roadmap items I happened to know about from earlier in the conversation.
- **feedback-pve-balloon-zero-needs-reboot** (feedback): "On Proxmox, `qm set <vmid> --balloon 0` does NOT live-remove the QEMU balloon device — it queues a [PENDING] config change for the next VM restart. The active config + the running balloon device stay in place. Force-deflating via `qm monitor balloon <mb>` is temporary; pvestatd auto_balloon will re-inflate within ~10 min based on the still-active config. To truly disable balloon, run `qm set --balloon 0` AND reboot the VM."
- **PVE-clone leaves snmpd.conf sysName pinned to the source host** (feedback): When a Proxmox node is cloned from another, /etc/snmp/snmpd.conf retains the source's sysName, blocking LibreNMS device add with "duplicate sysName"
- **feedback-pve-lock-backup-with-fleecing-image** (feedback): "PVE 8.2+ aborted-vzdump leaves stale `lock: backup` + `[special:fleecing]` block + orphan fleecing qcow2 — diagnostic signature, why it stays invisible until restart, recovery sequence"
- **PVE Maintenance Real-Run Lessons** (feedback): Issues found during first real GR maintenance (2026-03-26). Must fix before NL run.
- **feedback_pve_mgmt_wedge_pmxcfs_restart** (feedback): A PVE management wedge (pvestatd D-state, status=unknown, load high but CPU idle) is usually a hung pmxcfs (/etc/pve) — fix WITHOUT reboot by restarting pve-cluster FIRST (releases the D-state), then pvestatd. Don't restart pvestatd alone.
- **feedback-pve-root-extender-cache-pattern** (feedback): "For any LibreNMS / snmpd extender on a PVE host that needs root-only access (e.g. `/etc/pve/priv/authkey.key` for the `proxmox` app), DO NOT use sudo-prefix or www-data group hackery. Use the PVE-native cache pattern that `/etc/snmp/smart` already uses: cron writes data to /var/cache, snmpd just `cat`s it."
- **feedback-raw-input-ratchet-bump-pattern** (feedback): "When adding ANY new `<input ...>` tag to a Maud view in app/src/views/**.rs, the `views::a11y::g8_slice_2_raw_input_ratchet::raw_input_count_does_not_exceed_baseline` test will fail and block ALL downstream MRs from passing CI until you bump the baseline."
- **"Remove X completely" after operator flags X as misleading = remove the whole widget** (feedback): When the operator says "remove it completely" after explaining why a piece of UI is misleading or wrong, default to removing the entire row/bar/widget that contains the offending element — not just the sub-span the original complaint pointed at. Saves a second round-trip and matches the operator's actual intent.
- **Re-read CLAUDE.md before concluding when finding contradicts it** (feedback): When a live-system probe seems to contradict an architectural claim, re-read the relevant CLAUDE.md section verbatim before concluding the doc is outdated or wrong. The contradiction usually means I'm querying the wrong device.
- **Never combine `interruptible: true` with `resource_group` on the same GitLab CI job** (feedback): When auto_cancel transitions an interruptible job to `canceling`, it holds the resource_group lock indefinitely until the runner finishes tearing down — which can stall mid-apt or mid-clone for tens of minutes, blocking every other pipeline using that lock.
- **Right-size Bash tool timeouts to expected work, not worst-case** (feedback): Don't pad timeouts "just in case." Pick a realistic upper bound for the operation. Long-running migrations get dedicated runs; routine compose+verify scripts stay under 60-90s.
- **feedback-runtime-env-wiped-by-awx-deploy** (feedback): "Any env var added at runtime directly to /srv/omoikane-daemon/secrets/shared.env on the dmz hosts will be WIPED on the next AWX deploy. scripts/sops/decrypt-shared.sh decrypts the canonical secrets/shared.env.encrypted and OVERWRITES the file — runtime additions are not preserved. For persistent env vars, the only safe path is to add them to the sops canonical source via `sops --input-type dotenv --output-type dotenv`, commit, push, deploy."
- **Security scan report — known-noise filter for our topology** (feedback): Findings in weekly scanner reports (nlsec01 / grsec01) that are ALWAYS false positives or baseline-acceptable on our specific stack. Apply before triaging.
- **sed -i breaks single-file Docker bind mounts — restart container or write in-place** (feedback): When a Docker bind mount targets a single file (not a directory), sed -i creates a new inode and the container keeps reading the old one. nginx -s reload won't help. Restart the container OR use cat/tee to write in place.
- **session_log dead post-cc-cc (use sessions.duration_seconds for closure)** (feedback): After 2026-04-29 cc-cc cutover, gateway.db `session_log.ended_at` stops being written. Don't use it for lifecycle/SLA queries.
- **feedback_show_progress_during_long_waits** (feedback): Always show visible progress feedback (animation, spinner, "thinking" bubble) when user-facing operations take >2-3 seconds. Plain status text isn't enough.
- **feedback-silent-return-false-in-worker-loops** (feedback): "Any `return false` / silent skip path in a periodic-worker tick that ALSO writes to `last_attempt_at` BEFORE the gate creates an invisible cooldown lock-out. Must pair with a tracing::warn + Prom counter, or operators can't tell why throughput stalled."
- **Single Operator Context** (feedback): User is a single person managing 310 infra objects (113 devices + 197 VMs) across 6 sites — chatops efficiency is critical, not a nice-to-have
- **feedback_sonos_volume** (feedback): Sonos/Squeezebox speakers are loud — 15% is the standard volume for everything. TTS firmware uses 10%. Never above 20%.
- **feedback-sops-persist-via-dmz-host** (feedback): "2026-06-01 (CORRECTED). omoikane.coach/daemon sops-encrypted source for shared.env is in the daemon GIT REPO at secrets/shared.env.encrypted. AWX template 78 SCPs it to each dmz host on every deploy. Host-side edits to either shared.env.encrypted OR shared.env are ALWAYS overwritten. Persist via MR — NOT via on-host edits."
- **SQLite writers on gateway.db need busy_timeout** (feedback): Any script that writes to gateway.db while other writers are active must set busy_timeout=30s + order user-visible side effects before the final DB write
- **feedback_sqlite_wal_transition_lock_race** (feedback): "SQLite lost-update race — PRAGMA journal_mode=WAL on every connect races the fresh-DB DELETE->WAL transition under concurrency, throwing a transient 'database is locked' that (if it fires before the txn try-block) is unretryable -> silent lost write. Fix = busy_timeout-first + guard/tolerate the WAL switch + retry the whole transaction."
- **feedback-sqlx-fromrow-struct-drift-silent-404** (feedback): When a
- **feedback-sqlx-migration-row-mismatch-fix-forward** (feedback): "When daemon crash-loops with `migration X was previously applied but is missing in the resolved migrations`, the cause is usually a parallel-pipeline race where an older image got deployed AFTER a newer one ran the migration. Fix-forward: DELETE the migration row from _sqlx_migrations, preserve the data columns, force-recreate. Next deploy with the newer binary re-applies via idempotent ADD COLUMN IF NOT EXISTS."
- **Use SSH ControlMaster/ControlPersist for any externally-routed VM with DDoS auto-mitigation (defra01agri01 + similar)** (feedback): Each `ssh user@host 'cmd'` opens a fresh TCP+SSH session. Hundreds of these in a few hours from one source IP look like a flow-volume DDoS to upstream auto-mitigators (ifog FastNetMon at minimum). Multiplex with ControlMaster so all commands share one connection.
- **After restoring a backend, probe every static-site consumer for staleness** (feedback): HTTP 200 on a webhook is NOT proof the user-visible page is fixed when a static-site generator consumes the webhook at build time. Static HTML baked with fallback during the outage will still be served. Probe the HTML, find the rebuild pipeline, trigger it, verify via Playwright.
- **Status-page detail goes in the graph, not the banner text** (feedback): For /status/ portfolio page, failure detail belongs in per-element visualization (e.g. red D3 line on the specific broken tunnel), not spliced into the compound banner. Banner stays one line.
- **feedback_subagentstop_hook_schema** (reference): "Empirically-captured stdin schema of Claude Code's SubagentStop hook (official docs omit it). Provides agent_type + the sub-agent's own transcript path + its final message — ideal for wiring per-agent persistence like agent_diary."
- **Substring grep for keywords needs explicit negation filter** (feedback): Case-insensitive substring grep for keywords like VULNERABLE, ERROR, FAILED, BAD, BROKEN matches their negations too. Always pipe through a `grep -v` filter for the negation, or use word boundaries / lookbehinds.
- **Wrap `nohup … &` SSH-launched processes in systemd-run --user --scope** (feedback): n8n SSH nodes that launch background processes via `nohup … &` leak abandoned session scopes under user-0.slice (systemd-logind can't finalize scope when child stays in cgroup). Fix: prefix the nohup'd command with `systemd-run --user --scope --slice=app.slice --unit=…` so the child is placed in a separate cgroup (user@1000.service/app.slice) outside the SSH session.
- **systemd units must declare Restart=** (feedback): Bare-bones systemd units with Type=simple + no Restart= directive are footguns — any OOM/crash → permanent outage until manual intervention. Audit every *.service we install or import; add Restart=on-failure + StartLimitBurst.
- **feedback-systemd-user-slice-oom-score** (feedback): "systemd `--user` (per-user) services run with `oom_score_adj=200` by default. That makes them the kernel's preferred OOM-killer victims even though their RSS is tiny. Critical-path services (Tier-1 SMS, paging, escalation bridges) MUST NOT run as `--user` systemd units — they will die first during any host-pressure event, exactly when you need them most."
- **feedback-time-offset-replace-month-order** (feedback): "When constructing a fixed `time::OffsetDateTime` via `.replace_*()` chain, set day BEFORE month. `replace_month()` propagates the current day-of-month, so May 31 → June fails with ComponentRange because June 31 doesn't exist."
- **feedback_url_bracket_autolink_bug** (feedback): Markdown autolinkers (marked.js, GFM) include trailing punctuation in URLs when the URL appears inside `[...]`, `(...)`, etc. Always emit URLs as proper markdown link syntax `[text](url)` — never bare URLs inside text-brackets.
- **feedback_use_api_not_direct_db** (feedback): "NEVER write directly to a service's database (or generated config files) when a management API exists — use the API; direct DB writes corrupt clustered/replicated state and get regenerated away"
- **Use real n8n execution data (not synthetic fixtures) for parser regression testing** (feedback): Synthetic test fixtures miss formatting variants Claude actually emits. The n8n executions API exposes the exact `result` string that fed Prepare Result for every past run — replay those through the patched code to find regressions you'd never have synthesized.
- **feedback_verify_agent_generated_doc_claims** (feedback): "When subagents write/update docs from a prose brief, they fabricate plausible-but-false SPECIFICS (a non-existent section number, a stale count) that read as authoritative. Always verify agent-generated factual claims against the live system before committing."
- **feedback_verify_belief_not_rationalize_observation** (feedback): "When an observation contradicts what you \"know\", the BELIEF is the suspect — verify it, don't invent a cause to defend it. Never attribute a cause to a host without the full site-prefixed hostname + a confirming fact (uptime/log), especially across same-named hosts."
- **feedback-verify-rebase-conflict-resolution-via-git-show** (feedback): "After resolving merge conflicts during git rebase, ALWAYS verify the committed file with `git show HEAD:<path>` before pushing — Edit tool failures during conflict resolution can silently leave conflict markers in the commit. Caught 2026-05-26 on OMOIKANE-730 Phase 6c (commit bfee072c)."
- **feedback-visual-audits-must-render-not-grep** (feedback): "Token-based HTML audits (grep for class names, ARIA, form-element counts) miss missing-framework-wrapper bugs. Visual audits MUST render the page in a browser + diff against a known-styled reference page."
- **Never call subnets by their third octet as a "VLAN" [P0]** (feedback): P0 rule. Do NOT call 10.0.181.X/24 "VLAN 181". The actual VLAN tag is 10 (inside_mgmt). The third octet of the IP is not the VLAN ID. Use either the VLAN name (inside_mgmt) or the actual tag (VLAN 10) — never the subnet octet.
- **feedback-vps-onboarding-addpath-mirror** (feedback): "When adding a new VPS RR-client to an FRR route reflector, also mirror addpath-tx-all-paths from a sibling VPS peer — otherwise the new VPS silently becomes a best-path-only iBGP citizen."
- **VPS SSH access pattern** (feedback): SSH to NO/CH VPS as operator with one_key. Sudo requires password. strongSwan needs sudo.
- **feedback_wedged_iscsi_mount_needs_pod_reschedule** (feedback): "A k8s pod crash-looping long-term with a storage/EIO error on an iSCSI (democratic-csi) RWO volume usually means a WEDGED mount — fix by deleting/rescheduling the POD (force detach+reattach), not by letting the container restart"
- **feedback-workflow-chunk-concurrency-throttle** (feedback): Workflow tool fan-out at full concurrency trips a server-side throttle; chunk to <=4 + retry-once
- **feedback-workflow-fanout-rate-limit-batch-resume** (feedback): "A workflow that fans out many agents at once (parallel/pipeline over N items) can get the WHOLE batch killed by a transient server-side rate limit ('Server is temporarily limiting requests · not your usage limit'). Fix = batch the fan-out into small groups + resume (cached prefix preserves the expensive earlier phases). Guard the final return against a null synthesis agent."
- **feedback-youtrack-autocreate-authorized** (feedback): "Operator has authorized auto-creation of YouTrack epics and child issues via the REST API for approved plans, for current and future sessions. Don't draft-only when the plan calls for work-tracking issues."
- **YouTrack MCP state-update bug — use direct REST instead** (feedback): tonyzorin/youtrack-mcp:latest update_issue_state and update_custom_fields fail on StateIssueCustomField with "Unknown workflow restriction" — bypass via direct REST POST /api/issues/{id}.
- **YouTrack state transitions use command API** (feedback): YT MCP update_issue_state fails on workflow restrictions. Use curl POST /api/commands with "State Done" instead.
- **feedback-yt-duplicate-close-auto-propagates** (feedback): "When closing a YouTrack issue with a \"duplicate of X\" comment that explicitly names X, YT workflow rules can auto-propagate the Done state to X (the parent). After any mass-close that involves duplicate-naming, re-verify the state of every parent issue you intended to keep open."
- **feedback-zenodo-prereserve-doi-for-draft-deposits** (feedback): "Zenodo's POST /api/deposit/depositions returns the DOI at metadata.prereserve_doi.doi for unsubmitted drafts, NOT at the top-level doi field. Top-level doi only appears post-publish."
- **feedback-zfs-dio-diagnostic-recipe** (feedback): "3-command diagnostic recipe to confirm OpenZFS 2.3 dio_verify_wr race when a PVE VM is paused with io-error. The QMP \"I/O status nospace\" label LIES about the real cause — check zpool events first, then QEMU drive opts, then the ZFS direct property."
- **feedback-zfs-dio-must-be-disabled-on-pve** (feedback): "On any PVE host running OpenZFS 2.3+ with VMs using cache=none qcow2/raw on a ZFS dataset, ALWAYS set direct=disabled on the pool. Otherwise dio_verify_wr races between guest buffer mutation and ZFS DMA verify will pause VMs at random with bogus \"nospace\" io-error."
- **nl-claude01 lacks `zip` — use Python zipfile** (feedback): The `zip` CLI is not installed on nl-claude01. Use Python `zipfile.ZipFile` directly when assembling archives. `unzip` may also be missing — verify with `which unzip`.
- **freedom-ont-drill-trigger installer — manual, one-shot** (reference): Who schedules the Freedom-ONT failover drill, and why it's manual-by-design rather than cron/AWX-driven
- **Freedom ONT (Genexis XGS-PON) requires forced PoE re-detect after long down** (feedback): Plain `no shutdown` on nl-sw01 Gi1/0/36 after a long Freedom-down window is not enough — the ONT loses PON training and needs `power inline never` → `power inline auto` + `shut`/`no shut` to cold-boot cleanly. 2026-04-22 recovery exercise.
- **freedom-pppoe-outage-resolved-20260513** (project): "Post-mortem of the 4d 15h Freedom XGS-PON PPPoE outage on nl-fw01 (line F0381391). Down 2026-05-08 09:46:36 UTC (BRAS stopped returning PADO), restored 2026-05-13 ~00:00 UTC after Freedom NOC field-engineer dispatch + OLT/BRAS service-binding refresh. Failover via outside_budget → nlrtr01 Dialer1 carried all 6 affected VTIs with zero user-visible downtime."
- **freeipa01-httpd-scoreboard-outage-20260529** (project): "2026-05-29. nlfreeipa01 webui unreachable for 5d — httpd mpm_event scoreboard full since Sun May 24 00:00:11. Fixed short-term by LXC reboot. Two reusable findings — IPA host missing from LibreNMS (silent), chronic MPM tuning gap."
- **freeipa_admin_keytab_access_20260623** (reference): The agentic system has STANDING passwordless FreeIPA admin access via the svc-claude-gateway keytab — run any ipa op with scripts/ipa.sh; never ask the operator for IPA creds again
- **frigate_doorbell** (project): Frigate NVR (nlfrigate01) and n8n doorbell workflow — architecture, auth, JWT fix, integrations
- **gateway_governance_branch_is_active_wip_20260624** (project): "The claude-gateway working copy sits on local branch fix/governance-autodemote-autonomy-forward, which holds ACTIVE unmerged WIP (google-5day benchmark + intentional unified-guard disable) — do NOT clean/reset/rebase it blind."
- **GitHub Public Mirror — agentic-chatops** (project): Auto-synced public mirror at papadopouloskyriakos/agentic-chatops. CI pipeline sanitizes 99 patterns + gitleaks on every push to main.
- **GitHub PATs for agentic-agri repos (operator-authorised storage)** (reference): Two PATs stored at operator's explicit instruction 2026-04-27. One long-lived for papadopouloskyriakos/* repos, one one-time for the AGRI_JSON_proc seed clone.
- **GitHub Public Mirror Sync (IaC repo)** (project): IaC repo GitHub sync pipeline — paths, sanitization rules, runner image rebuild process. Second mirror (claude-gateway → agentic-chatops) added 2026-03-24, see github_mirror_chatops.md.
- **GitHub mirror sync chain hardening (2026-05-01)** (project): Three classes of failure in claude-gateway sync_to_github pipeline closed in one session. Reference for the gitignore-clean fix, GIT_STRATEGY=empty bypass, and resource_group serialization with the interruptible deadlock workaround.
- **GitLab Redis Disk-Full Outage 2026-05-08** (project): nl-gitlab01 root volume hit 100% → Redis BGSAVE failed → MISCONF read-only → all gitlab writes returned 500 → 3 daily AWX cert jobs failed at started=null because their project_update returned HTTP 500 on git fetch
- **gitlab_runner_topology** (project): NL + GR internal GitLab each have ONE online runner with run_untagged=false. NL failure mode = saturation (queued_duration>30m). GR failure mode = tag-less jobs hang 2h → stuck_or_timeout_failure.
- **golden_ratio_chaos_20260414** (project): Golden ratio chaos analysis -- max 4 tunnel kills (47/63 safe). Graph connectivity BFS in backend+frontend. 6 new catalog scenarios. Recovery OOB gap fixed. OpenClaw E2E wired. 6 YT issues closed.
- **google-5day-agents-benchmark-20260622** (project): 2026-06-22 IN-PROGRESS — adding Google 5-Day AI Agents course as the 10th benchmark source; 2 reports being generated by a resumable workflow
- **governance_chain_broken_test_pollution_20260627** (project): GovernanceChainBroken SMS (2026-06-27) = BENIGN test pollution, not a tamper. test-risk-integration.sh ran against the LIVE gateway.db, writing+deleting INTEGRATION-TEST audit rows → broke the SHA-256 chain. Healed by re-baseline; root-fixed by isolating the test to a temp DB (MR !107).
- **gpu01_daily_reboot_rca_20260629** (project): "RCA for nl-gpu01 alert churn (1403 rebooted / 1465 ProactiveDiskPressure / 1467 RAGRerankServiceDown). 2 deliberate reboot mechanisms (daily cron + ollama-nvml-selfheal.sh), stale post-2026-05-14 ZFS-DIO fix (held, 0 errors). Disk 80%/168GB reclaimable. Reranker+ollama healthy; alerts were transient post-reboot. Prior 06-27 triage missed it."
- **gpu01-freeze-qcow2-io-error-20260512** (project): "nl-gpu01 (PVE VM VMID_REDACTED on nl-pve03) freeze RCA — symptom is QEMU paused state=\"io-error\" / I/O status=nospace on scsi0, not a guest-side kernel freeze. Root cause = qcow2 cluster exhaustion because disk was created without discard=on. Fixed with discard=on + fstrim + RAM bump 28 → 32 GiB."
- **gpu01-nvml-stale-handles-20260514** (project): "nl-gpu01 CPU hammer (load 18-19, ollama 1277% CPU for 10h) — container's bind-mounted /dev/nvidia* nodes went stale after nvidia-persistenced restart at 07:01:54 CEST cycled the host device nodes. Different failure mode from 2026-05-13 (VRAM shortage). Fix: `docker restart ollama`. Hardening pending."
- **gpu01-zfs-dio-race-root-cause-20260514** (project): "nl-gpu01 (VM VMID_REDACTED) \"frozen daily with io-error\" was OpenZFS 2.3 dio_verify_wr race with QEMU cache=none/aio=io_uring. Fixed by setting direct=disabled on rpool across all 6 PVE hosts (NL: nl-pve01/nl-pve03/nlpve04, GR: gr-pve01/gr-pve02) + zfs_dio_enabled=0 modprobe.d safety net."
- **gr_chatops_infra** (project): GR site (gr) ChatOps infrastructure — complete multi-site alert pipeline, triage scripts, n8n workflows, kubeconfig, LibreNMS transport, Alertmanager webhook
- **GR Claude Agent (grclaude01)** (project): Claude Code agent at GR site for NL maintenance oversight. VMID 201021201, 10.0.X.X, gr-pve01.
- **gr_grk8s-ctrl01_etcd_gr-pve01_saturation_rca_20260623** (project): "2026-06-23 RCA of the GR etcd disk-I/O cascade behind the 91-SMS storm — root cause is chronic gr-pve01 host saturation + a thermally-throttling (SMART-clean, NOT failing) rpool mirror disk nvme2n1, NOT a ctrl01-specific fault. See VERIFIED UPDATE 2026-06-24 — the disk is overheating, not degraded; fix=cooling not replacement."
- **GR iSCSI Server (gr-pve02)** (project): GR K8s iSCSI storage — ZFS zvols on PERC H710P, architecture, tunables, AWX PVC fix, SeaweedFS migrated to NFS/sdc
- **GR Prometheus can't scrape VPS edge exporters — path asymmetry vs NL** (project): GR Prom gets `context deadline exceeded` scraping http://10.255.{2,3}.11:9342 + :9536 (frr + ipsec exporters on CH + NO VPS). NL Prom scrapes the same URLs fine. Surface alert: TargetDown × 2 on GR. Root-cause ticket IFRGRSKG01PRD-200. Filed 2026-04-22.
- **Grafana dashboards fully IaC-managed + Element-X widget mapping** (project): All 10 dashboards IaC-managed (MR !245/246/248). 4 embedded in Element-X Matrix rooms with timepicker hidden. Atlantis auto-plan needs manual trigger for JSON-only changes.
- **grafana_sidecar_oom_hardening_20260624** (project): "2026-06-24 NL grafana sidecar OOM-hardening: NEVER set a memory LIMIT on the grafana sidecars (they load all dashboard configmaps at startup → spike). Request-only. Plus helm timeout 300→600. I caused a grafana outage by rushing."
- **grvmorpheus-stuck-lock-backup-20260513** (project): "grvmorpheus (VMID 201061601) stuck `lock: backup` + dangling fleecing qcow2 from pre-Apr-16 aborted vzdump — RESOLVED 2026-05-13, ~244 GiB reclaimed, VM running"
- **grskg_mass_flap_20260511** (project): "GR site mass flap 02:08-02:14 UTC 2026-05-11 — 16 devices ICMP-down. RC CONFIRMED: gr-nms01 chronically over-budget poll cycles (518s vs 300s) + IO starvation (PSI io.full avg300=7.32%) + 6 concurrent zombie poller-wrapper.py + cororings device 9 pushed past cliff by nlpve04 6th-node add 2026-05-10."
- **HAHA chaos engineering catalog 2026-04-30 (~14 tests, 2 bugs surfaced+fixed)** (project): Same-day chaos engineering pass over the whole IoT infrastructure (HAHA + FISHA + sidecars + voice pipeline + cluster fencing). 14 tests run, 2 real bugs surfaced and 1 fixed (nodered start timeout 90s→180s); 1 outstanding (fence_pve list TypeError, IFRNLLEI01PRD-806). Empirical confidence table inside.
- **HAHA reliability hardening 2026-04-30 (Phases 1-5 implemented)** (project): Same-day follow-up after the 2026-04-27 → 2026-04-30 ~66h HAHA outage. App-level OCF docker monitor_cmd, NFS auto-flush, NFS stale-fh exporter, proactive ARP, host-pressure alerts, Twilio escalation. T1 e2e verified: 18s detect, 3m30s recover.
- **haha_voice_pe_upgrade** (project): HA Voice PE firmware — v7 working (v6 upstream + Squeezebox routing), Ollama q4_0 fix, REST sensors FIXED, 2026-03-16 audit fixes
- **health_audit_20260629** (project): "2026-06-29 agentic-system + orchestrator health audit. System GREEN (holistic 93%, 0 fail); 1 open item nlk8s-ctrl02 saturation; HolisticHealthFailing flaps; 3 empty tables=dormant features; audit methodology."
- **health_audit_24h_20260627** (project): 24h health/before-now audit after the orchestrator+convergence work — found+fixed monitoring debt MY OWN changes created; core pipeline healthy.
- **healthchecks_langfuse_access_20260626** (reference): "STANDING access for the orchestrator brick-growth services — Healthchecks.io (ping-based never-ran detection) + Langfuse v2 (LLM/agent trace observability), both self-hosted Docker on nlopenobserve01. Operator-approved 2026-06-26."
- **Holistic agentic health script — 100% pass (78/78)** (project): scripts/holistic-agentic-health.sh tests ALL README-claimed features across 20 sections. First run 91% (5 fixes needed), second run 100%.
- **hostname_deabbreviation_sweep_20260624** (project): 2026-06-24 estate-wide hostname de-abbreviation — all docs across the 4 IaC-relevant repos to 0 bare, verified + merged; guard + CI gate + bidirectional [P0] rule shipped
- **ibgp_full_mesh_fix_20260413** (project): iBGP full mesh routing fix (2026-04-13). next-hop-self force + BFD + table-map SET_SRC. 18 baseline experiments validated. ASA 9.16 limitations documented. Needs IaC sync.
- **iFog (CH VPS AS34927) drops raw ESP — strongSwan needs encap=yes** (project): CH VPS xfrm tunnels to peers using plain ESP (IP proto 50) get silently dropped inbound by iFog. Force NAT-T via `encap = yes` in swanctl so ESP rides UDP:4500.
- **Corosync cluster split incident 2026-04-11** (project): PVE 5-node cluster split — stale ASA conn table routed nl-pve01 knet via outside_freedom instead of VTI. Fixed by clear conn + timeout floating-conn on both ASAs.
- **incident_grdmz02_oom_shun_20260413** (project): grdmz02 OOM-killed twice on 2026-04-13, ASA shunned DMZ IP 10.0.X.X causing total network loss
- **DMZ disk-full pipeline break + resize to 128G + cleanup cron 2026-04-17** (project): gr-dmz01 / 100% full blocked Ansible tmp-dir creation, producing recurring UNREACHABLE across all portfolio deploy pipelines. Resized both DMZ VMs 64->128G and installed daily cleanup cron.
- **Freedom ISP PPPoE Outage 2026-04-08** (project): Freedom PPPoE outage → full remediation session. 5 phases: GR VPN restoration, VPS migration, NAT parity, grdmz02 TS fix, operational readiness. Dual-WAN parity achieved. QoS + SMS + triage training.
- **GR Site Isolation 2026-04-17 (stale IPsec SA)** (project): NL↔GR VTI/BGP break 2026-04-17 ~05:23 UTC. Root cause = stale IPsec SA on Tunnel4 (Freedom VTI). Fix = `clear crypto ipsec sa peer 203.0.113.X` on NL ASA. Resolved 08:48 UTC.
- **HAHA NFS stale-fh outage 2026-04-27 → 2026-04-30 (RESOLVED, ~66h 39m)** (project): Home Assistant down 2026-04-27 14:55 → 2026-04-30 09:34 UTC (~66h 39m). HA Python crashed with Bus error during nfs-group migration; container kept running so Pacemaker never noticed. Apr 30 02:15 weekly-update reboot exposed nlcl01file02 fh-cache poisoning. Fixed by restarting Pacemaker exportfs resource.
- **Multi-layer incident 2026-04-17 — consolidated overview** (project): 4-layer cascade in one day — VTI IPsec SA stale, BGP ECMP asymmetric paths, DMZ disk-full, and silent playbook skips — each hid the next. This is the index; each layer has its own memory.
- **n8n SQLite mutex timeout incident 2026-04-16** (project): ~90s n8n outage at 20:12 UTC caused by nl-pve01 IO pressure starving SQLite. Self-healed. Root cause identical to 2026-04-15 nl-pve01 memory pressure class.
- **Industry benchmark 2026-04-15 — 4.10/5.00 Optimized** (project): 15-dimension benchmark, 23 sources. 3.73->4.10/5.00 (Optimized, ~90th %ile). 10/10 YT Done. 39/39 E2E certified. All crons wired.
- **Industry research gaps 2026-04-10** (project): 14 gaps from 15 industry sources — 14/14 DONE as of 2026-04-19 (YT 424-437 all closed, verified against code).
- **Infrastructure Integration** (project): IaC repo integration, LibreNMS alerts, infra triage, Proxmox MCP, PVE drift detection, and operational details
- **infragraph-cascade-gating-1118-20260617** (project): 2026-06-17 — implemented IFRNLLEI01PRD-1118 cascade-probability gating on the infragraph predictor. The low precision behind -1065 is OVER-prediction (not the seed bug); gating it lifts precision ~2x recall-neutral.
- **infragraph-epic-state-20260609** (project): "Infragraph epic IFRNLLEI01PRD-1029 FINAL STATE — model-based control LIVE 2026-06-09 (13/16 done, system active, first rule -1046 approved); canonical record in repo memory/infragraph_epic_buildout_20260609.md"
- **infragraph_honest_gate_20260624** (project): "2026-06-24 why the infragraph B->C gate (-1040) can't graduate — it's unsatisfiable by honest predictions (~0.70 cascade ceiling), not a broken graph. Shipped honest v2-gate + fold-gate candidate metrics (MR !51); recalibration is the operator's -1040 decision."
- **infragraph-precisiondrop-suppression-20260619** (project): Tier-1 Phase-2 suppression added for the InfragraphPrecisionDrop alert (incident_knowledge id=1452) to stop the auto-resolve noise loop while -1065 precision calibration is in flight; must be removed when precision recovers.
- **IoT Pacemaker HA Cluster** (project): 3-node Pacemaker/Corosync IoT cluster (nlcl01iot01/nl-iot02/nlcl01iotarb01) — topology, resources, failover behavior, VMID 666
- **Local-first judge + synth migration (2026-04-19)** (project): Moved LLM-as-a-Judge and RAG synth defaults from Haiku to gemma3:12b (Ollama on nl-gpu01). Calibration = 85% agreement, +5pp drift (local is looser). Eliminates ~$5-10/month Haiku spend.
- **K8s Next Session Tasks** (project): Two pending tasks for K8s operational readiness — OpenClaw K8s access + Prometheus/Alertmanager/Gatus alert wiring
- **k8s-residual-triage-20260617** (project): "2026-06-17 — closed/triaged the residual open IFRNLLEI01PRD K8s alerts after the auto-resolve pipeline repair. seaweedfs filer OOM fix (MRs), notrf01dmz01 is a KNOWN scrape-path gap (not a real down), InfragraphPrecisionDrop is slow-recovery."
- **knowledge_injection** (project): Knowledge injection into triage pipelines. 51 CLAUDE.md + 200+ memories + compiled wiki (45 articles) surfaced at both tiers via 3-signal RRF. Repo sync cron on openclaw01.
- **kyriakos-portfolio-sitewide-audit-20260619** (project): "2026-06-19 full-site audit + fixes of kyriakos.papadopoulos.tech — 8 merged MRs (!18-!25), canonical infra numbers, verifiable certs, CV redesign"
- **lab-pve03-interfaces-md-stale-20260514** (project): 03_Lab interface doc 20241222_nl-pve03_interfaces.md is stale on bond0 NIC layout — lists 4×1G but actual is 2×10GBASE-T X550. NetBox + live lspci correct.
- **LibreNMS cororings threshold + nlpve04 onboarding 2026-05-10** (project): Bumped 5 cororings services from --rings 5 to 6 on NL+GR LibreNMS after nlpve04 onboarding. Also added nlpve04 itself as NL device 155 + cororings svc 40. snmpd-clone trap caught mid-flight.
- **librenms-extender-fleet-deployment-20260515** (project): "2026-05-15 fleet-wide LibreNMS extender deployment and hardening. nlpve04 got all 7 extenders from scratch (was bare). proxmox-extender switched to smart-style cache pattern on all 6 PVE hosts to bypass the /etc/pve/priv/authkey.key root-only requirement. apcupsd installed on nl-pve03+nlpve04 (shared SNMP UPS at 10.0.181.X). smart.config fleet sweep found stale gr-pve01 header on nl-pve01+nl-pve03 (now fixed) and empty config on gr-pve02 (5 disks now monitored, incl 2 MegaRAID)."
- **LibreNMS receiver template quirks on gr-pve02** (project): Two distinct GR LibreNMS receiver bugs that only surface on gr-pve02 — empty %msg substitution and UNKNOWN-classification of "Has worsened" critical transitions
- **nlrtr01 budget VDSL port-utilisation alarm during 2026-05-09 Freedom outage** (project): Diagnosed direction-mismatch artefact on Et0/1/0.6 (LibreNMS port_id 123127). Real DS sync 111 Mbps / US sync 33 Mbps; Cisco BW = 33 Mbps; LibreNMS ifSpeed = 33,000,000 → rule 6 false-fires. Operator chose Option A — override ifSpeed to 111,000,000.
- **litellm01-codestral-proxy-20260518** (project): nllitellm01 at 10.0.181.X now proxies Mistral Codestral (free tier) via LiteLLM with Anthropic-compatible /v1/messages — usable from laptop Claude Code via ANTHROPIC_BASE_URL env. Stack was dormant 15 months due to missing restart policy on the litellm service.
- **llm_judge_dead_3weeks_resurrected_20260627** (project): The LLM judge (gemma3:12b) returned overall_score=-1 on ~every session for ~3 weeks — TWO bugs (apostrophe heredoc-injection in the prompt build + a brace-truncating output-parse regex). Both fixed (MR !108), verified (real sessions now score 4.2-4.95). Surfaced by the composed-verdict tool. NO alert caught a 3-week run-but-silently-fail.
- **Per-Model LLM Usage Tracking** (project): llm_usage table, 3-tier token tracking (Tier 0 local GPU, Tier 1 OpenClaw OAuth Sonnet [migrated from OpenAI 2026-04-28 IFRNLLEI01PRD-746], Tier 2 Claude Code), JSONL-based pollers, Prometheus metrics, portfolio live widget. Poller rewrite 2026-04-10. Tier 0 per-call tracking added 2026-04-16. poll-openclaw-usage.sh added 2026-04-28 (replaces poll-openai-usage.sh).
- **local_n8n_db_snapshot_is_stale** (project): /tmp/n8n-db.sqlite on nl-claude01 is a stale manual snapshot, not the live DB. Use workflows/ repo exports or n8n-mcp for current state.
- **loop_engineering_benchmark_20260624** (project): "Loop Engineering (Anthropic Playbook / HuaShu Orange Book) added as benchmark source #10/11, scored A− (4.00/5.0) adversarially. Surfaced real actionable gaps in the gateway's enforcement layer."
- **maintenance_companion** (project): Maintenance Companion architecture — hybrid AWX/direct API, self-healing Layer 0, critical service map per PVE host, fallback ladder
- **Matrix Bridge Architecture** (project): Matrix Bridge (QGKnHGkw4casiWIU) — 73 nodes. Updated 2026-04-07: typography improvements (blockquote, nested lists, strikethrough, paragraph fix in Prepare Bridge Response markdownToHtml).
- **mempalace_hooks_fixed_20260624** (project): "2026-06-24 fixed + e2e-proved MemPalace: both Stop/PreCompact hooks now use the hook transcript_path (not the /tmp/claude-run-*.jsonl gateway-log glob). gateway MR !53, IFRNLLEI01PRD-1399. agent_diary write+read wired via MR !54 (SubagentStop hook) — live-confirmed ACTIVE 2026-06-25."
- **MemPalace Integration** (project): Ported 8 MemPalace patterns to claude-gateway (2026-04-09). Verbatim transcripts, hooks, temporal KG, agent diaries, 5-signal RRF (chaos baselines 5th, weight 0.25), layered injection, contradiction detection. 10 YT issues 401-410 Done.
- **merged_not_deployed_drift_20260627** (project): After landing 4 fix MRs (RAGAS/QA/seed/infragraph), the live working tree was 4 commits BEHIND origin/main — the ff-merge had silently failed (69 uncommitted cron-regen files blocked it). So fixes were MERGED but NOT DEPLOYED to the tree the crons run from; the infragraph fold-band metric never emitted. Fixed by stash+ff to da03770.
- **VPN Mesh Stats API** (project): Portfolio mesh-stats webhook — live SSH tunnel status, ping latency, RIPE BGP, 9 unique tunnels, standby-aware compound status. Script at scripts/vpn-mesh-stats.py.
- **MeshSat Mode A — Bundled Direwolf (MESHSAT-514)** (project): Direwolf bundled in bridge container; key non-obvious facts + four RF-surfaced bugs and their fixes, landed 2026-04-17
- **MeshSat next session plan** (project): T-Deck → Pi → SMS → Android → SBD → RockBLOCK → Webhook → MeshSat HUB → PGP Email pipeline
- **MeshSat session 2026-03-16** (project): MSVQ-SC compression, Android SMS, field intelligence, full Android parity (7 phases), 59 unit tests, MESHSAT-57 DONE
- **meshsat-slot-routing-940-20260617** (project): 2026-06-17 — IFRNLLEI01PRD-940 implemented + LIVE. Tag-based MESHSAT slot disambiguation so the 3 sibling repos (meshsat/meshsat-hub/meshsat-android) sharing one YT project route to the right repo.
- **MeshSat RTL-SDR spectrum analyzer — architecture + constraints** (project): Non-obvious constraints and design decisions from the 2026-04-17 RTL-SDR spectrum + jamming alert build (5 bands, SSE stream, TAK CoT + hub relay, dashboard widget + /spectrum page, MIL/ITU operator contract, D1-D12 debt closure, MESHSAT-412 Done)
- **MeshSat zigbee permit-join + cp210x wedge recovery** (project): Z-Stack 2.7.1 rejects broadcast permit-join with 0xC2 on fresh-restored networks — use unicast self-target; cp210x wedge needs driver unbind/rebind, not just USBDEVFS_RESET
- **MIL-contract Playwright audit pattern for safety-critical UIs** (project): Pattern for codifying MIL-STD / ITU / FM doctrine as a per-contract-item Playwright audit. Each contract is a regex/locator, passes recorded to JSON, prevents UI regressions from sneaking past screenshot reviews. First use: MeshSat RF spectrum 2026-04-17 (25 items, widget 11 + page 14).
- **model_downsizing_audit_20260627** (project): "Code-grounded per-step audit of whether the agentic system can run on smaller/local models. ~85% downsizable; router is BROKEN (empirically Opus-pinned, docs falsely claim fixed); 2 surfaces have no faithfulness oracle."
- **model_orchestration_research_20260627** (project): Research + recommended architecture for a centralized multi-provider model-orchestration layer (openai/mistral/deepseek/anthropic/z.ai API + claude-code subscriptions). Z.ai endpoints/coding-plan facts; current decentralized model-config state; LiteLLM is omoikane's NOT the gateway's; the cc-cc/oc-* modes are DEAD/vestigial → retire. Two-plane design + 4-phase migration.
- **n8n code-node safety — post 14h-outage guardrails** (project): Build Prompt dead code removed 2026-04-19 (3 returns → 1, 90KB → 36KB). Validator at scripts/validate-n8n-code-nodes.sh is now the required pre-push gate. The 14h outage class is caught automatically.
- **n8n OOM outage + Restart= drop-in (2026-05-11)** (project): 4h n8n outage (06:50-10:48 UTC) — OOM-killed inside the LXC, sat dead because n8n.service had Type=simple with NO Restart= directive. Operator-reported via "both pages live stats widgets are offline". n8n LXC has migrated from nl-pve01 to nlpve04 since the 2026-04-22 Known Host Pressure note in CLAUDE.md.
- **n8n Technical Facts and Pitfalls** (project): Key technical facts about n8n, Claude CLI, expression pitfalls, MCP update safety, webhook registration, and known bugs
- **NAT/PAT Audit 2026-04-09** (project): Tri-WAN PAT completed on NL ASA (28 rules). GR ASA NAT_dmz_servers02 /29→/27 fix. Both saved.
- **NL traffic-shaping audit during 2026-05-09 Freedom outage** (project): Read-only netmiko audit of rtr01 + nl-fw01 QoS state. Only one rule actually bites traffic — rtr01 Dialer1 egress 30 Mbps single-FIFO shape. All 8 per-tenant ACLs lifetime-zero. nl-fw01 throttle PMs all unattached + ACLs inactive. Improvement gaps documented for future hardening.
- **no_human_eval_anchors_20260627** (project): IFRNLLEI01PRD-1451 no-human eval ground-truth anchors. ANCHOR 1 (frontier cross-check) BUILT+LIVE (MR !113): Opus re-judges a sample vs the local gemma judge → drift/dead-judge detection, zero operator involvement. ANCHOR 2 (autoresolve outcome-truth) table created, script pending.
- **notrf01dmz01 + notrf01dmz02 onboarding — DONE 2026-05-05** (project): Two new public-IP DMZ Docker hosts at Gigahost NO. Active/active SaaS pre-staging complete. Full 6-tunnel mesh + 8/8 iBGP peers established per host. Source of truth = handoff doc. Status page diagram updated 2026-05-06.
- **notrf01dmz01/02 + txhou01vps01 TCP-unreachable from NL 2026-05-08** (project): 3 newly-onboarded mesh peers (notrf01dmz01 10.255.X.X, notrf01dmz02 10.255.X.X, txhou01vps01 10.255.X.X) were TCP+ICMP unreachable from NL because the NL edge ASA was missing inbound VTI access-group bindings. Resolved same day by operator's ACL fix (clears IFRNLLEI01PRD-849 + -684 ASABindingDrift)
- **npm_api_access_20260623** (reference): The agentic system has STANDING API access to Nginx Proxy Manager (nlnpm01) — manage proxy hosts with scripts/npm-api.py; NEVER edit the NPM DB/conf directly (cluster DB); never ask the operator for NPM creds again
- **NVIDIA DLI cross-audit 2026-04-29 — IMPLEMENTED** (project): 15th audit on the system. NVIDIA DLI Agentic-AI 19-transcript course cross-eval. ALL P0+P1 ITEMS SHIPPED 2026-04-29 (umbrella IFRNLLEI01PRD-747, children -748..-751). Score lifted A (4.4) → A+ (4.79).
- **ollama-gpu-only-lockdown-20260513** (project): "2026-05-13 nl-gpu01 catastrophic CPU saturation (load avg 29.93, ollama 1468% CPU). RCA: gemma3:12b loaded 0/49 layers on GPU — VRAM 1.3 GiB short of fitting (canary 4.0 + rerank 4.0 + chatterbox 3.4 + 3 others = 14.4/23 GiB occupied; gemma3 needed 8.9 GiB; only 7.6 free). Three-layer durable fix: (1) operator disabled canary + chatterbox-tts + comfyui.service freeing 7.5 GiB headroom; (2) server-side PARAMETER num_gpu 999 baked into all 24 model Modelfiles via ollama show + ollama create loop — official maintainer-recommended approach per @dhiltgen #1855 + @rick-github #6950, applies to all callers without client changes; (3) Prometheus alert OllamaModelNotGpuOnly + ExporterStale wired via scripts/write-ollama-gpu-metrics.py + /etc/cron.d/ollama-gpu-metrics + agentic-health-alerts.tf. Final state: client requests with NO num_gpu option get requested=999 automatically, offloaded 49/49 layers, 11ms/token. Discarded experiments (REVERTED): OLLAMA_LLM_LIBRARY=cuda_v12 env var (it's for FORCING CPU when GPU misdetected, not for preventing CPU spill — common doc misread); entrypoint that rm'd /usr/lib/ollama/libggml-cpu-*.so (broke ollama's GPU memory-fit scheduling — when CPU backend is unavailable, scheduler retracts a planned 49-on-GPU placement to 0-on-GPU). Two codellama Modelfiles needed deprecated `rope_frequency_base` line stripped before re-create."
- **omktst01_cloudinit_bootcmd_hang_20260624** (project): "2026-06-24 RCA+fix: nlomktst01 (omoikane.coach BENCHMARK host, NOT a test VM) wedged ~6 days — cloud-init bootcmd `systemctl enable --now qemu-guest-agent` deadlocks boot. Fixed via --no-block."
- **omoikane-693-goals-excellence-epic-20260525** (project): "Filed OMOIKANE-693 epic + 8 child tickets for /goals page absolute-excellence; shipped 8 MRs in session (5 merged + 3 rebased-on-fixed-main + 1 baseline-fix unblocker). Engineering rigor lifted, real-user validation still future work."
- **omoikane-724-followup-operator-decisions-20260526** (project): Operator decisions captured 2026-05-26 on remaining OMOIKANE-724 follow-up items. Authoritative for next-session planning when picking up the unfinished follow-ups.
- **omoikane-724-phase7c-decisions-20260526** (project): Operator decisions 2026-05-26 unblocking OMOIKANE-724 Phase 7c protected-attribute UI. Authoritative spec for implementation MR.
- **omoikane-820-tantivy-discover-index-20260527** (project): 2026-05-27 OMOIKANE-820 — Tantivy in-RAM HA discover-index ships in single MR !2554 (~1500 LOC); replaces 90s+ adapter fan-out with sub-ms index query
- **omoikane-879-litellm-self-healing-20260529** (project): "2026-05-29 OMOIKANE-879 — boot-time LiteLLM reconciler + dispatch auto-retry close the \"save-key-before-env-set\" race that produced silent daemon↔LiteLLM divergence."
- **omoikane-904-907-908-906-session-20260529** (project): "Marathon 2026-05-29 session — 4 OMOIKANE MRs (!2629/!2630/!2632/!2633) merged + 1 operational fix (Brave key) — all in the observability + pipeline-unblock theme. ~3h elapsed. Chain effect: each MR's metric/log surfaced the failure mode for the next."
- **omoikane-904-himalayas-chrome-finding-20260529** (project): "OMOIKANE-904 silent-gate observability shipped 2026-05-29; immediately surfaced that himalayas adapter is capturing 100% nav-chrome bodies (raw_len=12000, cleaned_len=0). 29 WARN lines + Prom counter within minutes of deploy."
- **omoikane-discover-perf-chain-20260528** (project): 2026-05-27/28 6-MR chain took /postings/discover 90-120s → sub-1s. Trajectory and lesson on instrumenting before fixing.
- **omoikane-fernet-key-envfile-drift-20260529** (project): "2026-05-29. OMOIKANE_FERNET_KEY was in /srv/omoikane-daemon/secrets/shared.env but the daemon compose only loaded app/.env, so the key was never exposed to the running container. Hot-fix appended to app/.env on both dmz hosts; IaC follow-up needed."
- **omoikane-modal-perf-chain-20260529** (project): 2026-05-28/29 5-MR chain took /company-info modal 10-15s → sub-1s. Pattern transfer from the discover-perf chain (-820/824/830) applied to a different request path.
- **omoikane-tei-reranker-teardown-20260520** (project): "2026-05-20: dmz-local omoikane-tei-reranker (TEI bge-reranker-v2-m3-onnx, port 8089) torn down on both notrf01dmz01 + notrf01dmz02 hosts after caller-archaeology confirmed zero live consumers. The daemon's runtime never wired a reranker consumer; the gateway-side KB scripts (kb-semantic-search.py, kb-latency-probe.py, test-session-holistic.sh) always used the GPU-host TEI at nl-gpu01:11436. Freed ~2-3 GiB RAM per host. Compose dirs archived to tei-reranker.removed-dead-code-20260520T165017Z on notrf01dmz01 and tei-reranker.removed-dead-code-20260520T165021Z on notrf01dmz02 — trivial restore (mv + docker compose up -d) if a future consumer materializes."
- **OOB Access via PiKVM + Cloudflare Tunnel** (project): BROKEN (2026-03-21) — PiKVM bricked by forced package upgrade. Requires physical access to GR site to recover. Cloudflare tunnel config still exists but PiKVM is offline.
- **OpenAI Agents SDK audit (v0.14.2)** (reference): Reference — audit findings for openai/openai-agents-python if ever evaluated for claude-gateway integration. Two non-obvious gotchas persist regardless of version.
- **OpenAI Agents SDK adoption batch (2026-04-20)** (project): 9-issue structural upgrade to the agentic substrate — schema versioning, typed events, handoff envelope, per-turn hooks, rejection taxonomy, compaction, snapshots, agent-as-tool, depth counter + QA suite. Pointers to every runtime component.
- **OpenClaw Audit & Upgrade — 2026-03-25** (project): Full compliance audit (Gulli book + Anthropic exam guide), config drift fix (6 files deployed), OpenClaw 2026.3.3→2026.3.23, LXC disk 21→64GB, 13 prioritized findings.
- **OpenClaw container — SOUL.md / memory-recall / safe-exec already in sync** (project): Audit 2026-04-24 — three CLAUDE.md "Remaining Roadmap" sync items are stale; files already match container md5
- **OpenClaw 402 — per-token burst window, not account exhaustion (2026-04-29)** (project): 2026-04-29 incident corrected. Per-OAuth-token 5-min burst limit, not account-level. Setup-token migration made it worse. The other Claude Code session staying alive proved the Max account itself was healthy.
- **OpenClaw → Ollama local triage (2026-04-29)** (project): Wired OpenClaw 4.26 to use local Ollama with qwen2.5:7b after failing to make the claude-cli OAuth path work post Anthropic April-4 OpenClaw policy. Working at ~3 min/call latency, $0 cost. Hardware caps model size at 7-12B on this GPU.
- **OpenClaw Tier 1 GPT-5.1 → Sonnet OAuth migration plan** (project): 2026-04-28 audit + plan to replace OpenAI GPT-5.1 in OpenClaw with Sonnet 4.6 via OpenClaw's native --auth-choice claude-cli (Max sub OAuth, $0). Plan file at ~/.claude/plans/replicated-napping-galaxy.md.
- **OpenClaw 4.10+4.11 Upgrade Audit** (project): Audit of OpenClaw releases 2026.4.7-4.11 against current system. Version gap 2026.3.3→4.11 (8 releases). Active Memory, Dreaming, memory-wiki, security hardening. Prioritized recommendations.
- **OpenClaw v2026.4.22 Upgrade Audit** (project): Pre-decision audit of OpenClaw v2026.4.22 (released 2026-04-23) vs our running v2026.4.11. 11 tags / 716 commits ahead. Relevant fixes, traps, non-applicable items, three paths. DECISION PENDING.
- **OpenClaw v2026.4.26 upgrade** (project): 2026-04-29 upgrade from 2026.4.11 to 2026.4.26. Build process, gotchas, persistence, test results.
- **openobserve_access_20260626** (reference): "STANDING access + OTLP trace-export setup for OpenObserve (nlopenobserve01) — the gateway's distributed-tracing sink. Admin creds in .env; the OTLP auth + 5h ingest-window gotchas."
- **OpenObserve Grafana datasource deployed via GitOps** (project): OpenObserve (10.0.181.X:5080) added as Grafana datasource via additionalDataSources in kube-prometheus-stack Helm values
- **Operational Activation Audit 2026-04-10** (project): Comprehensive audit scoring operational activation (not just implementation). 21/21 tables populated after remediation. 8 YT issues (445-452).
- **orchestrator_benchmark_gap_closing_20260626** (project): Autonomous gap-closing run after the orchestrator/controller-plane benchmark (B+, 3.48/5). Closed observability/governance/liveness gaps + ran a cross-benchmark audit + OWASP audit; documented the rest. Operator excluded security-identity + model-routing.
- **orchestrator_control_plane_20260626** (project): ACTIVE build of the agentic orchestrator/control-plane epic IFRNLLEI01PRD-1421 (3 bricks). Operator overrode the A-on-all gate ("begin the orchestrator, don't stop until I'm back"). Brick 1 (component registry) DONE+deploying; Bricks 2/3 next.
- **parsePoll fix 2026-04-25 — 8 bugs across Runner + Bridge** (project): Three-round deep e2e on Matrix poll formation. Five commits closed eight distinct parsePoll bugs (early-[POLL] hijack, trailing-prose absorption, latent fallback shape, code-fence absorption, parser duplicated in Bridge, single-blank-line break too aggressive, nested sub-bullets absorbed, Markdown horizontal rule absorbed). Live ↔ repo ↔ HEAD all SHA-matched.
- **pending_debt_followups_20260626** (project): 2026-06-26 pending-debt cleanup — done (valid_until migration + Session-End workflow retire, MR !61) + the 3 OPEN follow-ups operator chose to handle next (kubeEtcd Option A, schema.sql missing-tables drift, deploy-copy drift audit).
- **PiKVM and LTE Gateway Audit** (project): Audit of grpikvm01 (PiKVM v3) and grlte01 (Cisco C819G LTE) — config, findings, OOB architecture rationale
- **pipeline-autoresolve-repair-20260617** (project): 2026-06-17 — the auto-resolve pipeline was dark for months across MULTIPLE layers; repaired end-to-end + proven E2E. The system now actually triages + auto-resolves + closes.
- **Pipeline Hardening (2026-04-01)** (project): 11 fixes across 5 workflows + 3 scripts. NetBox Step 2-pre in triage, syslog 3-day, [POLL] fallback parser, escalation cooldown 1h, recovery dedup 60s, flapping timeout 4h, watchdog zombie bounce, Parse Response em-dash + [POLL] approval gate regex. All E2E verified.
- **plan-chatdevops-multi-repo-state-refactor** (project): "Approved plan to centralize ChatDevOps state at /home/app-user/gateway-state/, eliminating ~12 hardcoded cubeos paths so adding a new repo becomes a 1-line slot-config.json change. Prerequisite for parallel-dev + project-spec-schema plans."
- **plan-chatdevops-parallel-dev-architecture** (project): "Approved architecture to safely run up to 4 parallel Claude Code workers on a single feature using n8n as deterministic orchestrator, git worktrees for isolation, and a single-MR-per-feature merge coordinator. Industry-validated (MAST taxonomy, Cognition \"Don't Build Multi-Agents\", Anthropic Orchestrator-Workers pattern)."
- **plan-project-spec-schema-and-bootstrap** (project): "Approved Spec-Driven Development (SDD) schema + 7-phase bootstrap workflow + project-bootstrap-pack tooling so brand-new greenfield projects are onboarded via a strict, machine-verifiable spec contract before parallel agents touch a file. Adopts GitHub Spec Kit/Kiro/EARS/MADR methodology, implemented natively in our stack via Claude Code custom skills + Python validators."
- **platform_controller_20260626** (project): Plane-A platform controller — the agentic platform's k8s-style self-healing operator (the orchestrator's ACTUATOR half). Built+e2e-proven+LIVE (dark)+registered-critical+alerted 2026-06-26. Scoped STRICTLY to platform health, NEVER the mission. gateway MR !70 + infra MR !351.
- **pmxcfs_wedge_alert_build_20260630** (project): "Built the pmxcfs-wedge Prometheus alert (IFRNLLEI01PRD-1501). KEY: pve01 is NOT a Prometheus target so the existing PVELoadHigh rules are silently dead; collector runs on claude01."
- **portfolio_audit_20260628** (project): Audit of the kyriakos agentic-chatops portfolio page + the agentic-stats.py widget backend vs live; two-repo edit set in flight (workflow wf_5e1faf70-3ba)
- **portfolio-lab-page-rebuild-schedule-20260609** (project): "2026-06-09. kyriakos.papadopoulos.tech/lab/ auto-updates via Hugo CI build-time bake of data/lab_stats.json from n8n /webhook/lab-stats (lab-stats.py → NetBox+kubectl+ZFS). Rebuild schedule id=2 on GitLab project 9 is cron `0 * * * *` (HOURLY) despite being labeled \"5m\" and drawio saying \"*/5 cron\" — discrepancy, not yet fixed."
- **portfolio_status_page_fix_20260414** (project): Status page service-health proxy fix (DMZ SNI issue) + P0 accessibility audit fixes deployed 2026-04-14
- **postiz_gr-fw01_firewall_rules_pending_migration_20260624** (project): 2026-06-24 — gr-fw01 (GR ASA) firewall rules around the migrated postiz still point at GR; the :80 Meta-webhook static-NAT is BROKEN, Cloudflare still pins postiz to GR. Pending migration to nl-fw01.
- **postiz_migration_gr_to_nl_20260624** (project): 2026-06-24 migrated grpostiz01 (privileged Docker LXC) cross-site to nlpostiz01 on nlpve04 to relieve gr-pve01 memory pressure (the chronic etcd-cascade root). Full DNS/NPM/e2e done.
- **Preference-iterating prompt patcher (IFRNLLEI01PRD-645)** (project): N-candidate A/B trial framework for auto-generated prompt patches. Replaces single-shot prompt-improver with policy iteration at the prompt-policy level — prompt-level analogue of RLHF preference iteration without touching model weights.
- **Prometheus Receiver 'now' temporal dead zone bug fix (2026-04-11)** (project): Parse Alerts node crashed 30x/day — const now used before declaration. Fixed by replacing with now0. Both NL+GR receivers.
- **prometheus_oom_rightsize_20260627** (project): NL Prometheus replica -1 ContainerOOMKilled (8x exit-137, ~9-min crash loop) = transient memory spike past the 4Gi cgroup ceiling on ONE replica, NOT a cardinality leak. Fix = right-size 4Gi->6Gi limit / 2Gi->3Gi request via infra MR !353 (helm_release.monitoring in main.tf). In-flight: applied via Atlantis, verification pending.
- **nl-pve01 memory pressure causing apiserver restarts** (project): PVE01 host 88% RAM (2.5x overcommit, zero swap) starved etcd I/O on nlk8s-ctrl01. 754 apiserver restarts. Mitigated by shutting down androidsdk01.
- **pve01_pmxcfs_wedge_20260630** (project): nl-pve01 pmxcfs wedge killed matrix; lab-stats.py was the orphan amplifier; n8n is on pve04 NOT pve01.
- **nl-pve01_rpool_suspend_heatwave_20260623** (project): 2026-06-23 nl-pve01 ZFS rpool I/O-suspended (heatwave) → froze ~40 guests incl nl-pihole01 → site-wide DNS cascade. 2026-06-24 VERIFIED: host recovered (up ~20h), rpool DEGRADED running on a SINGLE FireCuda; the twin FireCuda 530 7VS00ZJ8 (eui…0048c7) genuinely FAILED (EIO storm + absent from the PCIe bus) → pending physical reseat/replace. DISTINCT from gr-pve01 nvme2n1 (= thermal throttle, NOT failed).
- **nl-pve03 capacity pressure (2026-04-22)** (project): nl-pve03 mirrors pre-remediation nl-pve01 pattern — no swap/zram, sustained 92%+ memory, hosts K8s ctrlr+NMS+GPU inference. Apply same zram fix; OOM blast radius is the K8s control-plane share + LibreNMS + Ollama inference simultaneously.
- **pve03-vs-pve04-hardware-delta** (project): "Live-verified 2026-05-14 hardware delta between nl-pve03 (Dell Precision 3680 workstation) and nlpve04 (ASRock GENOAD8X-2T/BCM server). Non-derivable operational constraints — RAM caps, BMC presence, expansion headroom — that should inform future capacity / placement / DR planning."
- **nlpve04 onboarding (COMPLETED)** (project): ASRock GENOAD8X-2T/BCM (AMD EPYC 9334 32C/64T, 128GB DDR5, 6TB max) onboarded as 6th cluster member nlpve04. Clone-of-pve02 corosync rejection → identity wipe → rename → join.
- **pve04_pvestatd_wedge_20260625** (project): nlpve04 PVE-management wedge (pvestatd D-state, cluster status=unknown, claude01 LXC OOM). RESOLVED 2026-06-27 WITHOUT a reboot — `systemctl restart pve-cluster` (un-hangs pmxcfs, releases D-state) THEN `restart pvestatd`. The 06-25 "reboot is the ONLY fix" was WRONG. IFRNLLEI01PRD-1419.
- **PVE drift jobs can 1h-timeout on inventory blinks** (project): sync_pve_drift / detect_pve_lxc_drift can run for an hour deleting dozens of LXCs after a transient PVE inventory blink — needs a sanity cap.
- **PVE Kernel Maintenance Automation** (project): Full-site PVE kernel update automation — ALL DONE + dry-run PASS on both sites. 14 playbooks, startup order (5 nodes), 6 AWX templates, maintenance mode (7 workflows), hardened per Proxmox best practices.
- **PVE Swap Audit 2026-03-25** (project): Swap configuration audit across all 5 PVE nodes — findings, changes, Proxmox best practices, disk layout
- **rag_circuit_breakers** (project): IFRNLLEI01PRD-631 shipped 2026-04-19. 4 named breakers guard RAG external calls (rerank service, Ollama embed, Anthropic Haiku synth, Ollama qwen synth). SQLite-backed state shared across processes; Prometheus gauges + CircuitBreakerOpen alert.
- **Q2 cross-chunk synthesis in RAG pipeline** (project): When cross-encoder max < 0.7 on a query (no single doc strongly matches), synthesize an answer from top-10 fresh-query candidates via qwen2.5:7b. Prepended as synthesis row. +13 points hit@5 on 50-query hard eval.
- **reference-notrf01-dmz-sudo-credentials** (reference): "Operator-provided sudo password for app-user@notrf01dmz01 + app-user@notrf01dmz02 stored at ~/.config/omoikane-sudo/notrf01-dmz (mode 600, app-user-only). For most operations the password is not needed — app-user already has NOPASSWD: ALL on both hosts. The stored password is the operator's escape hatch for the rare case where the cached NOPASSWD policy fails OR a su to another user (e.g. operator) is required."
- **Postiz self-hosted (postiz.example.net)** (reference): Where Postiz lives, how to reach it, the Postgres credentials and table layout for debugging post fan-out, and the 4 social-platform integration IDs used by the RSS autoposter workflow.
- **Syslog-ng servers are per-site — don't look for GR logs on the NL server** (reference): Each site has its own syslog-ng server; NL devices log to nlsyslogng01, GR devices log to grsyslogng01. Looking for GR device logs on the NL syslog-ng will silently return empty.
- **Cross-encoder rerank service on nl-gpu01** (project): BAAI/bge-reranker-v2-m3 via sentence-transformers as dedicated microservice at nl-gpu01:11436. Replaces Ollama yes/no hack.
- **risk_based_auto_approval** (project): IFRNLLEI01PRD-632 wired 2026-04-19. Classify Risk SSH → Build Prompt risk-section → Matrix Bridge [AUTO-RESOLVE] handler. Low-risk diagnostic sessions auto-resolve with m.notice (no ping). session_risk_audit table + weekly invariant check.
- **router_fixed_live_n8n_curl_access_20260627** (project): The model router IS fixed in the LIVE n8n Runner (row-count + risk-floor) — the git export is STALE (PUT-live-uncommitted confirmed). The benchmark/audit "router broken" was off the stale export. n8n is reachable via curl (key in ~/.claude.json); SSRF only blocks the MCP.
- **rr-restored-yb-v2-ca-migration-20260602** (project): 2026-06-02. Reactive Resume (cv.omoikane.coach) was down because the OMOIKANE-1063 YB cluster migration left the RR yb-tls-shim with stale v1 CA + stale connect IP. Three-layer fix needed; documented for future runbook.
- **rtr01 syslog source-binding fix (2026-04-22)** (project): IOS-XE F0/0 data-plane ACL logs (fman_fp_image) arrive with no hostname in the syslog header; syslog-ng then falls back to source IP if reverse-DNS fails. Fix = /etc/hosts entry on syslog-ng server. Durable + libc-backed; holistic-health guardrail added.
- **Runner and Poller Workflow Flows** (project): Runner (47 nodes incl. Evaluator-Optimizer), Poller (10 nodes), Session End (12 nodes). Updated 2026-04-07: XML-tagged RAG, hybrid RRF, tool profiles, Evaluator-Optimizer (3 new nodes), 16 PII patterns, tool call limit.
- **Scanner nuclei + testssl silent failure root cause + fix (PATH)** (project): Both daily security scanners had nuclei + testssl silently failing in the 03:00 cron run because cron's default PATH excludes /usr/local/bin. Fixed by exporting PATH at top of weekly-scan.sh.
- **scheduled_reboot_suppression_build_20260629** (project): "Built+verified the self-learning scheduled-reboot suppression feature (4 increments). Matcher (phase SR in tier1_suppression.py) suppresses on-schedule reboots on live-registered hosts; two-phase verify-and-reopen; discovery sweep + promoter(observe→live) + classifier(self-register at triage) + metrics/alerts/holistic/digest/audit. LIVE state: migration 022 applied, nl-gpu01 registered live (cron 0 7 * * *, 10 confirmed boots), INERT (TIER1_SCHED_REBOOT_ENABLED=0). All verified except Cronicle registration + commit pending."
- **screenity-selfhost-deploy-20260622** (project): "2026-06-22 IN-PROGRESS — self-hosting Screenity as a dockerized nginx distribution LXC nlscreenity01 on nlpve04; VMID/IP/DNS/NPM plan locked, not yet executed."
- **SeaweedFS Cross-Site Replication** (project): SeaweedFS bi-directional filer-sync between NL and GR K8s clusters — architecture, quirks, known limitations, and operational gotchas
- **SeaweedFS cross-site replication recovery (2026-05-05)** (project): Two stale-checkpoint failures fixed end-to-end — cross-site filer.sync (since 2025-12-11) AND GR intra-cluster meta_aggregator (since 2025-12-28 / 2026-03-24). Atlantis MRs !290+!291 merged + gRPC KvPut recovery script + runbook committed.
- **security_alert_receivers** (project): Security + CrowdSec alert pipelines (4 workflows, 6 CrowdSec hosts), scanner VMs, triage skill (10 steps, 3 TI sources), learning loop, baseline polls, ATT&CK Navigator. 2026-04-07: YT descriptions use markdown tables, triage delegation structured messages, 8 IF node singleValue fixes.
- **Session DB write-back anomaly pattern** (project): IFRNLLEI01PRD-457 had 133 JSONL events but 0 messages in sessions DB — Runner write-back may silently fail
- **session-llm-handbook-audit-20260616** (project): "2026-06-16 in-progress audit — agentic system vs LLM Engineer's Handbook, 3 deliverables"
- **session-summary-20260531-gr-adapters-and-cost-reduction** (project): "Multi-day session (started 2026-05-30) shipping the OMOIKANE-1015 web-search cost-reduction epic end-to-end (€305 Brave overage stop-the-bleed), 5 GR-adapter MRs, country filter + 3-bug fix + UX parity on /postings/companies, and xe_gr Scrapling bypass that failed against AWS WAF. 11 MRs total. GR vacancies 685→1008 (saturation ceiling on existing 8 adapters)."
- **Session summary IFRNLLEI01PRD-938** (project): Compacted session context for IFRNLLEI01PRD-938
- **Session summary IFRNLLEI01PRD-939** (project): Compacted session context for IFRNLLEI01PRD-939
- **Session summary IFRNLLEI01PRD-942** (project): Compacted session context for IFRNLLEI01PRD-942
- **Session summary IFRNLLEI01PRD-987** (project): Compacted session context for IFRNLLEI01PRD-987
- **Session summary IFRNLLEI01PRD-992** (project): Compacted session context for IFRNLLEI01PRD-992
- **Session summary IFRNLLEI01PRD-993** (project): Compacted session context for IFRNLLEI01PRD-993
- **Session summary IFRNLLEI01PRD-994** (project): Compacted session context for IFRNLLEI01PRD-994
- **Session summary IFRNLLEI01PRD-998** (project): Compacted session context for IFRNLLEI01PRD-998
- **Session summary IFRNLLEI01PRD-999** (project): Compacted session context for IFRNLLEI01PRD-999
- **Session summary cli-446fe240-f009-4fd5-a87c-b8ecb446a101** (project): Compacted session context for cli-446fe240-f009-4fd5-a87c-b8ecb446a101
- **session-thermal-and-gr-unreachable-20260616** (project): "2026-06-16 triage — NL \"thermal\" was stale phantom data; GR site was isolated ~06-15 22:58 → RECOVERED by 2026-06-17 (GR back online, reachable from NL)"
- **IFRNLLEI01PRD-843 SkillPrereqMissing — FIXED 2026-05-05 (commit bf91ab9, local-only push pending)** (project): SkillPrereqMissing for k8s-diagnostician + drift-check on bin:kubectl was a cron-PATH false-fire. Fixed via `export PATH` in write-skill-metrics.sh + audit-skill-requires.sh (commit bf91ab9). Verified under simulated cron (env -i + PATH=/usr/bin:/bin): bin:kubectl 0->1 both surfaces, _ok_all=1 both, audit 18/18 PASS. Commit on local main; push not yet executed.
- **sms_alert_fatigue_dedup_20260623** (project): 2026-06-23 fixed the 91-SMS/7d alert-fatigue leak — session->SMS path now dedups on a root-cause CLUSTER key + edge-trigger instead of issue_id
- **status-degraded-no-dmz-bgp-20260619** (project): "2026-06-19 status page \"Degraded — BGP 83/87\" root cause = 4 idle Norway-DMZ BGP sessions (notrf01dmz01 GR-leg stuck + tx-edge↔notrf01dmz03/04 idle); plus edge/CLAUDE.md audit findings."
- **status-diagram-upstream-render-gaps-20260516** (project): "RESOLVED 2026-05-16 (after one false-start iteration). kyriakos.papadopoulos.tech/status/ BGP diagram was rendering 4 transit bubbles all under iFog, 0 under Terrahost (real RIPE+bgp.tools view is 7+2). Final shipped fix: claude-gateway b7c8ca5 (suffix-pair aggregation + per-upstream top-7 cap + min-obs=4 floor in vpn-mesh-stats.py) + kyriakos 402ca0f (mesh-graph.js: transit row y bumped from cy-0.82*baseR to cy-1.0*baseR, single one-constant change; mesh-health.html: cache-buster v=40->41). First attempt 37462f3 introduced a per-upstream radial fan layout — operator rejected as 'complete disaster', reverted via c69581e. Operator preference confirmed: preserve the existing single-row transit visual; never reinvent layouts. Open follow-up: 5-item improvement list discussed (Prometheus alert + visual indicators for BGP failure states) — operator green-lit, not yet built."
- **Status page polish bundle 2026-05-06 (TX wire-up + UX/perf hardening)** (project): 11 fixes shipped to kyriakos.papadopoulos.tech/status/ in one session — TX chaos UI integration, dynamic latency matrix, NO orange→green, particle-timer perf hardening (~25-39% idle CPU drop), maximize button, click-to-select link UX. 24/24 Playwright. YT IFRNLLEI01PRD-850.
- **Status page chaos red-link bug + fix 2026-05-06** (project): Operator's 19:13 UTC chaos run lasted 600s with zero red links — root caused to canonical-order reversal in chaos.js tunnelLabel() + missing frontend chaos-allowlist for inter-VPS swanctl pairs. Fixed across chaos.js / mesh-graph.js / chaos-test.py + AS64512 center label removal + the whole BGP info bar removal (the RIPE first_seen of 2024-08-26 was a previous holder of AS64512; operator's RIPE registration is 2025-12-05). DEPLOYED 2026-05-06 ~21:50 UTC via kyriakos commit f458a8e (chaos.js?v=9, mesh-graph.js?v=36) + claude-gateway commit ca4c2e9 on fix/tx-latency-pings (MR !6).
- **Syncthing node inventory location** (reference): 12-node Syncthing mesh roster + IaC home + private discovery/relay setup
- **Teacher-agent DM audit 2026-04-23** (project): How to fetch operator DM history from the teacher bot + 4 bugs observed on 2026-04-23 awaiting operator triage
- **Teacher-agent — all 5 tiers done (IFRNLLEI01PRD-651..-655)** (project): Feature-complete. Foundation + intelligence + interface + loop + gate. Invariant audit + calibration baseline + ops runbook. 62/62 QA. Real-data calibration pending >=20 operator-graded answers.
- **Teacher-agent session — 2026-04-21 polish + E2E link audit** (project): Enhancements after 5-tier MVP closed. Semantic retrieval, wiki-link plumbing, memory+project-docs published to wiki, free-chat E2E proven end-to-end via Matrix API.
- **n8n template portal submission** (project): Status and feedback history for the n8n creator portal template submission of the Claude Gateway workflow
- **territory_gate_20260625** (project): "IFRNLLEI01PRD-1408 territory-aware hard-gate — make loading+respecting the relevant infra CLAUDE.md an ABSOLUTE prerequisite before a session acts in a territory. Operator chose Both (PreToolUse + Runner backstop), live-on-merge. Mid-build 2026-06-25."
- **Thanos Cross-Site Architecture** (project): Thanos Query federation NL<->GR via ClusterMesh — FULLY OPERATIONAL (2026-03-21). All gaps resolved, 275 targets visible cross-site.
- **tno-gate-same-day-arc-20260522** (project): "2026-05-22 same-day arc: 5 MRs (!2245 + !2247 + !2249 + !2251 + quota fix) to close TNO-gate chip-strip gaps. !2249 had to be hot-reverted because blocking-await on extract_for_posting stalled the gate page 75-225s when the runner was slow."
- **token_spend_attribution_20260624** (project): "2026-06-24 evidence-based answer to \"where do Claude tokens/usage go?\" — 98.7% is Tier-2 Opus, driven by giant cached prompt context across k8s-cascade sessions; model right-sizing is dead."
- **Tri-source audit implementation (2026-04-07)** (project): Full implementation of 16 YT issues to reach 11/11 A+ across all dimensions. Hybrid RRF, eval flywheel, Evaluator-Optimizer, deployment guide.
- **txhou01vps01 onboarding complete (2026-05-06)** (project): Third edge VPS at iFog Houston, TX. AS64512 IPv6 transit + 7 IPsec tunnels into the mesh + 9 iBGP peers + HAProxy edge serving 9 IPv6 anycasts + omoikane backends. End-to-end on the live status page (5th country site).
- **Visual Audit and Typography Improvements** (project): Playwright visual audit suite + formatting improvements across 10 workflows. markdownToHtml (blockquote, nested lists, strikethrough, paragraph fix), progress poller MCP tool names, triage delegation, YT structured comments. 2026-04-07.
- **VMID UID Schema** (project): Proxmox VMID encoding scheme — 9-digit structured ID encoding site, node, VLAN, automation tag, and resource ID. Some VMs have drifted from schema.
- **VPS DMZ /27 route is now BGP-driven (not static) on both VPSs** (project): Removed `ip route add 10.0.X.X/27 dev xfrm-nl-f/xfrm-nl` lines from /etc/systemd/system/swanctl-loader.service on notrf01vps01 + chzrh01vps01. FRR now installs the /27 via BGP (proto bgp metric 20 via 10.0.X.X). BFD-driven sub-second failover now actually works for DMZ service traffic.
- **VPS BGP VTI update-source fix** (project): GR VPS peering used loopback IPs causing ASA next-hop resolution failure + cross-tunnel ECMP asymmetric routing. Fixed 2026-04-14.
- **VPS ipsec-health cron enabled on both VPSs — chaos engineering implication** (project): `ipsec-health-check.sh` runs every 2 min on notrf01vps01 and chzrh01vps01 (re-enabled 2026-04-21). Re-initiates any missing strongSwan child SAs via swanctl. Chaos experiments that tear down IPsec tunnels on a VPS will see auto-recovery within 0-2 minutes — factor this into chaos scenarios.
- **VTI BGP outage investigation 2026-04-11** (project): NL-GR inter-site VTI tunnels down, BGP not peering, complete GR unreachability from NL. Root cause identified.
- **vti_dual_wan_lessons** (project): VTI dual-WAN deployment lessons — CrowdSec bans, netlink buflen, port_nat_t, UFW gaps found during 2026-04-09 session
- **VTI Migration + BGP Site Subnet Routing** (project): VTI tunnels (2026-04-09) + full BGP inter-site routing (2026-04-10). No static inter-site routes. 3-tier LP failover: Freedom 200, xs4all 150, FRR transit 100.
- **Unified Knowledge Base Wiki** (project): Karpathy-style LLM-compiled wiki in wiki/ — 45 articles from 7+ sources, RAG-integrated, daily cron at 04:30 UTC
- **xe-gr-waf-camoufox-blocked-20260531** (project): "xe.gr's AWS WAF (deployed 2026-05-30) blocks the Scrapling/Camoufox stealth browser at the same HTTP 405 as reqwest. Native Firefox UA gets HTTP 200 + JS challenge HTML; Camoufox via our sidecar gets straight 405. Adapter permanently kill-switched until a different bypass (Playwright with manual fingerprint, residential proxy, or upstream-officially-permitted API) is found."
- **youtrack_george_perms_and_ram_starvation_20260622** (project): "2026-06-22 — new YouTrack user \"George\" 403'd on every page (added to OH project team but no role); fixed by granting Contributor. Surfaced chronic root cause — nlyoutrack01 VM RAM-starved (4GiB), Xodus GC aborting, perm-cache stalled; restarted container."
- **youtrack_infra_board_triage_20260627** (project): 2026-06-27 triage of open NL/GR infra YouTrack issues. NL=31 open (17 work / 14 alert-generated), GR=4 (1 work / 3 alert). Systemic gap = alert→YT issues auto-CREATE but never auto-CLOSE → noise accrues. 2 genuinely-real items: NL-1333 (pve01 rpool DEGRADED) + GR-85 (bricked PiKVM). ~9 done-this-session items still open.
- **yt_triage_alert_remediation_20260625** (project): 2026-06-25 YouTrack triage (8 issues closed with evidence) + the IFRNLLEI01PRD-1408 commit-label mislabel finding + active-alert remediation (in progress).
- **zai_resume_srvtoolu_id_mismatch_20260630** (project): "claude -r resume 400 \"server_tool_use.id should match ^srvtoolu_\" = Z.ai-recorded transcript has OpenAI-style call_ tool ids that Anthropic rejects on replay."

*Compiled: 2026-07-03 04:30 UTC*